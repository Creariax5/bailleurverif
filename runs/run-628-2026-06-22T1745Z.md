# run-628 — 2026-06-22T17:45Z

## WHY_THIS_NOT_THAT (DIRECTIVE 10)

**Contexte** : critic-91 (06-20T07:01Z, 5.8/10) a flaggé un "polish loop INVERSE" (6 wakes meta-théâtre 619-624). Déjà cassé : run-625 (intent_signal câblage UI), run-626 (chips ville P1), run-627 (validation user-réelle). Pas de nouveau critic/Florian depuis. Levier réel sous-exploité repéré en lisant le funnel : **`email_submitted_lifetime = 0` sur N=6 humains ayant atteint le verdict**. Cause structurelle : la valeur concrète (lettre LRAR + étapes de recours, le moat cat-3) est **verrouillée derrière un email-gate + envoi manuel "sous 24-48h"**. Le template existe (`/api/recourse/<tag>`, 5 étapes + lettre 2053c + 60% succès + 90j + jurisprudence) mais **aucun user ne l'a jamais vu**.

**THIS — Ship produit-excellence P1 : recours instantané au verdict, 0 email-gate** :
Quand verdict loyer-trop-cher (→ `loyer-abusif`) ou DPE F/G (→ `dpe-invalide`), un bouton "📄 Vos étapes de recours + modèle de lettre pré-remplie — gratuit, immédiat, sans email" apparaît dans `#recourse-block`. Au clic : `fetch /api/recourse/<tag>` → panel inline (étapes numérotées + lettre LRAR personnalisée montant exact + délai médian + taux succès + nb décisions justice + bouton "Copier la lettre" + disclaimer ADIL). C'est la définition verbatim du critère P1 mission : *verdict → action concrète, 0 friction, <30s recours*. Attaque directe le dropout `email_submitted=0/6` (la valeur n'est plus otage de l'email). Différenciation explicite vs ANIL/Service-Public (lettre pré-remplie instantanée + jurisprudence). Surface le moat cat-3 jusqu'ici invisible aux users réels. Autonome (0 Florian / 0 Strategic / 0 trigger critic).

**NOT THAT** :
- PAS une N-ième page séquentielle / FAQPage cycle 6 (§G transposition + critic-90 STOP #1 STRICT) — c'est une amélioration produit du flux verdict existant, 0 nouvelle page (`pages_total` UNCHANGED).
- PAS de méta-théâtre "wake X/N acquis" ni carve-out NEG verbatim (critic-91 STOP #1+#2) — ledger format mission-aligné normal.
- PAS de re-gate : l'email reste optionnel (alertes), il n'est plus le mur de la valeur core.
- PAS de push GitHub : token `.env` mort (auth fail empirique persistant) → SB-5 N/A, ledger-only, 0 Florian re-flag.

## SHIP / SMOKE

Code net ~55 LOC : `static/app.js` +52L `renderRecourse()` IIFE (escape HTML, guards null, trackFunnel try/catch, clipboard fallback) ; `static/index.html` +3L `#recourse-block` ; `server.py` +3L whitelist funnel `recourse_viewed` + `recourse_letter_copied`.

Smoke 6/6 PASS :
1. `node -c app.js` + `py_compile server.py` ✓
2. server restart PID live, health HTTP 200 ✓
3. `GET /api/recourse/loyer-abusif` → title + 5 steps + lettre 2053c ✓
4. `POST /api/funnel/event recourse_viewed` → `{"ok":true}` ✓
5. prod nginx : `/static/app.js` grep `renderRecourse` = 1 ✓ (servi live)
6. **Playwright headless iPhone 390×844** `smoke_recourse_run628.py` walk Paris/nu/20m²/1500€/dpe=C → verdict ⚠️ → bouton recours présent → click → 18 `<li>` étapes + lettre 2053c rendus → **PASS EXIT 0** (valeur user-réelle prouvée bout-en-bout).

## État

- visits 507 / humans 9-13 raw UNCHANGED gap ~148h depuis #14 — bruit Poisson dominant
- pages_total=233 UNCHANGED (amélioration flux, pas nouvelle page)
- NEW funnel events `recourse_viewed` + `recourse_letter_copied` (instrumentation P3 : mesure si la valeur instantanée convertit là où l'email-gate échouait)
- PAT 67→68 commits ahead, push auth fail (token mort) — SB-5 N/A, 0 Florian re-flag
- PAS ScheduleWakeup (DIRECTIVE 7). NEXT : cron. Observer `recourse_viewed` vs `email_submitted` T+7j (hypothèse : recours instantané > email-gate pour délivrer la valeur).
