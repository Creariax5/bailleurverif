# Run-110 — 2026-05-16T13:20Z — Form signup étendu aux 50 pages DPE F/G (×50 surface)

## CONTEXT
15e wake exécutif mission B2C 5000 users / 90j. Wake +15 min après run-109 (DIRECTIVE 5 pacing respecté). Florian silence depuis 09:40Z. Plan run-109 NEXT prévu : étendre form signup aux 50 pages DPE F/G via patch `build_dpe_pages.py` topic `dpe-bailleur` — c'est la dernière grande surface tenant non équipée. État entrée : 3 pages avec form (run-108 + run-109). Sortie cible : 53 pages tenant produit avec form signup live.

## ASSESS
- 50 fichiers `*-dpe-f-g-interdit-location.html` générés par `build_dpe_pages.py`, ratiosse template Tailwind utility light theme (post-DIRECTIVE 6 phase 1).
- Topic `dpe-bailleur` réservé run-108 dans l'endpoint allowlist mais jamais activé.
- Source URL doit être ville-spécifique pour tracker la conversion par ville plus tard (`/{slug}-dpe-f-g-interdit-location.html`).
- Risque idempotence : un `python3 build_dpe_pages.py` standalone réécrivait le sitemap à 93 URLs (perdait `widget-bailleurverif.html` et `locataire-loyer-legal.html`). À fixer en parallèle pour éviter régression sitemap.
- Risque collision IDs : 53 pages exposent `#bv-subscribe-form` mais chacune chargée isolément, scope IIFE.

## PLAN
1. Patch `build_dpe_pages.py` : insérer aside `#alerte-maj` + form `topic=dpe-bailleur` + script IIFE, entre le 2e CTA-wedge aside et le `<hr>` disclaimer.
2. Source dynamique `/{slug}-dpe-f-g-interdit-location.html` (f-string injection).
3. Copy DPE-spécifique : "Soyez prévenu si le calendrier DPE évolue (à {ville})" + mention reports F/G/E, MaPrimeRénov', méthode 3CL, jurisprudence "logement non décent".
4. Fixer parité sitemap : ajouter `widget-bailleurverif.html` et `locataire-loyer-legal.html` au `tools_pages` des deux builders (build_dpe_pages.py + build_programmatic_pages.py) pour qu'ils restent dans sitemap quel que soit le builder qui tourne en dernier.
5. Re-run `build_dpe_pages.py` → 50 pages régénérées.
6. Vérif live : 3 pages échantillon HTTP 200 + grep `alerte-maj` + grep `dpe-bailleur`.
7. Smoke E2E HTTPS 8/8 : baseline, subscribe paris, subscribe lyon, stats pending, confirm Paris, confirm Lyon, stats confirmed/signups_24h, negative consent missing.
8. Purge smoke entries → baseline 0/0/0/0.
9. IndexNow round-15 : 50 URLs DPE à api.indexnow.org + Bing + Yandex.
10. Update state/ledger/tasks/inbox/metrics + run report.

## ACT

### 1. Patch `build_dpe_pages.py` (+~85 lignes template)
- Inséré après le 2e CTA-wedge aside et avant `<hr class="my-10 border-slate-200" />`.
- Form HTML : `<input type="hidden" name="topic" value="dpe-bailleur">` + `<input type="hidden" name="source" value="/{slug}-dpe-f-g-interdit-location.html">` + email + consent checkbox + submit.
- Script IIFE : POST `/api/subscribe` topic=`dpe-bailleur` source=`/{slug}-dpe-f-g-interdit-location.html`, gère 200/already_confirmed/429/400/network, affiche lien confirm inline (workaround TODO-20 SMTP).
- Curly braces JS escapées (`{{...}}`) pour f-string Python — `{ville}` et `{slug}` injectés dynamiquement par ville.
- Style Tailwind utility light, cohérent avec préavis-bail.html (run-109 levier e).

### 2. Patch parité sitemap (build_dpe_pages.py + build_programmatic_pages.py)
- `build_dpe_pages.py` `main()` : refactor `urls = [BASE_URL+"/"]` puis ajouter `tools_pages = ["preavis-bail.html", "widget-bailleurverif.html", "locataire-loyer-legal.html"]` filtrés sur existence — 3 builders convergent désormais sur le même set.
- `build_programmatic_pages.py` : ajout `"locataire-loyer-legal.html"` au `tools_pages` existant (`preavis-bail.html`, `widget-bailleurverif.html`).
- Résultat : `python3 build_dpe_pages.py` produit maintenant 95 URLs (idempotent avec run-106 build_programmatic).

### 3. Rebuild 50 pages
`python3 dashboard/build_dpe_pages.py` :
- `Generated 50 DPE pages.`
- `Sitemap rebuilt with 95 URLs.`
- Healthcheck 3 pages : `/` 200/27457, `/paris-dpe-f-g-interdit-location.html` 200/31858, `/lyon-dpe-f-g-interdit-location.html` 200/31791.
- `grep -l alerte-maj wedge-tool/static/*-dpe-f-g-interdit-location.html | wc -l` = **50/50**.
- `grep -c dpe-bailleur wedge-tool/static/paris-dpe-...` = 2 (hidden input + JSON body), idem Lyon = 2.

### 4. Smoke E2E HTTPS — 8/8 OK
| # | Cas | Résultat |
|---|---|---|
| 1 | `/api/stats` baseline | pending=0, confirmed=0, signups_24h=0 ✅ |
| 2 | POST `/api/subscribe` topic=dpe-bailleur source=/paris-dpe-... consent=true | 200 `{ok, confirm_url, unsubscribe_url}` ✅ |
| 3 | POST `/api/subscribe` topic=dpe-bailleur source=/lyon-dpe-... consent=true | 200 `{ok, confirm_url, unsubscribe_url}` ✅ |
| 4 | `/api/stats` après 2 subscribe | pending=2 ✅ |
| 5 | GET `/api/confirm?token=A` (Paris) | 200 ✅ |
| 6 | GET `/api/confirm?token=B` (Lyon) | 200 ✅ |
| 7 | `/api/stats` après 2 confirm | confirmed=2, signups_24h=2 ✅ KPI live |
| 8 | POST `/api/subscribe` sans `consent` | 400 ✅ |

### 5. Purge smoke
4 entrées (2 subscribe + 2 confirm) → `subscribers.jsonl` vidé. `/api/stats` baseline 0/0/0/0 propre.

### 6. IndexNow round-15
50 URLs DPE submises (toutes les communes), 3 endpoints :
- api.indexnow.org : HTTP **200** ✅
- www.bing.com/indexnow : HTTP **200** ✅
- yandex.com/indexnow : HTTP **202** ✅

## VERIFY
- `wedge-tool/static/*-dpe-f-g-interdit-location.html` : 50 fichiers.
- `grep -l "alerte-maj" wedge-tool/static/*-dpe-...` : **50/50** ✅
- `grep -c "<url>" wedge-tool/static/sitemap.xml` : 95 (vs 95 pré-run, parité préservée).
- `grep "locataire-loyer-legal\|widget-bailleurverif" sitemap.xml` : présents ✅ (parité fixée).
- `/api/stats` baseline 0/0/0/0 post-purge ✅.
- 3 endpoints IndexNow OK ✅.
- DIRECTIVE 6 light theme : palette CSS vars existantes (--accent, --border, --text-secondary) + classes Tailwind light identiques au template DPE — 0 dark résidu ajouté.
- 0 régression endpoints autres (`/api/visit`, `/api/capture`, `/api/result`, `/api/share`, `/api/feedback`, `/api/embed/*`).
- Gel run-103 « pas de nouvelle page » respecté : 50 pages déjà existantes, juste patch template (re-skin signup, pas création).

## METRIC
| Métrique | Avant | Après |
|---|---|---|
| `pages_with_signup_form_live` | 3 | **53** |
| `signup_form_topics_live` | 3 | **4** (dpe-bailleur activé) |
| `dpe_pages_with_form_signup` | 0 | **50** |
| `endpoints_api_count` | 12 | 12 (réutilisation 100%) |
| `humans_engaged_lifetime` | 0 | 0 (110e wake honnête, surface ×17 d'un coup) |
| `signups_24h` | 0 | 0 (mesurable, 0 humain réel) |
| `indexnow_rounds_lifetime` | 14 | **15** |
| `urls_soumises_indexnow_lifetime` | ~122 | **~172** (+50) |
| `sitemap_urls` | 95 | 95 (parité préservée post-patch parité) |
| `wakes_executifs_nouvelle_mission` | 14 | **15** |
| `leviers_cyclés` | 8/8 | 8/8 (e ré-activé 3e fois sur ×50 nouvelles surfaces) |
| `dépense` | 0 | 0 |
| `nouvelle page produit` | 0 | 0 (gel run-103 respecté) |
| `régression` | 0 | 0 |
| `bugs_latents_fixés_lifetime` | 4 | **5** (parité sitemap entre 2 builders) |

## DECISION
- **Branche A (signup ×50 surface)** retenue sur (B) fallback Reddit browser-bridge. Justifs : (i) plan explicite run-109, (ii) ROI structurel max (50 surfaces signup d'un coup, audience DPE = 5,2M propriétaires F/G concrètement concernés en FR), (iii) déterministe / 0 dépendance externe, (iv) coût marginal nul (data + template existent), (v) idempotent.
- **Bonus parité sitemap** non planifié mais détecté pendant ACT : régression de 95→93 URLs au 1er rebuild standalone. Fix appliqué les 2 builders (idempotent) — préviens toute future régression.

## NEXT (run-111)
Plan probable, sans attendre Florian (silence +3h40) :
- **Branche A** : valider si Bing a crawlé une des URLs IndexNow round-1→14 (`site:bailleurverif.fr` via WebSearch Bing direct ou WebFetch DuckDuckGo) — confirmer/infirmer "indexnow live" effectif vs nominal.
- **Branche B** : levier (g) viralité re-cyclé — programme referral ou A/B copy hero homepage (gros levier conversion à 0 trafic = roi nul, donc plutôt B prioritaire = capture quand trafic arrivera).
- **Branche C** : levier (d) outreach autonome — tester si je peux soumettre Findly.tools via Browserbase (cf. TODO-19 ★★★) sans attendre Florian. Si Gmail scope ok dans browser-bridge → essai signup autonome (réversible, 0€).
- **Fallback honnête** : si toutes branches bloquées, levier (h) content authority : créer 1 page guide DPE national agrégateur (sans ajouter nouvelle ville).

ScheduleWakeup 180s.
