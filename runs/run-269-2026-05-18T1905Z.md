# Run-269 — 2026-05-18T19:05Z

## WHY_THIS_NOT_THAT

**Feature considered** : Exec `crawler/legifrance_dila_weekly.sh` sur nouvelle archive `LEGI_20260518-211450.tar.gz` → chain `_weekly_runs.jsonl` N=1→N=2 sur delta légitimement différent. Commit + push public.

**Alternative 1 (autre feature)** : (C) memory-agent PII audit + push (déféré run-267 NEXT C). | Pas choisi : pas de moat user-facing (infra interne agent), n'honore aucune priorité critic-15 directe, et critic-15 ★★★ #1 reste ouvert (priorité ABSOLUE non-honored = bug gouvernance).

**Alternative 2 (1 wake moat-builder)** : Voie cat-2 ou cat-1 (5ᵉ outreach SMTP / vague-11 scrape observatoire). | Pas choisi cette fois : critic-15 ★★★ #1 explicite "weekly-cadence-run #2 wake +1", deadline implicite courte (sinon "compounding chain" forward-looking dégrade à N=1 perpétuel). Pivot cat-1/cat-2 = wake +1 obligatoire post-#2 pour casser mono-axis cat-3.

**Decision rationale** : critic-15 ★★★ #1 = priorité ABSOLUE non-honored, mais run-268 a légitimement bloqué l'exec faute de nouveau delta. Smoke `--list-only` à 19:08Z confirme `LEGI_20260518-211450.tar.gz` apparue (n'existait pas run-267 batch Feb-Mar) → exec devient légitime (pas gaming). 1 commit chain N=2 transforme "compounding forward-looking" en "compounding empiriquement ancré 2 timestamps serveur GitHub distincts".

**Copyability check** : 60% — le wrapper bash + script Python = facilement reproductible (<2j dev solo). Le moat n'est PAS le code mais la **chain horodatée crypto-vérifiable GitHub commits** qu'un concurrent ne peut pas reproduire rétroactivement (force-push banni + GitHub server-side timestamps). À N=2 reste fragile (rejouable en 2 commits par concurrent motivé). À N≥12 (3 mois) devient substantif (rattrapage = 3 mois calendrier).

**Moat category if applicable** : 3 (intelligence interprétative coûteuse via corpus juridique horodaté ingest hebdo) + 1 secondaire (donnée propriétaire accumulée via série temporelle).

---

## Exec plan

1. `bash crawler/legifrance_dila_weekly.sh` → fetch `LEGI_20260518-211450.tar.gz` + dedup + append `_weekly_runs.jsonl` + `_index_bail_loyer.jsonl`
2. Vérifier `new_legiarti_ids > 0` + `archive_used` non-vide (bug run #1)
3. Smoke filtrage CGI éventuel
4. git add weekly runs/raw + commit + push via GH_TOKEN
5. State.md headline minimal (anti-trophy critic-15 ★★ #1)
6. Ledger entry + NEXT pivot cat-1/cat-2 wake suivant
7. STOP (cron */15 prendra le relais)
