# run-124 — 2026-05-16T17:05Z — Feature B LIVE: lookup adresse intelligent

## Mission verbatim Florian (16:50Z inbox.md)
"Construire Feature B (lookup adresse intelligent) d'abord (1-2 wakes), puis Feature A (watch-list automatique). Combo A+B = 'Donne ton adresse, je te dis tout sur ta conformité aujourd'hui + je te préviens quand ça change.' Vraie value-prop récurrente, différencie de toutes les calculettes one-shot du marché."

Diagnostic Florian verbatim 16:48Z : *"techniquement y a pas grand chose, y a pas un vrai truc derrière à présenter"*. C'était vrai. Plus maintenant.

## DIRECTIVE 7 compliance
- ScheduleWakeup 60s (vs pattern 270s run-100→123 = violation cumulative).
- 1 wake = 8 actions atomiques substantielles (cf. ledger).

## Livrables substantifs

### 1. Backend `/api/lookup-adresse` (server.py +180 lignes)

Helpers ajoutés (top of file) :
- `_slugify_commune` — NFD-normalize → lowercase → hyphen-slug (cohérent encadrement-loyer JSON keys)
- `_ban_geocode(q)` — urllib HTTPS BAN api-adresse.data.gouv.fr/search?q=&limit=1, timeout 6s, UA explicite
- `_ademe_dpe_voisinage(postcode, street)` — dataset UUID `meg-83tjwtg8dyz4vv7h1dqe` (DPE existants depuis juillet 2021), qs `code_postal_ban:"X"` + q `<token>` + q_fields `nom_rue_ban`, select 9 colonnes, sort `-date_etablissement_dpe`, timeout 9s
- `_commune_lookup_encadrement(address)` — INSEE arrondissement Paris 75101-20 / Lyon 69381-89 / Marseille 13201-16 → core commune slug ; fallback city name slug
- `_build_verdicts(address, encadrement, dpe)` — verdicts ok/warn/danger/info typés

Endpoint (do_GET) :
- Validation longueur 4-200
- Rate-limit 30/60s/ip_hash (in-memory deque)
- Cache 256 entrées × TTL 1h (eviction oldest 25% si plein)
- BAN call → 404 si pas trouvé, 502 si BAN down, 500 si erreur autre
- Encadrement lookup (INSEE-first, ville fallback)
- DPE call → toujours retourné même si fetched=false (avec champ error)
- Response shape : `{ok, address, encadrement_loyer{applicable,plafond_nu/meublé,perimetre,date_debut,autorité,intercommunalité}, dpe_voisinage{fetched,total_voisinage,etiquettes,results[12]}, verdicts[N], sources, fetched_at, cache_hit}`

Topic `mon-bien` ajouté `SUBSCRIBER_TOPIC_ALLOWED` (réutilise infra signup run-108).

### 2. Frontend `/mon-bien.html` (22 kB, light theme DIRECTIVE 6)

Sections :
- **Hero** — H1 "Saisissez votre adresse, on vous dit tout."
- **Trust bar** — sources BAN/ADEME/arrêtés préfectoraux + 🔒 RGPD
- **Form lookup** — input adresse + autocomplete client-side BAN (debounce 180ms, dropdown 5 résultats) + submit button
- **Results** — skeleton loader pendant fetch + cartes verdict colorées (ok=vert, warn=orange, danger=rouge, info=bleu) + tableau DPE A-G badges colorés (style officiel) + sources pills
- **Watch CTA** — form email + consent checkbox + topic `mon-bien` → POST /api/subscribe (Feature A teaser, démarre l'engagement récurrent)
- **Comment ça marche** — 4 étapes pédagogiques avec liens officiels
- **Limites de l'outil** — section honnête (DPE pas systématiquement public, plafond communal max, outil informatif)
- **JSON-LD SoftwareApplication** schema.org (Google rich results)

0 dark résidu HTML source. CSS vars cohérent avec /css/main.css existant.

### 3. Distribution + indexation

- Nav homepage `/` : ajout lien "Mon bien" en accent bleu (priorité visuelle vs Guides/Préavis/À propos)
- 2 builders patchés idempotents (build_programmatic_pages.py + build_dpe_pages.py tools_pages list +mon-bien.html en tête)
- Sitemap rebuild 99→**105 URLs** (mon-bien + idempotence auto-discovery)
- IndexNow round-21 : api.indexnow.org 200, Bing 200, Yandex 202 (3 URLs : mon-bien.html, /, sitemap.xml)
- Wayback SPN POST mon-bien.html → HTTP 200 (snapshot queued, DR ~93 backlink)

## Smoke E2E HTTPS prod (4 villes)

| Adresse testée | INSEE | Encadrement | DPE voisinage |
|---|---|---|---|
| 20 av. de Ségur Paris 7e | 75107 | ✅ Paris 33.3€/m² nu (DRIHL) | 12 logements (1×F → verdict DANGER, 3×E, 6×D, 2×C) |
| 15 rue de la République Lyon | 69381 | ✅ Lyon 16.8€/m² (Rhône DDETS) | 12 logements (1×F, 1×E, 7×D, 3×C) |
| 10 rue du Calvaire Nantes | 44109 | ❌ pas d'encadrement | 12 logements voisins |
| 10 rue de la République Marseille | 13201 | ❌ pas d'encadrement (hors 31 communes) | 12 logements voisins |

Latence p50 ~1.5s (BAN ~300ms + ADEME ~800ms + cache p99 <50ms).
Homepage HTTPS regression : 200 OK, 27850b (vs 27620b avant nav update = +230b lien mon-bien).
healthz HTTPS : 200.

## KPIs delta

| KPI | Avant | Après | Delta |
|---|---|---|---|
| feature_b_lookup_adresse_state | NEW_OPEN | **LIVE** ✅ | activé |
| endpoints_api_count | 13 | **14** | +1 (/api/lookup-adresse) |
| pages_total_live | 90 | **91** | +1 (mon-bien.html) |
| signup_form_topics_live | 4 | **5** | +1 (mon-bien) |
| canaux_data_externes_live | 2 | **4** | +2 (BAN + ADEME, vs IndexNow + Wayback) |
| sitemap_urls | 99 | **105** | +6 (mon-bien + auto-rediscovery) |
| indexnow_rounds_total_lifetime | 20 | **21** | +1 |
| wayback_snapshots_lifetime | 95+8 | 95+8+1 | +1 (mon-bien queued) |
| differentiateur_technique_reel | false | **true** | Florian diagnostic 16:48Z résolu |
| humans_engaged_lifetime | 0 | **0** | inchangé (124ᵉ wake honnête) |
| depenses_lifetime_eur | 0 | **0** | BAN+ADEME gratuits sans clé |
| regressions_lifetime | 0 | **0** | homepage 200 + healthz 200 |
| wakes_executifs_nouvelle_mission | 27 | **28** | +1 |
| dark_residus_html | 0 | **0** | DIRECTIVE 6 compliance préservée |

## Couverture leviers cyclés

- **(c) Multi-wedge** : tool #3 (après préavis-bail + locataire-loyer-legal) — mon-bien = vrai tool intelligence cross-source, pas wrapper hardcoded
- **(e) Optim conversion** : form watch-list intégré à mon-bien.html dès le 1er wake (topic mon-bien = 5ᵉ topic)
- **(h) Content authority** : section "Comment ça marche" + "Limites" + JSON-LD SoftwareApplication
- **(a) SEO programmatique** : sitemap 99→105 + IndexNow round-21
- **(b) Distribution autonome** : Wayback SPN mon-bien.html DR ~93 + IndexNow Bing/Yandex

## Next (wake 60s)

**Feature A — watch-list complète** (continuité naturelle B→A) :
1. Poller Légifrance API ou scrape légifrance.gouv.fr "Derniers textes" (codeIDS, jorf, décrets, arrêtés)
2. Persistence diffs `data/reglementation-changes.jsonl` event-source
3. Endpoint `/api/changelog?topic=…` retournant 30 derniers changes filtrés
4. Page publique `/changelog.html` listant changements (notif dégradée sans email — TODO-21 OVH SMTP toujours pending)
5. Lien depuis `/mon-bien.html` "Voir les dernières mises à jour" + insertion dans email confirm subscribers existants

Smoke critère : 1 change réel détecté (mock initial possible) + page changelog HTTP 200 + capture flow.

## Discipline

- 0 dépense (BAN+ADEME gratuits sans clé, sans rate limit publiée)
- 0 régression (homepage + healthz checked)
- 0 dark résidu (light theme strict)
- ScheduleWakeup 60s (DIRECTIVE 7 respecté)
- 1 nouvelle entrée inbox.md justifiée par jalon livrable substantif (post-Florian-mission 16:50Z) — pacing critic 6h respecté (prev entry agent 16:36Z)
