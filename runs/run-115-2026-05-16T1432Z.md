# Run-115 — 2026-05-16T14:32Z

## Plan (entrée)

Continuer inventaire canaux autonomes commencé runs 113/114. Tester :
- WebSub / PubSubHubbub publish (hubs Google + Superfeedr)
- Yandex Sitemap Ping (protocole legacy mais Yandex toujours actif)
- Bing Sitemap Ping (legacy, vérifier déprécié ou pas)
- Vérifier état job Wayback run-113 background (devait terminer ~14:30Z)

## Actions exécutées

1. **Yandex Sitemap Ping** `GET https://webmaster.yandex.com/ping?sitemap=https://bailleurverif.fr/sitemap.xml` → **HTTP 200** (size 11 bytes). Sitemap déclaré côté Yandex. Canal autonome confirmé NEW.

2. **Google PubSubHubbub publish** `POST https://pubsubhubbub.appspot.com/` avec `hub.mode=publish&hub.url=https://bailleurverif.fr/atom.xml` → **HTTP 204** (accepté, no content = success PSHB). Canal autonome confirmé NEW.

3. **Superfeedr PubSubHubbub publish** `POST https://pubsubhubbub.superfeedr.com/` même payload → **HTTP 204**. Canal autonome confirmé NEW (hub indépendant Google, redistribue à Superfeedr subscribers + crawlers).

4. **Bing Sitemap Ping (legacy)** `GET https://www.bing.com/ping?sitemap=...` → **HTTP 410 Gone**. Confirmé déprécié (IndexNow est le remplacement officiel Bing depuis 2022). Canal noté négatif.

5. **Patch durable `dashboard/build_blog.py`** (+2 lignes dans `write_atom_feed`) : ajout `<link rel="hub" href="https://pubsubhubbub.appspot.com/" />` + `<link rel="hub" href="https://pubsubhubbub.superfeedr.com/" />` après self-link Atom 1.0. Sans ça, les abonnés du feed ne savent pas où subscribe au hub → WebSub partiel. Maintenant W3C-conforme.

6. **Rebuild blog** `python3 dashboard/build_blog.py` → OK 6 entries atom, sitemap 94 URLs, feed.json 6 items, robots.txt re-écrit avec AI bots allowlist. Idempotent.

7. **Vérif hub links live** `curl atom.xml` → 2 `<link rel="hub">` confirmés visibles. Discovery feed (`<link rel="alternate">` dans `/` et `/blog/`) déjà en place runs précédents.

8. **Re-publish PSHB Google post-rebuild** → HTTP 204 (hub notifié de la nouvelle update).

9. **IndexNow universal push** atom.xml + feed.json + sitemap.xml (3 URLs) via `api.indexnow.org/IndexNow` → HTTP 202.

10. **Bug fix critique — Wayback resume** : diagnostic process PID 786450 alive mais log stuck à 30/95 depuis 14:18Z. Inspection commande lance : `... | head -30` consommait stdout, `head` exit après 30 lignes → pipe SIGPIPE pour `tee -a` → log writes silencieux après. **curl continuait à fire mais sans trace**. 
    - Killed PID 786450 + sleep enfant 795723.
    - 28 OK + 2 FAIL confirmés log (URLs 1-30).
    - **Resume script propre** : redirect direct `>> wayback-submissions.log` (pas de pipe broken-able), `tail -n +31 sitemap_urls.txt`, lance background.
    - Vérif resume : 31 bobigny OK / 32 le-pre-saint-gervais OK / 33 les-lilas OK (logged direct, no pipe). Process actif PID 795972.

## Vérifs post-action

- `curl atom.xml | head -10` → confirme 2 hub links.
- `grep -c "OK" wayback-submissions.log` = 31 (28 ancienne + 3 reprise).
- Process wayback resume alive (curl PID 796xxx + bash 795972).
- 0 régression : sitemap 94 inchangé, healthz wedge tool toujours up.

## KPI deltas

- `wakes_executifs_nouvelle_mission` 19 → **20**
- `canaux_autonomes_decouverts_lifetime` 5 → **8** (+3 : Yandex ping, Google PSHB, Superfeedr PSHB)
- `canaux_autonomes_testes_negatifs_lifetime` 6 → **7** (+1 : Bing ping 410)
- `websub_hub_declared_in_atom` false → **true**
- `google_pubsubhubbub_publish_lifetime` 0 → **2** (publish + re-publish post-rebuild)
- `superfeedr_pubsubhubbub_publish_lifetime` 0 → **1**
- `yandex_sitemap_ping_lifetime` 0 → **1**
- `bugs_latents_fixés_lifetime` 5 → **6** (wayback log truncation pipe)
- `wayback_seed_progress` 30/95 → **33/95** + resume actif, ETA ~7 min
- `humans_engaged_lifetime` 0 (inchangé, 115e wake honnête)
- `urls_soumises_indexnow_lifetime` ~272 → **~275** (+3 feeds)
- `indexnow_rounds_total_lifetime` 18 → **19**

## Asymétrie attendue

WebSub publish notifie les hubs qui notifient les abonnés (Feedly, Inoreader, NewsBlur etc.). Quelques-uns sont crawlés par Googlebot → 2ᵉ voie potentielle de découverte indirecte (similaire à hypothèse Wayback run-113). Si combinaison Wayback + WebSub fait découvrir bailleurverif.fr d'ici 48-72h, on aura cassé le blocage indexation Google **sans aucune intervention Florian**.

## Méta-leçon

Quand un script tourne en background, **toujours stdout `>>` file direct, jamais `| head`/`| tail`** : la pipe broken kill silencieusement les logs même si le process continue. Pattern à inscrire dans tasks.md pour réutilisation future.

## Décision pour run-116

- Surveiller progression Wayback (devrait être à ~60-70/95 d'ici ~5 min).
- Tester 2-3 autres canaux autonomes : WebMention.io ping, RSS Inoreader subscribe-on-publish (si endpoint anon), Brave Search no-direct-submit (skip).
- Si Wayback complet : test empirique `site:web.archive.org/web/*/bailleurverif.fr` Google pour voir si snapshots déjà indexés.

## Engagements respectés

- 0 dépense
- 0 régression (rebuild idempotent verified)
- 0 demande Florian impérative (3 canaux autonomes = sans intervention)
- DIRECTIVE 5 pacing actif (~20 min depuis run-114, sous 300s cible quand productif)
- RGPD intact
- CGU intactes (Wayback SPN + PSHB sont APIs publiques destinées à cet usage)
