# run-254 — 2026-05-18T09:25Z — Locservice DETAIL JSON-LD richesse probe SHIPPED

48ᵉ session DIRECTIVE 7 RÉVISÉE conforme (0 ScheduleWakeup, cron tick ~09:15Z).

## WHY_THIS_NOT_THAT

**Feature considered** : Probe 1 URL Locservice DETAIL listing JSON-LD pour décision upgrade `crawler/locservice_v0.py` regex → JSON-LD.

**Alternative 1 (autre feature)** : Patcher directement `locservice_v0.py` ce wake sans probe préalable | rejeté = crawler en steady-state production (4 villes/jour cron `*/30`) + 0 donnée empirique sur richesse JSON-LD detail = risque casser pipeline. Probe = decoupling = wake +N patch avec confiance.

**Alternative 2 (1 wake moat-builder)** : Explorer sources open-data alternatives non-anti-bot (OpenStreetMap Overpass / WikiData annonces / cadastre.data.gouv.fr) pour diversifier SPOF Locservice | rejeté ce wake = trop ouvert (≥3 sources à évaluer, ≥10min chacune), incompatible cron 15min. Probe Locservice DETAIL = test focused 1 URL, dérive concrète + actionnable wake +N. Alternative reste sur la table (run-253 PISTE #2).

**Decision rationale** : Probe 1 URL = test minimal pour répondre OUI/NON à "upgrade vaut le coup" avec donnée empirique. Plus asymétrique que 4 sources alternatives mal échantillonnées.

**Copyability check** : 85% (dev solo refait probe en 30min). MAIS le **finding** (Locservice DETAIL expose `apartment` JSON-LD structuré dans NOTRE niche) est une intelligence qui compose avec notre infra crawler 9 vagues. Upgrade wake +N = moat-cat-1 facette défendabilité directe (anti-fragile parsing + structured address.postalCode + floorSize.value).

**Moat category if applicable** : 1 (données propriétaires accumulées — facette anti-fragilité parsing).

## Test "Demain disparition" (DIRECTIVE 10 §c)

UNCHANGED 4 composants : (1) obs N=232 + 9 vagues git history horodatée ; (2) data.gouv.fr v1 dataset ; (3) `/api/recourse` 3 templates CC-BY-4.0 + endpoint ; (4) GitHub crypto-timestamp commit `8840c77`. + cross-source DVF×loyer 17 communes publié run-251.

Probe ≠ nouveau composant. Mais débloque PISTE concrète crawler upgrade JSON-LD wake +N qui augmenterait défendabilité parsing (vs regex fragile).

## Actions

1. **Inbox audit** 09:23Z : 0 nouveau msg Florian T+~13h depuis briefing run-244 (notre run-253 self-write 08:30Z reste top).
2. **IMAP poll** 09:24:25Z = 0 UNSEEN (DAL T+~3h25 + FAP T+~2h + 4/4 presse silent T+14h+).
3. **Locservice DETAIL JSON-LD probe** : `agent-browser/locservice_detail_jsonld_probe.py` 130 LOC. 1 URL `/paris-75/location-appartement-paris-17/2413067` plain urlopen UA `BailleurVerifCompliance/0.1` 20s timeout. Schema-only summary (pas de stockage valeurs PII).
4. **Résultat** : HTTP 200 / 93682 chars body / 2 JSON-LD blocks / 0.24s elapsed.
   - **Block 1** : `BreadcrumbList` (nav only, non utile).
   - **Block 2** : `@type=apartment` keys `[address, description, floorSize, photos, potentialAction]`.
     - `address` = PostalAddress with `[addressCountry, addressLocality, postalCode]` ✅
     - `floorSize` = QuantitativeValue with `[unitCode, unitText, value]` ✅
     - `potentialAction` = RentAction (seulement `@type`, **pas de price nested**) ❌
     - **PAS de `energyEfficiencyRating`** dans JSON-LD ❌
   - DPE marker `/dpe/energie-D.{hash}.png` présent en HTML brut hors JSON-LD ✅ (regex actuelle marche toujours).
5. **Output** : `data/locservice-detail-jsonld-probe-run254.json` (3KB schema summary, 0 PII).

## Diagnostic moat-cat-1 facette défendabilité directe

- **Upgrade partiel actionnable** : `address.postalCode` + `floorSize.value` peuvent migrer regex → JSON-LD = anti-fragile + cleaner data.
- **Pas d'upgrade total** : price reste regex (extraction card index), DPE reste regex filename photos.
- **Hybrid approach optimal** : JSON-LD primary pour CP/surface, fallback regex si JSON-LD absent. ~30 LOC patch sur `parse_detail_dpe()` → `parse_detail_full()`.
- **Quand patcher** : wake +N (besoin de valider sur 3-5 URLs supplémentaires multi-villes pour confirmer schema stable avant casser pipeline production).

## Skip volontaire ce wake

- PAS patch `locservice_v0.py` direct (steady-state production, validate sur N URLs avant).
- PAS 4ᵉ template cat-3 (anti-amplification strategic warning maintenu).
- PAS 3ᵉ outreach SMTP (cooldown 30min runbook + 2 outreach déjà 06:00Z+07:25Z).
- PAS Browserbase (cron 15min budget).
- PAS Claude API externe (TODO-26 silent T+~6h).
- PAS git push (commit groupé wake +N).
- PAS ré-évocation TODOs cooldown <24h.
- PAS auto-post compte agent (self-policy).
- PAS scrape continu PAP/AvendreALouer (anti-bot wall = éthique bypass refus).

## NEXT (run-255 ~09:30Z OU 09:45Z cron tick, PAS ScheduleWakeup)

(A) Inbox audit Florian priorité MAX.
(B) IMAP poll DAL+FAP+presse (créneau lundi matin pro 09-11h Paris = 07-09 UTC closing, P50 DAL/FAP ≥48h asso).
(C) TODO-24 api-key → republish v3.
(D) **PRIORITAIRE moat-cat-1** : probe 2-3 URLs supplémentaires multi-villes Locservice DETAIL (Lille / Marseille / Lyon) confirmant `apartment` JSON-LD schema stable AVANT patch crawler.
(E) Si schema stable confirmé sur ≥3 URLs : patch `locservice_v0.py` JSON-LD hybrid parsing (parse_detail_full) + smoke test 1 commune.
(F) Sinon : probe sources alternatives non-anti-bot (run-253 PISTE #2 — OpenStreetMap Overpass / WikiData / sous-domaines particuliers / RSS feeds régionaux).
(G) Check `/api/dvf-stats/*` log first external bot fetch T+~2h+ post IndexNow round-66.
(H) "stop" Florian → arrêt.

Bans inchangés : PAS Mission 2 / PAS 3ᵉ canal cat-2 / PAS GET stats / PAS reskinning / PAS 5ᵉ press / PAS publish HTML Δ<50 / PAS auto-post / PAS Claude API externe / PAS sed observatoire / PAS amplifier cat-1 (sauf facette nouvelle = JSON-LD upgrade) / PAS spoof UA / PAS bypass anti-bot éthique.

**0 ScheduleWakeup. Cron prend le relais.**
