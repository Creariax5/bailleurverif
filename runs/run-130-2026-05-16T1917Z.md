# Run-130 — 2026-05-16T19:17Z

**Type** : Substance code/SEO interne (post-meta wake). Exécution NEXT runs-128 (option B hub internal links) + 129 (option B Wayback SPN). 7ᵉ wake consécutif DIRECTIVE 7 ZERO-POSE (60s).

---

## CONTEXT

Run-129 a écrit la synthèse Florian unique + investigué visite ip_hash + mesuré SERP J+1 + confirmé 4 Googlebot crawls (1er à T+31min post-GSC). Substance technique du wake 129 = meta/honesty. Run-130 doit livrer code/SEO concret car DIRECTIVE 7 = "1 action substantive par wake".

Plan executable autonome (sans dépendance Florian) : option B des deux NEXT runs précédents = **patch hub `/preavis-bail.html` avec section "Par ville · 50 communes" en grille internal-links**. C'est le coup d'horloge SEO compounding évident — chaque ville a sa landing depuis run-128, mais sans le hub maillé, Googlebot ne distribue pas le PageRank vers les 50 enfants. Indexation profonde bloquée structurellement sans ce patch.

## ASSESS

### Pourquoi maintenant et pas plus tard

- Run-128 a livré 50 city pages MAIS sans link section dans le hub → 50 orphan pages relatives.
- Run-129 a confirmé Googlebot actif (4 crawls 31min post-GSC). Si crawler tombe sur le hub MAINTENANT sans les 50 links, il rate les filles. Patch maintenant = augmenter recall index Google de 1→51 pages dans les prochaines fenêtres crawl.
- Coût wake : 5 min (générer block + Edit). Bénéfice : compounding sur tout l'horizon indexation 7-30j.

### Pourquoi pas plus ambitieux

- Critic 18:50Z a flaggé "polish vanity" sur runs 126/127. Une feature complexe = risque autre polish trophée.
- Cette tâche est de l'**infrastructure-SEO**, pas du polish — c'est ce qui transforme 50 pages stockées en 50 pages indexable.
- Builder IRL (option D runs-129) reportée à run-131 : ajoute matière, mais a moins de levier que le maillage interne d'inventory existant.

## ACT

### Step 1 — Génération block HTML grille 50 villes

`python3` regex extraction des 50 titres + statut zone-tendue → mapping slug→(ville_display, ZT|HZ).
**Result** : 30 villes ZT (·1mo emerald) + 20 HZ (·3mo slate). Tri alphabétique.

Output : `/tmp/par-ville-block.html` (8597 bytes, 54 lignes HTML).

### Step 2 — Edit hub `/preavis-bail.html`

Insertion entre la CTA wedge bonus (line 220 `</aside>`) et la section "Outils complémentaires" (line 222 `<h2>`).

**Bloc inséré** :
- `<h2 id="par-ville">Préavis par ville · 50 communes</h2>`
- Sous-titre stat 30 ZT + 20 HZ.
- `<div class="grid grid-cols-2 sm:grid-cols-3 md:grid-cols-4">` : 50 `<a>` avec badge `·1mo`/`·3mo`.

Cible SEO :
- Internal-link mass 50 vers les filles depuis le hub (boost crawl découvrabilité).
- Anchor text porteur de mot-clé ville (50 anchors uniques).
- Badge couleur = signal UX scannabilité (locataire identifie sa ville + statut zone tendue en 1 coup d'œil).

### Step 3 — Validation patch

```python
import re
links = re.findall(r'/preavis-bail-([a-z\-]+)\.html', t)
```
= **50 unique slugs** matched. Anchor `id="par-ville"` présent. Stat 30/20 présente. Section "Outils complémentaires" intacte. File bytes = 44341.

### Step 4 — Smoke E2E HTTPS prod

```
preavis-hub:    200 44841b 57ms
preavis-paris:  200
preavis-amiens: 200
healthz:        200
curl | grep -c /preavis-bail-: 50
```

Pas de restart server.py nécessaire (le serveur sert static directement le file system, hot-reload effectif). 0 régression.

### Step 5 — IndexNow round-26

8 URLs poussées (hub + #par-ville anchor + sitemap + Paris/Lyon/Marseille/Lille/Bordeaux) :
- api.indexnow.org : **200**
- bing.com/indexnow : **200**
- yandex.com/indexnow : **202**

### Step 6 — Wayback SPN

4 URLs queued pace 30s (hub + Paris + Lyon + Marseille) : 3 × 302 OK + 1 retry hub (timeout initial 10s, retry 30s = 302 OK). 4/4 snapshots queued lifetime+4.

## METRICS

- **internal_links_in_preavis_hub=0→50** (★★★ cardinal SEO)
- **anchor_text_ville_unique=0→50** (chaque ville = anchor unique porteur de keyword)
- **badge_zone_tendue_visuel=oui** (UX scannabilité 30 ZT + 20 HZ)
- **preavis_hub_bytes=39798→44341** (+11.4% inventory anchor)
- **indexnow_rounds_total_lifetime=25→26** (api 200, bing 200, yandex 202)
- **wayback_snapshots_lifetime=111→115** (+4)
- **endpoints_smoke_run130=4/4 OK** (latence p50 < 80ms hub)
- **wakes_executifs_nouvelle_mission=33→34**
- **wakes_total_lifetime=129→130**
- **humans_engaged_lifetime=0** (130ᵉ wake honnête)
- **0 dépense**, **0 régression**, **0 dark résidu**
- **schedulewakeup_default=60s** (DIRECTIVE 7 7ᵉ wake compliance)

**Couverture leviers AGENT BUILDER** : (a) SEO programmatique infrastructure (maillage interne) + (b) distribution autonome IndexNow/Wayback. Pas de polish JORF, pas de niche landing — pur SEO compounding.

## NEXT (run-131 ≈ 19:18Z)

ScheduleWakeup 60s. Actions executable autonomes ordonnées par levier asymétrie :

A. **Builder #4 IRL calculateur révision annuelle** (~80 lignes Python + 1 landing page) — directive AGENT BUILDER (a) explicite, locataire-focus 16M FR, IRL trimestriel INSEE publié récent. Levier SEO compounding nouveau wedge.

B. **Wayback SPN 46 villes preavis-bail restantes** pace 30s. Bloc background `run_in_background` parallèle au wake-N+1.

C. **Mesure crawl Googlebot 24h** : grep server.log + visits.jsonl UA Googlebot, calculer crawl rate post-GSC. Si crawl monte → signal positif indexation J+3.

D. **Submit data.gouv.fr** open-data CSV/JSON encadrement-loyer-france-2026 (florian.demartini.dev@gmail.com account, pas Gmail disabled). Référencement gov FR = haute autorité backlink + canal users data analysts/journalistes.

E. **Attendre réponse Florian** sur visite ip_hash + TODO-21 OVH. Max 1 wake puis pivot A/B/C/D sans attendre.

**Choix prioritaire** : A (builder IRL) — c'est la directive AGENT BUILDER (a) pure et locataire-focus = cible plus large (16M vs 5M bailleurs).
