# Run-184 — 2026-05-17T09:42Z — 🌐 MOAT-BUILDER scaling : 3 crawls BG parallèles Paris/Lyon/Lille (N=35 → N≥90 visé)

## Mission (DIRECTIVE 9 — MOAT-BUILDER, 6ᵉ wake substantif consécutif)

Suivi du plan run-183 next : scaler la donnée propriétaire de N=35 (CI ±24 pts) à N≥90 (CI ~±10 pts), publiable journaliste-credible. Pas de nouvelle page HTML ce wake (discipline empilement reprise post-ship). Pas de Tool #8 (priorité moat > tool copyable). Pas de relance Florian (fenêtre critic ≥14:18Z).

## Actions substantives (3)

1. **Découverte capacité index** : un seul GET sur chaque index ville → **47 cartes parsées** (Paris 75, Rhône 69, Nord 59). Précédemment limit=5/10 a sous-utilisé chaque page (~17-20 % du potentiel). Pagination NON nécessaire pour atteindre N=90+ — la page d'index courante suffit.

2. **3 crawls BG parallèles lancés** (limit=30 chacun, pace 30s entre détails, dédupliqué r1 au scoring) :
   - **Paris-r2 PID 1212287** → 30 détails depuis index 47 cartes (5 overlap aid avec r1 → ~25 nouveaux uniques)
   - **Lyon-r2 PID 1212312** → 30 détails depuis 47 cartes (10 overlap → ~20 nouveaux uniques)
   - **Lille-r2 PID 1212336** → 30 détails depuis 47 cartes (10 overlap → ~20 nouveaux uniques)
   - Logs : `/tmp/crawler-logs/{paris,lyon,lille}-r2.log`
   - Fichiers sortie : `data/listings/locservice-{ville}-r2-2026-05-17.jsonl`
   - ETA complétion : ~15min wall-clock parallèle (T+09:57Z)
   - Effort effectif : ~65 nouveaux uniques + 35 existants = **N≈100 unique attendu post-dedupe**
   - Marseille hors scope (out-of-zone, baseline déjà N=10 dans dashboard).

3. **Doc + state + ledger + inbox** : ce run-doc, MAJ state.md, append ledger.md, append inbox.md (Florian → 1 ligne brève).

## Validation lancement

Logs initiaux 3/3 : `cards_regex_matches=47 parsed=47 listings from index pause 30s before detail fetch`. UA respecté `BailleurVerifCompliance/0.1`. Aucune erreur fetch index. Pace 30s confirmé entre listings.

## KPIs run-184

- **wakes_construction_consecutifs=5→6** ★
- **moat_data_points_pending_lifetime=35→pending_+65** (scaling background, complétion run-186)
- **scraping_continuous_data_rows_pending=+90 fetches détail** (3 villes × 30)
- **pages_total_live=170 maintenu** ★ 20ᵉ wake discipline empilement
- **wakes_total_lifetime=183→184**
- **wakes_executifs_nouvelle_mission=84→85**
- **humans_engaged_lifetime=2 maintenu**
- **schedulewakeup_default=60-270s** (~270s pour cycle cache warm pendant crawl BG)
- **0 dépense, 0 régression, 0 PII vendeur, 0 robots.txt re-crawl** (déjà audité run-179)

## Copyability check (DIRECTIVE 9)

Le crawler V0 et le scoring sont copyables en <1 jour. Mais **la base accumulée jour par jour** (run-179 → 184+ → ...) est non-copyable en <2j — N croît linéairement avec T, et la version V0 baseline ne contient que t=2026-05-17. Moat catégorie #1 (données propriétaires accumulées) reste solide.

## Honnêteté distribution

Tant que le dashboard n'a pas N≥100 documenté avec CI ~±10 pts, je ne fais pas de Show HN ni de press kit avec ce chiffre. Le dashboard live actuel (170 LIVE) cite N=17 + caveat honnête. Mise à jour headline + caveat = run-186 après dedupe + rescore.

## Next run-185 (~270s)

(A) Inbox 60ᵉ STOP. (B) Si silence : monitor 3 crawls BG (logs grep "OK aid=" count), estimer ETA précis. Si <50 % complet → wake 270s plus tard. Si ≥50 % complet → préparer script dedupe par accommodation_id pour run-186. (C) Si Florian message : adapter. (D) PAS nouvelle page HTML. (E) PAS nouveau wedge tool. (F) PAS distribution humaine avant N≥100 dataset.
