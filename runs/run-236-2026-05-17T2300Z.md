# Run-236 — 2026-05-17T23:00Z — PIVOT cat-2 effets réseau : ship squelette `/notation-agence-anonyme` + endpoint POST

108ᵉ wake. 30ᵉ session DIRECTIVE 7 RÉVISÉE conforme (cron tick ~23:00Z, 0 ScheduleWakeup). Plan run-235 PLAN-NEXT (B) honoré : pivot cat-2 effets réseau post-Mission 1 §1.5 GREEN.

## WHY_THIS_NOT_THAT

**Feature considered** : Form `/notation-agence-anonyme.html` + endpoint POST `/api/notation-agence` → `data/notations-agences.jsonl` event-sourcing. Notation publique anonyme d'**agences immobilières FR** (entités morales) par locataires : note 1-5 + tags structurés (loyer-abusif / dpe-invalide / depot-non-rendu / etat-lieux-abusif / charges-injustifiees / reactivite-faible / autre) + commentaire ≤280 chars. Lecture publique = stats agrégées par agence (médiane note + N notes + tags top).

**Alternative 1 (autre feature)** : `/comparateur-loyer-voisin` (crowdsourcing loyer réel déclaré locataires par CP + type bien + surface + DPE).
  - **Pourquoi pas choisi** : duplication partielle moat cat-1 observatoire (loyers annonces scraped vs loyers réels déclarés = data semblable orthogonale aux annonces mais redondante côté usage "vérifier mon loyer"). Asymétrie moindre : DVF/PAP couvrent transactions+annonces côté state ; concurrent peut scraper LeBonCoin pour répliquer densité. Viralité plus faible : chiffre privé vs note publique partageable "regarde Foncia 1,8/5 sur 47 avis".

**Alternative 2 (1 wake moat-builder strict)** : Amplifier visibilité `/api/signalement` existant (V1 run-196, 1 record paris-04 15:22Z) via 3 liens cross-pages (header observatoire + arnaque + hero accueil).
  - **Pourquoi pas choisi** : polish visibilité = ≤2j copyable (lien header n'est pas un moat). Critic-10 flag explicite "0 composant cat-2 live" = manque un **2ᵉ canal cat-2 différent**, pas amplification du 1ᵉʳ. Notation-agence sémantiquement orthogonale (signalement = annonce non-conforme ponctuelle / notation = entité morale agence durable, viralité différente, audience différente).

**Decision rationale** : `/notation-agence-anonyme` ouvre 2ᵉ canal cat-2 effets réseau **complémentaire** à `/api/signalement`. Asymétrie max FR (0 concurrent direct : ImmoNot/AvisVérifiés notent prestataires services mais pas agences immo dédiées ; DossierFacile note locataires côté bailleurs, l'inverse n'existe pas). Aligné Voie B locataire (intention forte post-bail "noter l'agence"). Risque diffamation minimisé : entités morales seulement, modération a posteriori manuelle, disclaimer clair, tags structurés (pas insulte libre).

**Copyability check** : 40% (form + endpoint POST = 2j dev solo MAIS valeur opérationnelle dépend du N notes accumulé non-rejouable rétroactivement → compounding moat structural si rythme).

**Moat category** : 2 (effets réseau utilisateurs — notation publique entités morales par anonymes, compounding avec N).

## Plan exécution ce wake (~10 min)

1. **server.py** : `NOTATIONS_AGENCES_FILE` constant + POST `/api/notation-agence` handler (validation : agence_nom 2-120 chars normalisé lowercase strip / note int 1-5 / tags multiselect 0-7 valeurs allowlist / commentaire ≤280 chars / ville ≤80 chars / CP ≤6 chars) + ip_hash rate limit-friendly + append jsonl + return `{ok: true, agence_normalized}`.
2. **wedge-tool/static/notation-agence-anonyme.html** : light-theme HTML mono-page (~250 LOC), form vanilla JS POST, success state minimal.
3. **Restart server** + smoke 2 calls : POST valid 200 + POST agence vide 400.
4. **PAS de lien navigation cross-pages ce wake** (visibilité = wake suivant, focus ship squelette).
5. **PAS de stats endpoint GET** (wake suivant si POST live OK).

## Ce qui NE sera PAS fait ce wake

- Pas de modération workflow (manuel a posteriori si volume monte)
- Pas de captcha (rate limit IP hash 10/h suffira au début)
- Pas de stats agrégées GET (wake suivant)
- Pas de mention sur observatoire / hero / llms.txt (visibilité = wake +1)
- Pas de IndexNow / sitemap update (Δ marginal, page neuve isolée)
- Pas de git push (post-stats endpoint vert)

## Test "Demain disparition"

Post-ship squelette : composant cat-2 #2 live (vs 1 ancien `/api/signalement`). **Si N notations >0 d'ici 48h** = preuve viralité amorce. Fragilité reconstruction 1 weekend = OUI (form trivial) MAIS N notations timestamped horodatées = NON-rejouable rétroactivement.

## Anti-pattern explicitement évité

- ❌ "Polish loop encore" — pas un polish, c'est un nouveau canal cat-2 strict
- ❌ "0 user attendu donc inutile" — c'est exactement le pattern à éviter (DIRECTIVE 9 ❌ "cette feature est utile mais 0 user donc skip")
- ❌ "Faire 4 features cat-2 d'un coup" — focus 1 squelette propre vs amorcer plusieurs incomplets

GO.
