# run-593 — 2026-06-17T15:46Z — Hypothesis-test F.4 self-applicative critic-85 + ship-code chirurgical fix design `verdict_dwell_ms` capture page-hide

## WHY_THIS_NOT_THAT (DIRECTIVE 10 ritual, SB-3 substantive pré-commit)

**Contexte wake** : cron tick 15:43Z. Triple carve-out évalué :
- **(a)** critic-86 cycle 12h ETA ~19:00Z 06-17 NEG futur (T+~3h17)
- **(b)** Florian inbox HEAD UNCHANGED T+~54h+ post-brief 09:15Z 06-15 SB-5 N/A re-flag
- **(c)** fenêtre 13:44Z→15:44Z = 2 events Applebot tail-suffix 2/2 BOT REJECTED pre-classification méthodologie critic-79 #1 self-applicative 21ᵉ+22ᵉ instances ip 7236912018 14:11Z + 85594984 14:50Z ⇒ NEG humain qualifying

⇒ Carve-out (c) PARTIEL self-applicative critic-85 hypothèse F.4 "instrumentation cassée silencieusement" (anti-cargo-cult code-read empirique ≠ FYI sub-threshold).

## 5 options considérées-rejetées

**α) M0 cron-tick standard ≤1L hygiène** : REJET. Critic-84 STOP #3 cap M0 1/6 fenêtre 591-596 = run-592 a déjà consommé le slot M0. Run-593 = 2/6 M0 violation cap. Critic-85 STOP #3 cap analytic 1/6 = run-591 ship+analytic consommé. Ne reste que ship-code mission-aligned OU re-violation STOP.

**β) Ship visibilitychange ONLY sans guard `wasIntersecting`** : REJET. v1 IntersectionObserver initial callback below-fold isIntersecting=false ⇒ verdict_dwell_ms ≈ 0ms noise. v2 doit guard `wasIntersecting` empêche noise. +3L total (state vars + guard) = quasi-zéro coût marginal pour signal propre.

**γ) Ship beforeunload au lieu pagehide+visibilitychange** : REJET. Safari iOS unfire beforeunload empiriquement (HUMAN #6 Bouygues persona target mobile). pagehide+visibilitychange = duplex correct iOS Safari + Chrome Android. Convention MDN BFCache-friendly.

**δ) Ship ALSO email_gate_reached visibilitychange retry** : REJET. Scope-creep audit-66 §6 single action cap respecté. Gate reach v1 design correct (fires sur 1ʳᵉ intersection 0.5 threshold + disconnect une-fois OK). Pas de transition manquante côté gate (gate apparaît dans flow scroll OU jamais). Différé observation T+10j si signal positif gate_reach mais 0 email_focused = scroll-gate-friction hypothèse confirmée v3 ship dynamic.

**ε) Défer ship +N+1 documenter analyse seul concept post-verdict-capture-dropout-analysis.md** : REJET. Critic-85 STOP #3 anti-analytic 1/6 = run-591 ship+analytic consommé + run-593 analytic seul = 2/6 analytic violation. Plus important : fix immediate-actionable (12L net) sans signal risque tang. Document analyse dans WHY ritual ce run = suffit.

## Décision retenue

**Option ζ** = Hypothesis-test code-read app.js L364-384 + ship chirurgical 12L net v2 IntersectionObserver guard + visibilitychange/pagehide capture page-hide. Mission P3 MESURE alignment (instrumentation = mesure ; bug structurel = signal MESURE empêché). Ship-code rompt M0 critic-84 STOP #3 + analytic critic-85 STOP #3 cap conformes.

### Bug v1 détecté empirique

`wedge-tool/static/app.js` L364-384 IntersectionObserver block shipped run-584 :

```javascript
let visibleStart = verdictDisplayedAt;
const dwellObs = new IntersectionObserver((entries) => {
  entries.forEach(e => {
    if (e.isIntersecting) visibleStart = Date.now();
    else trackFunnel("verdict_dwell_ms", { ms: Date.now() - visibleStart, sev: severity });
  });
}, { threshold: 0.3 });
dwellObs.observe(card);
```

**Cas dominant manquant** : User render verdict → IntersectionObserver fire initial callback `isIntersecting=true` (card en viewport post showResult) → `visibleStart = Date.now()`. User lit 30s → ferme page sans scroll. IntersectionObserver disconnect silently sur page-unload (pas de final callback). **0 event verdict_dwell_ms émis.**

**Cas noise** : Card initial below-fold (scroll position anchor) → initial callback `isIntersecting=false` → emit `verdict_dwell_ms ≈ 0ms` (verdictDisplayedAt très récent). Noise.

**Cas OK v1** : User scroll past verdict (visible→non-visible transition) → dwell event fires avec ms correct. Pris en compte.

Contribution structurelle hypothèse N=0/9 candidates lifetime post-instrumentation : cas dominant manquant + cas noise mélangés expliquent partiellement absence signal v1. (Autres causes possibles : N réel humains post-verdict trop faible 5 candidates ; mais avec design bug, on ne peut pas distinguer "instrumentation broken" vs "no humans".)

### PATCH chirurgical v2 (12L net app.js)

```javascript
const verdictDisplayedAt = Date.now();
if ("IntersectionObserver" in window && card) {
  let visibleStart = null;
  let wasIntersecting = false;
  const dwellObs = new IntersectionObserver((entries) => {
    entries.forEach(e => {
      if (e.isIntersecting && !wasIntersecting) { visibleStart = Date.now(); wasIntersecting = true; }
      else if (!e.isIntersecting && wasIntersecting) {
        trackFunnel("verdict_dwell_ms", { ms: Date.now() - visibleStart, sev: severity });
        wasIntersecting = false;
      }
    });
  }, { threshold: 0.3 });
  dwellObs.observe(card);
  const emitFinalDwell = () => {
    if (wasIntersecting && visibleStart) {
      trackFunnel("verdict_dwell_ms", { ms: Date.now() - visibleStart, sev: severity, src: "hide" });
      wasIntersecting = false;
    }
  };
  document.addEventListener("visibilitychange", () => { if (document.hidden) emitFinalDwell(); });
  window.addEventListener("pagehide", emitFinalDwell);
  // email-gate IO reach inchangé v2 (design v1 correct, 1ʳᵉ intersection + disconnect)
  ...
}
```

Modif clés : (1) `wasIntersecting` state explicit anti-noise initial below-fold ; (2) guard `!wasIntersecting` empêche set `visibleStart` redondant ; (3) guard `wasIntersecting` empêche émission redondante ; (4) `emitFinalDwell` closure capture state via `visibilitychange` + `pagehide` (duplex Safari iOS + Chrome Android) ; (5) `meta.src="hide"` distingue page-close vs scroll-away events post-aggregation.

Diff v1→v2 : 9L brut → 21L brut (+12L net). Rétrocompatible : v1 event shape `{ms,sev}` toujours accepté v2 ajoute optional `src` field (server meta_clean passthrough str≤120 OK).

### Smoke 4/4 PASS

1. `node --check wedge-tool/static/app.js` → JS syntax OK ✓
2. `curl https://bailleurverif.fr/static/app.js | grep -c "emitFinalDwell\|wasIntersecting\|visibilitychange\|pagehide"` → 10 matches (prod serve new code immédiat python http.server file-reload per request, no restart needed) ✓
3. `POST /api/funnel/event` `{"sessionId":"smoke-run593","event_type":"verdict_dwell_ms","meta":{"ms":12345,"sev":"ok","src":"hide"}}` → `{"ok":true}` (server passthrough src field OK meta_clean accepts str≤120) ✓
4. `FUNNEL_EVENT_TYPES` whitelist `verdict_dwell_ms` intact run-584 ✓

0 server.py restart needed (python http.server serves /static/ via file-reload-per-request). Browser cache max-age=300 ⇒ vrais users voient v2 dans 5min max.

### Test décisif fix v2 (binding T+10j 2026-06-26)

Wake humain qualifying suivant verdict_displayed → si verdict_dwell_ms event `meta.src="hide"` émis sur page-close ⇒ fix valide hypothèse F.4. Cross-ref funnel-events.jsonl post-deployment :
- N≥1 `verdict_dwell_ms` avec `src="hide"` ⇒ instrumentation v2 fonctionne, hypothèses H1-H4 testables binding sample N≥3
- N=0 `verdict_dwell_ms` cumul T+72h post-humain qualifying ⇒ root cause autre (JS error console / blocker mobile / CSP / etc.) ⇒ wake +N analyse étendue

Observation extension T+10j 06-26T21:46Z run-589 reste binding pour aggregation 4 hypothèses H1-H4 + maintenant 5ᵉ hypothèse F.4-resolved.

## Bans reconduits

audit-57+58+61+62+63+64+critic-78+79+80+81+82+83+84+85 STRICT 28/28 intacts post-ship v2.

## Counters changés

- `visits_total` = 489→491 (+2 Applebot tail-suffix REJECTED fenêtre 2h ip 7236912018 + 85594984)
- `bot_pre_classification_rejections_cumul` = 22→24 (agrégé compteur existant tableau récap UNCHANGED, anti-cargo-cult dimension STOP #1 audit-80 sustained 23+/23+ wakes)
- `instrumentation_design_fixes_lifetime` = 0→1 ★ NEW counter run-593 (1ʳᵉ fix design flaw post-ship v1 critic-84 #1, distinct vs ship neuf : v1 capture page-hide miss = bug structurel hypothesis F.4 self-applicative confirmé)
- `builder_ship_substantive_wakes_591_596` = 1/6→2/6 (591 ship+analytic + 593 ship-code, 592 M0 reste 1/6 cap)

UNCHANGED : strategic_cumul=65/65 ★ 36ᵉ J+0 STREAK / tactical_cumul=97 / pages_total=233 / florian_briefs_honored_j0_lifetime=12 / meta_q_acks_received_cumul=1 RESOLVED / verdict_displayed=13 events 11 distinct / humans_engaged_lifetime=9-13 raw 7-9 conf-adj / auto_patch_par_cible_strategic_sem_06-12_06-19=1/1 consommé / auto_patch_par_cible_builder_sem_06-12_06-19=0/1 conserve / wakes_critic_80_action_1_consolidation=2 counters / wikipedia_fr_drafts_armed_cumul=2 DORMANT / bot_post_classification_rollbacks_cumul=1 / share_card_subline_render_fix_lifetime=1 / post_verdict_capture_deadline_extended_lifetime=1 / robots_txt_llms_discovery_lifetime=1.

## Discipline self-binding

- **SB-1** N/A : patch ≤12L net pas NEW FILE ≥100L
- **SB-2** N/A : 0 ECLI
- **SB-3** APPLIQUÉ : run file + snapshot update + ledger append pré-commit
- **SB-4** N/A : patch app.js pas NEW page OR PATCH ≥+50L HTML (Indexing API / IndexNow cap N/A)
- **SB-5** ACTIVE : 0 re-flag HEAD inbox 1L ledger seulement T+~54h+ post-brief 09:15Z 06-15

## DIR7 + PAT

DIR7 cron `0 */2 * * *` baseline 2026-05-21 streak continued (13:44Z → 15:46Z honored, PAS ScheduleWakeup). PAT BLOCKER 30→31 commits ahead origin/main local silent (SB-5 active T+~54h+).

## Observation windows actives

- **NEW** Test décisif fix v2 = wake humain qualifying suivant verdict_displayed → cross-ref `verdict_dwell_ms` meta.src=`hide` émis (instrumentation v2 LIVE 15:46Z)
- Post-verdict instrumentation T+10j NEW deadline 06-26T21:46Z (N=0 sur ~18h cumul, v2 reset compteur empirique)
- Share-card iOS Safari T+72h 06-19T05:44Z (run-588 ship)
- FAQPage cycle 1 ship-observe 06-18T13:43Z+ décision ship Montreuil+Saint-Denis queued run-587
- Friction-fix q3+q4 helper T+72h obs run-575 deadline 06-18T03:43Z (T+~12h)
- Tracking T+30j 2026-07-17 re-grep 10 LLM UAs cible ≥1 explicit crawler hit valide robots.txt llms.txt discovery (run-591)
- Strategic option F hypothèse T+14j 2026-06-29 (delta empirique)

## NEXT (description textuelle, PAS ScheduleWakeup DIRECTIVE 7 STRICT)

(a) cron tick 15:46Z→17:46Z standard ; (b) critic-86 ETA cycle 12h ~19:00Z 06-17 T+~3h17 ; (c) audit-67 Strategic ETA 06-19T11:46Z cycle 48h ; (d) observation T+10j post-verdict instrumentation v2 06-26T21:46Z (N=0 sur ~18h cumul, v2 LIVE 15:46Z reset baseline) ; (e) observation share-card iOS Safari T+72h 06-19T05:44Z ; (f) Florian-silent SB-5 PAT+GSC ; (g) post-observation T+72h cycle 1 FAQPage 06-18T13:43Z+ décision ship Montreuil+Saint-Denis queued run-587 ; (h) tracking T+30j 2026-07-17 re-grep 10 LLM UAs ; (i) cron tick suivant M0 micro-action mission-aligned ou ship-code si signal (fenêtre 591→596 = 2/6 consommée 591+593 ship substantive, 592 M0 hygiène, 4 wakes restants 594→596). 0 ScheduleWakeup. 0 émission. 0 publication Wikipedia. 0 3ᵉ Wikipedia draft. 0 ré-émission méta-Q. 0 FYI sub-threshold inbox HEAD (audit-63 ban STRICT reconduit). 0 narration `tactical_cumul=N→N+1 ★ record vélocité` (critic-85 STOP #2). 0 `audit-67 ETA T+~Xh restant` decrementing (critic-85 STOP #1).
