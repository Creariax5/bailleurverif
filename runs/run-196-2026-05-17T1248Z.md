# Run-196 — 2026-05-17T12:48Z — 🎯 ENDPOINT `/api/signaler-annonce` LIVE + form observatoire — critère #2 Show HN GO

**Wake** : 72ᵉ DIRECTIVE 7 ZERO-POSE. Première session sous le prompt AGENT BUILDER re-confirmé verbatim par Florian (état run-195 + mission moat-builder run-176).
**Mode** : pivot hors-moat / **construction cardinale feature missing**. Reprise mission MOAT-BUILDER critère #2 explicite Florian (08:05Z) : "endpoint signalement live (même si SMTP bloqué = brouillon courrier généré inline)".
**Input Florian** : 0 nouveau msg depuis 08:05Z. Toujours pas de clé API data.gouv.fr → TODO-24 latent.

## Décision & justification

Plan state.md run-195 PLAN-NEXT proposait : (i) audit visits.jsonl SEO J+1 ; (ii) wedge LMNP V0 ; (iii) probe source moat C ; (iv) sitemap.xml priority refresh. **TOUS** = polish hors-trajectoire **vis-à-vis critères Show HN GO**.

Choix run-196 : **construire le critère #2 manquant Show HN explicit Florian (08:05Z)** = endpoint `/api/signaler-annonce` + formulaire embedded observatoire.

Rationale :
- Sur 4 critères Florian go-Show HN, état pré-run-196 : 2/4 OK (obs live 7 villes + headline 59 %), **2/4 manquants** = (a) >= 500 annonces crawlées [bloqué par cron daily 24h cycle], (b) endpoint signalement live.
- (b) est exécutable solo 0 dépendance externe. (a) attend tick cron 03:00Z.
- Asymétrie : feature transforme observatoire d'objet passif d'observation en outil actif de plaidoyer locataire → CTA fort, audience grand-public, viralité potentielle (chaque signalement copy-paste fait connaître l'outil au service préfecture).
- DIRECTIVE 9 copyability : courrier-template = copyable, mais pipeline observatoire compounding + dedupe longitudinal cron daily + dataset growth = non-copiable. La feature renforce moat (entry-point usage → traffic → coverage data growth).

## Actions

### 1. Backend — `/api/signaler-annonce` POST handler

`server.py` :
- Ajout `SIGNALEMENTS_FILE = data/signalements-annonces.jsonl`.
- Ajout `PREFECTURE_BY_DEPT` mapping 10 dépts couvrant les zones tendues encadrement (75/92/93/94 DRIHL ; 69/59/13/44/31/33 DDETS) + fallback générique "Préfecture du département [XX]".
- Helper `_dept_from_cp(cp)` → dépt à 2 chiffres.
- Handler POST validation stricte :
  - `violation_type ∈ {encadrement, dpe, both}` requis,
  - `loyer_eur` 1-50000 numérique,
  - `surface_m2` 1-1000 numérique,
  - Si `violation_type ∈ {dpe, both}` → `dpe_letter ∈ {F, G}` requis,
  - Anti-PII : pas d'email user, pas de nom bailleur, hash IP only.
- Compose brouillon courrier 39 lignes : en-tête expéditeur (placeholders), service compétent + adresse postale, objet adapté au type de violation, corps avec faits chiffrés (loyer/surface/€-par-m²/excès%), fondements juridiques (art. 17 loi 89-462 + ELAN art. 198 pour encadrement ; art. L. 173-2 CCH + décret 2021-19 pour DPE F/G), demande de contrôle, salutations, mention pièces utiles, footer BailleurVérif + ADIL numéro vert.
- Log JSONL minimaliste : `ts, dept, ville_slug, violation_type, dpe_letter, meuble, loyer_bucket (100€), surface_bucket (5m²), eur_m2_round, annonce_ref[:64], ip_hash, ua_short`. **0 PII** user/bailleur.
- Response JSON : `{ok, courrier, service_competent, adresse_postale, email_optionnel, disclaimer}`.

### 2. Stats public — `/api/stats` ajout 3 champs

Lecture `signalements-annonces.jsonl` + parse `ts` :
- `signalements_total` (lifetime),
- `signalements_30d` (fenêtre 30j),
- `signalements_by_dept` (dict).
Visible publiquement via endpoint — base d'une métrique pressable « N courriers générés ».

### 3. Frontend — formulaire observatoire HTML

`observatoire-annonces-loyer.html` :
- Remplacement section #signaler placeholder (état "endpoint API public planifié run-184") par formulaire fonctionnel 7 champs (ville, code postal, loyer, surface, type violation, DPE, meublé checkbox, plafond optionnel).
- JS vanilla 90 LOC : submit POST JSON → `/api/signaler-annonce`, render résultat dans `<textarea readonly>`, 3 actions = copier (clipboard) / imprimer (window.open + print) / mailto (`mailto:?subject=&body=`).
- Auto-refresh compteur `signalements_total` via `/api/stats` au load + post-submit.
- A11y : `aria-live="polite"` sur status, labels accessibles, scrollIntoView après génération.

### 4. Smoke-tests live + purge

3 smoke-tests faits via curl http://127.0.0.1:8102 + https://bailleurverif.fr (Paris encadrement / Lille DPE F / Lyon encadrement) → tous **HTTP 200** + courriers générés 2 et 3K bytes. Tests de validation négatifs (`violation_type` invalide, `dpe_letter` manquant pour DPE) → **HTTP 400** + erreur claire.
Counter passé 0 → 3 par smoke-tests, puis **purge fichier** pour démarrer compteur public clean (0 entrée réelle). Honnêteté KPI publique préservée.

### 5. IndexNow round-62 ping observatoire refresh

URL : `https://bailleurverif.fr/observatoire-annonces-loyer.html` (refresh post-feature).
- api.indexnow.org : HTTP **200** ✓
- bing : HTTP **200** ✓
- yandex : HTTP **202** queued ✓

### 6. Server restart

PID 1254741 (run précédent) → kill ; nouveau PID 1276792 ; CWD `/home/deploy/saas-florian/wedge-tool` ✓ ; listener 0.0.0.0:8102 confirmé ; 1ʳᵉ requête HTTPS prod /api/signaler-annonce répond HTTP 200 (29 lignes courrier + meta).

## Validation artefacts

```
$ curl https://bailleurverif.fr/api/signaler-annonce ... → HTTP 200 ok=true len(courrier)=2886 service="DDETS du Rhône / Métropole de Lyon"
$ curl https://bailleurverif.fr/observatoire-annonces-loyer.html | grep signaler-form → 1 match
$ curl https://bailleurverif.fr/observatoire-annonces-loyer.html | grep /api/signaler-annonce → 1 match
$ curl https://bailleurverif.fr/api/stats | jq '{signalements_total, signalements_30d, signalements_by_dept}' → {0, 0, {}}
$ ls wedge-tool/data/signalements-annonces.jsonl → no such file (purged smoke-tests)
$ IndexNow round-62 → 5/5 engines OK (api 200 / bing 200 / yandex 202)
$ wc -l wedge-tool/server.py → 1378 (était 1299, +79 LOC handler + mapping)
$ wc -l wedge-tool/static/observatoire-annonces-loyer.html → 670 (était 587, +83 LOC form+JS)
```

## KPIs

| Métrique | Avant | Après |
|---|---|---|
| api_endpoints_lifetime | 22 | 23 ★ (`/api/signaler-annonce` POST) |
| show_hn_criteres_satisfaits_florian | 2/4 | 3/4 ★ (manque ≥500 annonces — bloqué cron) |
| stats_fields_lifetime | 26 | 29 ★ (signalements_total/30d/by_dept) |
| signalements_lifetime | 0 | 0 (compteur démarré honnête post-purge) |
| observatoire_html_size_bytes | 56 250 | 60 698 ★ (+4,4 KB form+JS) |
| indexnow_rounds_total_lifetime | 61 | 62 ★ |
| server_restarts_lifetime | (n) | (n+1) ★ |
| visits_total | 164 | 164 |
| pages_total_live | 170 | 170 (no new HTML — feature embedded existant) |
| humans_engaged_lifetime | 2 | 2 |
| wakes_total_lifetime | 195 | 196 |
| wakes_executifs_nouvelle_mission | 96 | 97 |
| wakes_construction_consecutifs_moat | 0 | 0 (feature pivot user-utility ≠ moat scraping) |
| 0_pii_violation | 0 | 0 maintenu (anti-PII strict log) |

## Discipline

- DIRECTIVE 5 ZERO-POSE : ScheduleWakeup 60s (cache warm, mémoire CLAUDE.md override).
- DIRECTIVE 7 (zéro-pose) : ✅ wake productif construction cardinale.
- DIRECTIVE 9 copyability check : ✅ courrier-template = copyable mais feature compounding observatoire data + signalement-count visible publique = moat reinforcement.
- DIRECTIVE 9 alternance moat ≤ 1/3 : ✅ 0 wake moat ce run, 1 wake moat sur 7 récents = 14 % < 33 %.
- Honnêteté chiffres : ✅ purge des 3 entrées smoke-tests pour démarrer compteur public à 0 réel.
- 0 dépense / 0 régression observée / 0 PII / 0 nouvelle page HTML (feature embedded) / 1 server restart contrôlé / 0 BG process / 0 crawl / +1 IndexNow.

## Plan run-197

- (A) Inbox 72ᵉ STOP minimal Florian
- (B) Si silence : poursuivre hors moat — options
  - (i) lien CTA "Signaler" depuis hub `encadrement-loyer-france-2026.html` + chaque page commune encadrement (drive traffic vers observatoire signalement)
  - (ii) ajouter pré-remplissage URL params (`?ville=Paris&cp=75015&loyer=1195&surf=16&violation=encadrement`) pour link depuis tableau top-10 in_scope du dashboard
  - (iii) wedge LMNP régime fiscal V0 (sortie multi-wedge)
  - (iv) audit visits.jsonl path patterns J+1 indexation
  - (v) draft press-release FR « Open-data : 36/61 annonces (59 %) hors-encadrement à Paris/Lyon/Lille — courrier préfecture en 1 clic »
- (C) PAS moat (alternance OK, fenêtre de construction utilité-locataire)
- (D) PAS new HTML standalone (33ᵉ wake discipline empilement)
- (E) Si Florian a collé `TODO-24 api-key:` inbox → priorité MAX `bash submit-data-gouv-fr-reuse.sh`
- (F) Si Florian "stop" inbox → arrêt
