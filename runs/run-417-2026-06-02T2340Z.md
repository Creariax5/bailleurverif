# run-417 — 2026-06-02T23:40Z

## Context
Wake T+~1h53 post run-416 (cron `0 */2 * * *` cadence baseline). Strategic audit-40 droppé 2026-06-02T22:00Z prescrit SHARPEN CTA verdict→email home (lift ban touch home audit-39 strict carve-out CTA-zone). Critère T+72h `email_submitted_lifetime ≥ 3` OR `humans_engaged_lifetime ≥ 6` deadline 2026-06-05T22:00Z. Bans audit-40 14 items (NEW FILE city-page, monétisation, Reddit/HN/X/TikTok, Telegram, ScheduleWakeup, méta-Q ≤06-04, spawn 7ᵉ, SMTP, IndexNow, Indexing API >1 URL, patch sub-agents, hero swap, touch scan-url, touch share-card, NEW funnel event types). Lift explicit touch home CTA-zone post-verdict.

## WHY_THIS_NOT_THAT (ritual DIRECTIVE 10)

**THIS** : honor strategic-40 prescription = patch chirurgical ≤30L app.js verdict-topic auto-dérivé + reframe email-gate DPE F/G + parallèle POST /api/subscribe post-success.

**NOT THAT 1** : NEW FILE city-page Bordeaux/Nantes Phase 2 ⇒ ban audit-40 strict + strategic >> Phase 2 (hierarchy §c-bis). Reporter audit-41 si email_submitted critère échec.

**NOT THAT 2** : touch share-card ou scan-url ⇒ ban audit-40 explicit. Patch home strict CTA-zone post-verdict uniquement.

**NOT THAT 3** : ajouter NEW funnel event type "verdict_cta_subscribed" ⇒ ban audit-40 "NEW funnel event types hors email_submitted existant". Réutilise email_submitted trackFunnel run-416 ready.

**NOT THAT 4** : remplacer /api/capture par /api/subscribe (breaking change captureEmail) ⇒ trop large, risque régression flow rapport manuel Florian. Préfère ADD parallèle POST /api/subscribe (idempotent) sans toucher /api/capture flow existant.

## Actions

### 1. Patch verdict-topic auto-dérivé (app.js L312-336 BLOCK) — +17L

Logic : après render verdict-card, dériver `verdictTopic` selon mapping :
- `depassement > 0` ⇒ `loyer-legal`
- DPE F/G ⇒ `dpe-bailleur`
- warn/danger générique ⇒ `veille-reglementaire`
- ok ⇒ "" (pas de subscribe)

Setter `form.dataset.subscribeTopic` sur email-form pour transmission à captureEmail. Refactoring inline du bloc reframe email-gate existant (depassement>0 déjà géré, AJOUT branch DPE F/G).

### 2. Parallèle POST /api/subscribe dans captureEmail (app.js L382-388) — +7L

Post-success `/api/capture` (data.ok==true), si `form.dataset.subscribeTopic` présent ⇒ fire-and-forget `fetch("/api/subscribe", {topic, email, source:"home-verdict-cta", consent:true})`. Idempotent côté serveur (server.py L1564 deduplique email+topic confirmed). Pas de await ⇒ pas de régression UX existante.

### 3. Smoke E2E test endpoint

```
curl -X POST http://localhost:8102/api/subscribe -d '{"email":"smoke-strategic40-run417@example.com","topic":"loyer-legal","source":"smoke-test-run417","consent":true}'
→ HTTP 200 ✅
tail -1 data/subscribers.jsonl
→ email:smoke-strategic40-run417 topic:loyer-legal source:smoke-test-run417 ✅
```

JS syntax `node -c static/app.js` ✅.

## Métriques

- `code_lines_shipped_prod_lifetime` : +24 net app.js (+24/-7 cumul +31)
- `strategic_critic_recommendations_followed_cumul` : 39→40 ★ honored J+0 T+~1h40
- `florian_briefs_honored_j0_lifetime` : UNCHANGED (Strategic prescription pas brief Florian)
- `produit_excellence_signal` : verdict→email CTA différenciée par tag = sharpen P1 directe
- `audit_39_bans_consecutive_strict` : RESET (audit-40 lift ban touch home strict carve-out CTA-zone respecté)
- `email_submitted_lifetime` : 0 UNCHANGED baseline, mesure T+72h via funnel events.jsonl

## NEXT

run-418 : si critic-57 ≤T+3h ⇒ honor #1 ; sinon WAIT critère audit-40 T+72h ETA 2026-06-05T22:00Z ⇒ entre-temps option (a) Phase 2 ville suivante observatoire (Bordeaux N=42 ou Nantes) enrich Phase 2 (PAS NEW FILE, edit existant) ; option (b) M0+ §a baseline GSC refresh ; option (c) tactical critic-57 ETA si drop ≤22:00Z. Préférer (a) Bordeaux enrich si page existe car P2 SEO HAUTE aligné mission RE-RECALIBRÉE.
