# run-297 — 2026-05-19T12:28Z — SOUS-AGENT #1 sub-judilibre-enrich (Haiku 4.5) CRÉÉ ★★★

## Context entrée

- Brief Florian top inbox 2026-05-19T11:55Z : **CAPABILITY UPGRADE** champ `model` sur `/api/agents` testé bout-en-bout 12:00Z (POST + connector + CLI cmdline `/proc/PID/cmdline`). Coût Haiku ≈1/7-1/10 d'Opus.
- Ordre explicite Florian : « 1 sous-agent ce wake = `sub-judilibre-enrich`. Helpers déjà shipped run-287. Pas de spawn massif. »
- État cat-3 templates `jurisprudence_refs[]` entrée run-297 : **dpe-invalide=0, depot-garantie=0, loyer-abusif=1** (vérifié `data/interpretation-library-v0/recourse-templates/*.json`). N=3 ref total alors que decision-2026-05-19-cat3-saturated.md tag templates "complete" sur legal_basis seul — `jurisprudence_refs` reste un levier moat cat-3 substantif sous-exploité.

## Ritual DIRECTIVE 10 — WHY_THIS_NOT_THAT

**(a) Copyability ~95%** : créer un sous-agent Haiku est une commodité technique reproductible par tout dev avec API key agents-control. **MAIS** : le moat ici n'est pas la création du sous-agent, c'est l'**enrichissement compound** des templates cat-3 sur 30+ cycles/mois en cadence soutenue + bien plus tard la profondeur de `jurisprudence_refs` cumulée (irrattrapable rétroactivement par un concurrent qui ouvrirait son SaaS J+1).

**(b) Moat category** : cat-3 (RAG-LLM substantif) — chaque ref enrichie = composant DIRECTIVE 9 cat-1 (données factuelles vérifiables) ajouté.

**(c) Pourquoi ce sous-agent et pas spawner les 5 ?** Florian explicite : « 1 sous-agent observé sur 2-3 cycles avant les 4 autres. Discipline pareille que ship moat. » Anti-spawn-bomb. Si cycle 1 = drift / coût aberrant, rollback `enabled=0` propre avant prolifération.

**(d) Alternative écartée — `sub-imap-summarizer` first** : tentant car canal externe latent. Mais 0 IMAP entrant 5j (ANIL T+9h, press 4/4 silent T+17h), donc valeur immédiate ≈0. Judilibre = stream de données actuel (37868 décisions "loyer abusif" déjà confirmées run-287), value à chaque cycle.

**(e) Alternative écartée — D1 ne-rien-faire** : critic-19 STOP wait cron est valide pour Builder Opus en absence de signal, **mais** ici Florian a livré un signal explicite TOP inbox H-32min — l'ignorer = anti-instruction explicite (worse que polish).

## Actions ce wake

1. **Backup payload** `agent-browser/prompts-backup/sub-judilibre-enrich-create-2026-05-19T1227Z.json` (rationale + payload complet) — 2625 chars prompt strict, 10 étapes loop + 7 hard bans + exit clauses + log format.
2. **POST** `${AGENTS_CONTROL_BASE}/api/agents` payload `model=claude-haiku-4-5-20251001` + `schedule_interval=3600` → HTTP 200 réponse id=`2bbb1dc8-1336-4b64-890b-063c486de4aa`, status=stopped, total_runs=0, enabled=1 (démarrera au prochain tick interne agents-control).
3. **Init registry** `agent-browser/sub-agents-registry.json` (append-only convention, prompt_hash sha256 first16=`a2a0a49cd0205b64`, 1 entrée). Max 6 actifs, budget 20€/mois total. Annonce path log dédié `data/sub-agents/sub-judilibre-enrich.jsonl` (créé vide, le sous-agent appendera).
4. **Update florian-todos.md** : nouvelle section `## SOUS-AGENTS ACTIFS` en TOP (sub-judilibre-enrich line, id, interval, status, état entrée). Permet à Florian de voir d'un coup d'œil.
5. Mkdir `data/sub-agents/` (touch implicite par log futur du sous-agent).

## KPIs delta

- `sub_agents_active_count`=0→1 ★ NEW
- `sub_agents_lifetime_created`=0→1 ★ NEW
- `sub_agent_judilibre_enrich_id`=`2bbb1dc8-1336-4b64-890b-063c486de4aa`
- `sub_agent_judilibre_enrich_status`=enabled (stopped pre-1er-tick)
- `sub_agent_judilibre_enrich_interval_s`=3600
- `jurisprudence_refs_dpe_invalide`=0 (sera enriched cycle 1)
- `jurisprudence_refs_depot_garantie`=0
- `jurisprudence_refs_loyer_abusif`=1
- `florian_todos_sous_agents_section_added`=true
- `prompts_backup_files_count`=existing+1 (premier réel populate post critic-19 ★ #3)
- `sub_agents_registry_initialized`=true
- `wakes_total_lifetime`=296→297
- `directive_7_revisee_compliance_consecutive_wakes`=76→77
- `wakes_since_last_strategic_critic`=8→9 (cible 16, marge 7)
- 0 ScheduleWakeup. 0 dépense (Haiku futur ~$0.05-0.10/cycle facturé sur clé Anthropic existante .env). 0 PII. 0 outbound SMTP. 0 nouvelle page HTML. 0 nouveau signup nominatif (sub-agent = ressource interne, pas signup externe).

## Risques / surveillance run-298+

- **R1 sous-agent dérive** (boucle infinie, modif files hors-scope, push direct) : mitigation = prompt 7 hard bans + Builder vérifie `data/sub-agents/sub-judilibre-enrich.jsonl` chaque wake. Si outcome="error" 2× → PATCH `enabled=0`.
- **R2 coût explose** : 30 wakes/jour × $0.10 = $3/jour worst case → budget mensuel ~$90 single sub-agent (dépasse $20/mois Florian-set). Mitigation : interval 1h = 24/jour, exit clause "saturated_3" kick in dès N≥3 sur 3 templates = arrêt naturel après ~9-15 cycles utiles. Builder run-298+ vérifie cumul.
- **R3 commit explosion** : sous-agent commit 1×/cycle = ~24 commits/jour. Mitigation : Builder push batch (le sous-agent NE PUSH PAS, donc commits restent locaux jusqu'à Builder push). Acceptable.

## NEXT plan run-298 (~T+~15-60 min cron externe)

- **(A) Inbox Florian MAX prio** (ack sub-agent + autres signals).
- **(B) Lire `data/sub-agents/sub-judilibre-enrich.jsonl` HEAD 5 lignes** — si 1er cycle déjà tourné (interval 3600s, premier tick agents-control inconnu mais souvent immédiat sur enable=1) → vérifier outcome=ok et N refs ajoutées. Si error → PATCH enabled=0 + log incident.
- **(C) Critic-20 ~14:00Z lecture priorité ABSOLUE** — si validation ★★ → autoriser spawn `sub-imap-summarizer` ou `sub-crawler-postprocess` run-299+. Si critic-20 flag spam-burst (2ᵉ PR) → différer spawn 2ᵉ sous-agent ≥critic-21.
- **(D) IMAP poll auto reply** ANIL/press/PR-#410-comment/PR-#28-comment.
- **(E) Anti-spawn-bomb** : NE PAS créer 2ᵉ sous-agent run-298 même si cycle 1 = ok. Discipline = ≥2 cycles observés (Florian explicite).
- **(F) PR #28 + #410 0 nag policy ≥2026-06-02** (cohérent run-296).
- **(G) `wakes_since_last_strategic_critic`=9→10 cible 16, marge 6**.
- **(H) Options fallback si 0 Florian + cycle pas tourné** : H1 ne rien faire (anti-action-pour-action), H2 cleanup vieux files `agent-browser/awesome-real-estate-pr-body-draft.md` ou rename `-opened.md` (TODO run-296 NEXT fallback E2 reportée).
- **(I) « stop » Florian** → arrêt.

**0 ScheduleWakeup.** Cron externe relance.
