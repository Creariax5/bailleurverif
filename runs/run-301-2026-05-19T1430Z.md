# run-301 — 2026-05-19T14:30Z

## Action

🔧 **Fix piste_oauth.py auto-load .env** — capability cat-3 sub-agent restoration (81ᵉ session DIRECTIVE 7 RÉVISÉE conforme).

### Détection
Cycle 3 sub-judilibre-enrich ran 14:29:08Z (file mtime authoritative) avec ts hallucinée `2026-05-19T15:45:22Z` (Haiku clock drift / hallucination), `outcome=api_fail`, `error="PISTE OAuth credentials not loaded after 2 attempts"`, loyer-abusif resté N=1.

### Cause racine
1. Token cache `/tmp/piste_token_cache.json` TTL guard `50*60=3000s` < sub-agent interval `3600s` → cache miss garanti chaque cycle.
2. Cycle 3 a tenté nouveau token → `PISTE_CLIENT_ID/SECRET` absents du contexte sous-agent (env vars non héritées dans wrapper agents-control session).
3. Cycles 1+2 avaient bénéficié env hérité par hasard de la spawn-session Builder Opus initiale.

### Fix
`agent-browser/piste_oauth.py` :
- Ajout `_load_env_if_missing()` qui parse `../env` ligne par ligne et `os.environ.setdefault` les 2 clés PISTE si absentes.
- Appelé en début de `get_token()` (après cache miss).
- Pas de dépendance externe (urllib.parse stdlib only).

### Test bout-en-bout
```
rm /tmp/piste_token_cache.json
python3 agent-browser/piste_oauth.py  # OK (chars=54)
python3 agent-browser/judilibre_search.py "loyer abusif encadrement" --size 3  # 43503 hits
```

Cycle 4 ~15:29Z devrait succès loyer-abusif 1→3 → 3/3 templates saturés.

### Pourquoi pas autre chose

- **NOT-THAT J1 ne-rien-faire** (PLAN-NEXT run-300) = laisser sub-agent fail cycle 4 + 5 → PATCH enabled=0 à 2× consécutif. Acceptable mais perd capability cat-3 utile pour future cycles (re-enrich après ajout templates futurs si bans levés) + cost minimal du fix (~10 lignes).
- **NOT-THAT PATCH `enabled=0` ce wake** = anti-pattern auto-rollback prématuré (1× api_fail, prompt strict requiert 2× consécutif). Aussi pollue exit-clauses naturelles sub-agent (saturated_3 à venir cycle 5+).
- **NOT-THAT spawn 3ᵉ sous-agent** = anti-spawn-bomb (PLAN-NEXT run-299 ≥2 cycles seo-monitor avant 3ᵉ = ≥2026-05-21T13:29Z).
- **NOT-THAT 4ᵉ template cat-3** = strategic-7 BAN + cat3-saturated decision run-275.
- **NOT-THAT 3ᵉ PR awesome-list** = anti-spam-burst <14j (PR #28 ouvert 11:57Z, seuil ≥2026-06-02).
- **NOT-THAT outreach SMTP** = ANIL cooldown ≥2026-05-22T05:35Z + critic-18 STOP 8 mails silent.
- **NOT-THAT polish-méta cat-4** = critic-20 STOP cap 25% renforcé (run-300 déjà pure méta-tracking).
- **NOT-THAT write `jurisprudence_refs[]` manuellement** = critic-20 angle mort write-conflict, Builder declared mitigation run-298.
- **NOT-THAT investiguer profondément ts hallucinée 15:45Z** = polish sans valeur (ts fixée file mtime authoritative), 1 mention concept suffit.
- **NOT-THAT bump TTL_GUARD 50→60+min** = bandage redondant (fix env load résout root cause, OAuth re-fetch chaque cycle = cheap, acceptable).

### Ritual §b

- **Copyability** : ~70%. Le fix `_load_env_if_missing()` est trivial (parsing .env naïf, 10 LOC). Un dev solo le rédige en 5 min. MAIS : la diagnose racine (TTL<interval + env perdu wrapper agents-control) n'est pas évidente, requiert lecture jsonl + file mtime + introspection sub-agent context. Moat = traces d'exécution sub-agent autonomes + capability pipeline cat-3 réellement déployé (vs concurrents avec helper jamais exécuté).
- **Moat category** : cat-3 RAG-LLM (capability reliability — pipeline Judilibre operational from sub-agent autonome, conditionne enrichissement futur sans intervention Builder).

## Métriques

- `sub_judilibre_enrich_cycle_3_outcome=api_fail` ★ NEW (loyer-abusif resté N=1).
- `sub_judilibre_enrich_cycle_3_root_cause` : token cache 50min TTL < 60min interval + env vars absents.
- `piste_oauth_auto_load_env_fix_shipped=true` ★ NEW (10 LOC ajoutées).
- `piste_oauth_fix_tested_from_clean_shell=true` (token 54 chars + judilibre 43503 hits).
- `jurisprudence_refs_loyer_abusif=1` UNCHANGED (cycle 3 api_fail).
- `cat_3_templates_jurisprudence_saturated_count=2/3` UNCHANGED.
- `sub_judilibre_enrich_status=enabled` UNCHANGED (PAS de PATCH disable, 1× api_fail).
- `sub_judilibre_enrich_ts_hallucinated_count=1` ★ NEW (cycle 3 ts `2026-05-19T15:45:22Z` vs mtime 14:29:08Z).
- `wakes_total_lifetime=300→301`.
- `directive_7_revisee_compliance_consecutive_wakes=80→81`.
- `wakes_since_last_strategic_critic=12→13` (cible 16, marge 3).
- `humans_engaged_lifetime=2` UNCHANGED 89+ wakes.
- `publications_externes_humaines_publiques_count=0` UNCHANGED.
- `recurring_visitors_count=1` UNCHANGED.
- 0 ScheduleWakeup. 0 dépense. 0 PII. 0 outbound SMTP. 0 nouvelle page HTML. 0 nouveau signup. 0 Claude API external Builder. 0 Browserbase. 0 4ᵉ template. 0 weekly run #4. 0 Bluesky autonome. 0 PR opened. 0 spawn 3ᵉ sous-agent.

## PLAN-NEXT (run-302 ~T+15-60 min cron externe)

- (A) Inbox Florian MAX prio (ack fix + autres signals).
- (B) **Check `data/sub-agents/sub-judilibre-enrich.jsonl` cycle 4 ~15:29Z** : (i) si `outcome=ok` loyer-abusif 1→3 → 3/3 saturés → cycle 5 attendu `saturated_3`. (ii) si `outcome=api_fail` 2× consécutif → PATCH `enabled=0` (auto-rollback hard règle). (iii) si `outcome=saturated_3` directe (loyer 1→cycle a abandonné car already 1 < 3 mais 2 autres saturés ; pas le cas attendu).
- (C) Si cycle 5 `saturated_3` confirmé → PATCH `interval=21600s` (6h) ou `enabled=0` selon cost monitoring (saturated_3 cycles cheap mais bruit log).
- (D) IMAP poll auto reply ANIL/press/PR-#410/PR-#28.
- (E) PR #28 + #410 0 nag ≥2026-06-02.
- (F) Recurrence check Google referrer ip_hash 3790475865 (due 2026-05-21T12:53Z).
- (G) `data/sub-agents/seo-monitor.jsonl` 1ʳᵉ tick ~2026-05-20T13:29Z, rien à check run-302.
- (H) **Critic-21 ~T+48-72h** (strategic audit-8 due ~run-304, marge 3). Lecture prio si shipped.
- (I) **Anti-spawn-bomb 3ᵉ sous-agent maintenu** (≥2 cycles seo-monitor d'abord = ≥2026-05-21T13:29Z).
- (J) Options fallback si 0 Florian + cycle 4 pas dispo : J1 ne rien faire (anti-action-pour-action), J2 lire critic-19 ★ checkpoints résolution, J3 sample `https://bailleurverif.fr/sitemap.xml` HTTP 200 baseline lecture seule pour sub-seo-monitor demain.
- (K) « stop » Florian → arrêt. **0 ScheduleWakeup. Cron externe relance.**
