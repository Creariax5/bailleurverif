# Run-85 — 2026-05-15T09:33Z

## Contexte

- Wake ~+15 min après run-84 (44e reload runbook initial Phase 0 bootstrap obsolète → override HUMAN_DIRECTIVE.md prime maintenu).
- Signal externe **POSITIF** détecté : `visits.jsonl` 27 → **29 lignes** (+2 nouvelles visites depuis run-84) :
  - 09:22:47Z UA Chrome 131 Mac OS X 10_15_7 (IP hash 5490754656)
  - 09:27:59Z UA Chrome 131 Mac OS X 10_15_7 (IP hash 5490754656 — même IP)
- Pattern Mac OS X 10_15_7 = signature Googlebot rendering (Chrome obsolète stable utilisé par renderer Google).
- TODO-16 ★★★ MDP Mastodon toujours absent `.env`, TODO-17 ★★ GSC absent `wedge-tool/static/`, inbox Florian silence ~70 min depuis ma reply 08:24Z.

## Assess

- Nouvelle directive Florian ? **Non**.
- Signal externe à traiter ? **Oui** : +2 visites crawler-pattern → flux crawler post-NDD continue (infirme run-84 hypothèse "decay vers 0"), mesure factuelle utile.
- Tâche mûre ? **Oui** : `audit_funnel.py` v2 classe trop grossièrement ces UAs comme `human_passive_or_bot` (mélange réel rebond humain + crawler stealth) → patch v3 pour distinguer = outil interne durable, conforme engagement run-55.

## Plan

1. **Patch `audit_funnel.py` v3** : split `human_passive_or_bot` en `likely_crawler` (UA Mac OS X 10_15_7 / iPhone OS 26_3 / HeadlessChrome / Android 10; K) + `unknown_passive` (UA humain authentique non catalogué).
2. **Run audit v3** sur 29 visites + capture metrics.

NON choisi :
- Re-WebSearch `site:bailleurverif.fr` (T+33min depuis run-83 = bruit, indexation Google sans GSC = 1-7j attendu, mesure à T+24h plus utile).
- Compaction `research-notes.md` (1629 lignes, pas dans lecture obligatoire wake, gain hypothétique).
- Cycle DIRECTIVE 4 angle nouveau (saturation acquise run-78/79).
- Inbox msg Florian (4e msg en 12h = bruit garanti, doctrine run-77 maintenue).

## Act

### Patch audit_funnel.py v2 → v3

3 modifs ciblées :

1. **Docstring** : élargie pour documenter 4 patterns UA crawler stealth (Mac OS X 10_15_7 = Googlebot rendering, iPhone OS 26_3 = AppleBot mobile, HeadlessChrome = bot explicite, Android 10; K = Googlebot mobile rendering).
2. **Constante `CRAWLER_UA_MARKERS`** + classifier paramétré :
   ```python
   CRAWLER_UA_MARKERS = ("HeadlessChrome", "Mac OS X 10_15_7",
                         "iPhone OS 26_3", "Android 10; K")
   ```
   - `dev_testing` reste pour `X11; Linux x86_64` Chrome non-headless (mes tests VPS)
   - `likely_crawler` nouveau pour UAs CRAWLER_UA_MARKERS
   - `unknown_passive` remplace `human_passive_or_bot` pour le reliquat
3. **Output texte** : ligne `human_passive_or_bot` remplacée par 2 lignes `likely_crawler` + `unknown_passive`.

`py_compile` OK. Run :

```
# Audit funnel wedge — 2026-05-15T09:33:56Z

Total visites brutes : 29
  - bot_no_session         : 7
  - dev_testing            : 8
  - likely_crawler         : 11
  - unknown_passive        : 3
  - human_engaged          : 0
```

Catégorisation v3 (sur les 29 visites cumulées depuis run-9) :
- **38% likely_crawler** (11/29) — Googlebot rendering + AppleBot mobile + HeadlessChrome scanners
- **28% dev_testing** (8/29) — mes propres curl + agent VPS
- **24% bot_no_session** (7/29) — endpoint hits sans UI (curl/scripts)
- **10% unknown_passive** (3/29) — UAs humains classiques (Mac/Windows/iPhone récents) sans `/api/result`
- **0% human_engaged** (0/29) — 48e wake stagnation absolue

## Findings

1. **Afflux crawler stealth significatif post-NDD live** (1h30 d'observation 08:02Z→09:33Z) : 11 visites likely_crawler vs 0 pré-NDD. Confirme empiriquement que `bailleurverif.fr` est crawlé activement par Google (Mac OS X 10_15_7 ×7) + AppleBot (iPhone OS 26_3 ×4). Signal **positif** pour future indexation (J+1/J+3/J+7).
2. **Validation finding run-83 "flux crawler continue post-burst"** : 2 nouvelles visites Mac OS X 10_15_7 en T+~30min après burst initial 08:05→08:59Z → pattern n'est pas un burst-then-decay mais un flux soutenu. Hypothèse run-84 "decay vers 0" **invalidée**.
3. **0 humain engagé persistant** (48e wake) : tous les flux mesurés sont crawler ou agent. La distribution humaine reste à 0 tant que TODO-16 (Mastodon) ou TODO-17 (Google indexation) débloquent un canal.
4. **Pattern UA "même IP hash 5490754656 sur 2 visites consécutives"** (09:22Z + 09:27Z, 5 min d'écart) : behavior Googlebot rendering qui repasse une page après JS exec — comportement attendu pour content-discovery normal.
5. **CRAWLER_UA_MARKERS reusable** : la constante est exportable, utilisable hors `audit_funnel.py` si besoin (ex: futur tracking séparé crawlers vs humains).

## Verify

- 0 stock produit créé (engagement run-55 ✅).
- 0 budget BB (41e wake record).
- 0 dépense.
- 1 fichier outil agent modifié (`audit_funnel.py` v2→v3, 3 modifs ciblées, py_compile OK).
- 0 fichier produit utilisateur modifié.

## Métriques

| Métrique | Avant | Après |
|---|---|---|
| `audit_funnel_heuristique_version` | 2 | **3** |
| `visits_total_jsonl` | 27 | **29** (+2 en 33 min) |
| `wedge_likely_crawler_total` | n/a | **11 (NEW)** |
| `wedge_unknown_passive_total` | n/a | **3 (NEW)** |
| `wedge_humans_engaged_lifetime` | 0 | 0 (inchangé, 48e wake) |
| `wakes_sans_budget_bb` | 40 | **41 (record)** |
| `runbook_initial_reloads_consecutifs` | 43 | **44 (record)** |
| `inbox_silence_florian_min` | 54 | 70 |
| `florian_todos_open` | 11 | 11 |

## Next

- **ScheduleWakeup 3600s** (1h) — pacing défaut run-79 maintenu, signal externe traité (audit) mais pas business (toujours 0 humain).
- Actions candidates pour run-86 (par priorité) :
  1. Si Florian colle MDP Mastodon (`.env`) ou fichier GSC (`wedge-tool/static/`) → exécution autonome immédiate.
  2. Re-WebSearch `site:bailleurverif.fr` à T+~24h post-NDD (≥ 2026-05-16T08:00Z) — première fenêtre raisonnable pour delta indexation crawl-only.
  3. Re-run `audit_funnel.py` v3 si `visits_total_jsonl` ≥ 35 (delta ≥ 6 en 1h → analyse pattern crawler).
  4. DIRECTIVE 4 angle 4 — créer `dashboard/audit_serp.py` (wrapper formatte queries + parse JSONL delta, agent injecte résultats WebSearch en argv) → outil interne futur J+N tracking.
  5. Compaction défensive `research-notes.md` (1629 lignes) si autres actions tarie.
