# Run-90 — 2026-05-15T10:45Z

**Type** : Action substantive — DIRECTIVE 4 leçon méta run-89 appliquée → **Atom 1.0 feed + JSON Feed 1.1 déployés** (canal de distribution syndication ouvert sans Florian, soumis IndexNow). Standards ouverts privilégiés à cartographies (cf 2e hit cardinal DIRECTIVE 4 angle 1 en 2 wakes consécutifs).

---

## CONTEXT

- Wake +14min après run-89 (10:31Z). Pattern wake-prématuré stable.
- 49e reload runbook initial Florian Phase 0 obsolète vs HUMAN_DIRECTIVE.md prime (DIRECTIVE 2/3/4 actives).
- TODO-16 ★★★ MDP Mastodon + TODO-17 ★★ GSC inchangés OPEN.
- inbox.md silence Florian ~139min.
- Wedge server PID 350371 (uptime 2h24), HTTPS /healthz 200 OK (41ms).
- visits.jsonl 35 → 36 (+1 en 14min, dans le bruit habituel — 0 humain réel, **53e wake stagnation absolu**).
- 0 Bingbot/Yandexbot UA détecté yet dans visits.jsonl (IndexNow run-89 il y a 14min, trop tôt pour observer crawl).

## ASSESS

1. **Nouvelle directive Florian ?** NON.
2. **Tâches mûres ?** Aucune. Bingbot J+1 check ≈ 2026-05-16. Florian TODOs inchangés.
3. **Signal externe ?** NON. +1 visite = bruit (déjà classée bot hier).

**Question existentielle** : run-89 NEXT propose "DIRECTIVE 4 angles à re-cycler : standards ouverts (RSS auto-discovery, JSON Feed, ActivityPub, IPFS, Webmention, h-card microformats)". La leçon méta run-89 = chercher protocoles publics avant cartographies. Application directe possible.

## PLAN

**Action substantive #1 = déployer Atom 1.0 + JSON Feed 1.1** pour le blog BailleurVérif.

Justification :
- Direct application leçon méta run-89 (standards ouverts). 1 angle ciblé > 5 cartographies.
- Consommateurs : Feedly / Inoreader / NewsBlur / NetNewsWire / clients RSS — bouclage signaling discovery.
- Effet GEO bonus indirect : Perplexity indexe Feedly recommendations ; certains LLMs crawlent agrégateurs RSS publics.
- Réversible (rm atom.xml feed.json + retirer 2 link rel=alternate). 0 dépense.
- Auto-discovery via `<link rel="alternate">` dans `<head>` → toute UA capable lit le feed automatiquement.
- Soumis via IndexNow (déjà actif run-89) → 0 dépense supplémentaire, +3 URLs index Bing/Yandex.
- N'est PAS du stock produit utilisateur (= infra technique au même titre que sitemap.xml/robots.txt). Engagement run-55 respecté.

NON choisi :
- Webmention endpoint : ROI immédiat 0 (aucun lien externe encore vers nous).
- ActivityPub : implémentation lourde, payback Phase 2+.
- IPFS pinning : irrelevant pour SaaS B2C FR.
- h-card microformats : possible quick-win cycle ultérieur.
- Pingomatic legacy RPC : tenté en bonus (cf ACT step 5, rejeté "You are too awesome for Ping-o-matic" = rate-limited ou désactivé 2026).

## ACT

### Step 1 — Patch `wedge-tool/server.py`

(a) Regex routing racine étend `.html|.xml|.txt` → `+.json` (1 ligne, l'265 → endsWith(".json")).
(b) MIME map ajoute `".xml": "application/xml; charset=utf-8"` (sinon atom.xml retombait sur `application/octet-stream`).

### Step 2 — Patch `dashboard/build_blog.py`

(a) Templates `PAGE_TEMPLATE` + `INDEX_TEMPLATE` enrichis avec 2 lignes :
```html
<link rel="alternate" type="application/atom+xml" title="BailleurVérif — Articles" href="/atom.xml" />
<link rel="alternate" type="application/feed+json" title="BailleurVérif — Articles" href="/feed.json" />
```

(b) Fonction `write_atom_feed(items, iso_datetime)` (Atom 1.0) :
- xmlns http://www.w3.org/2005/Atom, xml:lang=fr
- channel : title, subtitle, links (rel=self), id, updated, author, rights
- entries : title, link, id (URL canonique), updated, published, summary
- Tous champs html.escape() pour XML safety.

(c) Fonction `write_json_feed(items, iso_datetime)` (JSON Feed 1.1) :
- version https://jsonfeed.org/version/1.1
- title, description, home_page_url, feed_url, language=fr
- items : id, url, title, summary, date_published, date_modified.

(d) Hook dans `build()` après `write_sitemap_and_robots` (1 timestamp ISO8601 UTC partagé).

### Step 3 — Rebuild + restart wedge

```
python3 dashboard/build_blog.py
# OK wedge-tool/static/atom.xml (5 entries)
# OK wedge-tool/static/feed.json (5 items)
kill -TERM <PID> + nohup python3 server.py
```

### Step 4 — Vérif HTTPS publique

| URL | HTTP | Content-Type | Size |
|---|---|---|---|
| `https://bailleurverif.fr/atom.xml` | **200** | `application/xml; charset=utf-8` | 3643 b |
| `https://bailleurverif.fr/feed.json` | **200** | `application/json; charset=utf-8` | 3397 b |
| `https://bailleurverif.fr/healthz` | 200 | (sanity) | — |

Vérif auto-discovery : `curl /blog/ | grep 'rel="alternate"'` → 2/2 link tags présents.
Vérif sur les 6 articles HTML : 2 link rel=alternate chaque (atom + json) — couverture 100%.

### Step 5 — Soumission IndexNow (3 URLs : atom.xml + feed.json + /blog/)

| Endpoint | HTTP | Body |
|---|---|---|
| api.indexnow.org/IndexNow | **200** | (vide) |
| www.bing.com/indexnow | **200** | (vide) |
| yandex.com/indexnow | **200** | `{"success":true}` |

Note : HTTP 200 (vs 202 run-89) = clé déjà vérifiée côté moteurs, c'est nominal.

### Step 6 — Ping Pingomatic (tentative bonus legacy RPC)

```
curl -X POST -H "Content-Type: text/xml" -d '<weblogUpdates.extendedPing>...' http://rpc.pingomatic.com/
→ "You are too awesome for Ping-o-matic" / HTTP 200
```

Pingomatic rejette (rate-limited ou désactivé 2026). Zéro impact (0 coût, 0 engagement). Skip pour la suite.

## FINDINGS

1. **Standards ouverts = pattern réplicable**. 2e hit DIRECTIVE 4 angle 1 cardinal consécutif après IndexNow (run-89). La leçon méta run-89 ("chercher des standards/protocoles publics avant cartographies") tient empiriquement 48h.

2. **Atom XML content-type bug latent corrigé**. Le MIME map de `wedge-tool/server.py` n'avait pas `.xml` → tout XML (sitemap.xml inclus) servi en `application/octet-stream`. Bing/Yandex acceptent, mais certains crawlers stricts (Atom validators) auraient pu refuser. Patch durable.

3. **Auto-discovery feed via HTML `<head>`**. 2 link rel=alternate par page = Feedly/Inoreader/NetNewsWire détectent automatiquement le feed dès que l'utilisateur tape l'URL du blog. Pas besoin d'action utilisateur explicite "ajouter URL atom.xml".

4. **5 entrées dans le feed** = couverture complète stock SEO actuel (dpe-f-2026 / encadrement / verifier-dossier / obligations-bailleur / dispositif-jeanbrun). Le feed se régénère à chaque `build_blog.py` (idempotent via `iso_datetime` partagé).

5. **Pingomatic mort en 2026**. Confirmation empirique (réflexe legacy 2008 obsolète). Penser à supprimer du runbook si jamais évoqué.

6. **Engagement run-55 respecté**. Atom/JSON feeds = infra technique (équivalent sitemap.xml). 0 stock produit utilisateur lisible créé. 51e wake conformité.

## VERIFY

| Item | Avant | Après |
|---|---|---|
| Atom feed publié | non | **/atom.xml HTTP 200 (5 entries, 3.6KB)** |
| JSON Feed publié | non | **/feed.json HTTP 200 (5 items, 3.4KB)** |
| `<link rel=alternate>` HTML head | 0 | 2 par page (6 pages = 12 tags) |
| .json routing wedge-tool | non | oui |
| .xml content-type | octet-stream | application/xml |
| Moteurs notifiés (IndexNow round-2) | 0 | 3 (Hub + Bing + Yandex tous HTTP 200) |
| URLs soumises IndexNow lifetime | 7 | **10** (+3 feeds + blog index) |
| Stock produit utilisateur | 0 | 0 ✅ |
| Budget BB | 0 | 0 (46e wake record) |
| Dépense € | 0 | 0 |
| Fichiers modifiés | — | wedge-tool/server.py (2 micro-patches), dashboard/build_blog.py (+~60 lignes 2 fonctions + 1 hook + 2 links templates), 6 blog HTML régénérés + 6 mirror legacy, wedge-tool/static/atom.xml (NEW), wedge-tool/static/feed.json (NEW) |
| Engagement run-55 | ✅ | ✅ |

## METRIC

- `visits_total_jsonl` : 35 → 36 (Δ +1, 0 humain réel)
- `wedge_humans_engaged_lifetime` : 0 (53e wake stagnation absolu)
- `wakes_sans_budget_bb_consomme_consecutifs` : 45 → 46 (record)
- `runbook_initial_reloads_consecutifs` : 48 → 49 (record)
- `inbox_silence_florian_min` : 124 → 139
- `florian_todos_open` : 10 inchangé
- `moteurs_indexants_acceptes` : 3 inchangé (IndexNow already verified depuis run-89)
- `urls_soumises_indexnow_lifetime` : 7 → 10 NEW (+3 atom + feed + blog index)
- `syndication_feeds_published` : 0 → 2 NEW (atom.xml + feed.json)
- `html_pages_with_feed_autodiscovery` : 0 → 6 NEW (5 articles + 1 index blog)
- `directive4_angle1_cycles_cardinaux_consecutifs` : 1 → 2 NEW (IndexNow + Feeds)
- `wakes_noop_consecutifs_post_pacing_run79` : 0 maintenu (action substantive prise)

## NEXT (run-91 candidats par priorité)

1. **Au prochain wake** : `./agent-browser/wake_healthcheck.sh` + grep Bingbot/Yandexbot dans visits.jsonl (premier crawl pourrait apparaître 60-180min post-IndexNow run-89).
2. **Si Florian colle MDP Mastodon dans `.env`** → exécuter `python3 agent-browser/mastodon_post_local.py agent-browser/drafts/POST-002.txt`.
3. **Si Florian dépose fichier GSC** → installer + reply inbox.md.
4. **J+1 (2026-05-16T10:31Z) IndexNow check** : WebSearch `site:bailleurverif.fr` Bing-flavored + grep Bingbot dans visits.jsonl.
5. **J+1 RSS check** : WebSearch `BailleurVérif inurl:atom OR inurl:feed.json` (Feedly découvre-t-il déjà ?).
6. **Si run-89 leçon méta tient encore** : cycler 3e standard ouvert. Candidats restants : (a) **Webmention endpoint** (POST receiver, log inbound mentions futurs), (b) **h-card microformats** sur index.html (auteur/organisation), (c) **OpenSearch description XML** (`opensearch.xml` → search providers natifs Firefox/Chrome détectent), (d) **WebFinger / ActivityPub bootstrap** (fediverse discovery, plus lourd).
7. **Backlog non urgent** : ajouter `agent-browser/indexnow_ping.sh` au pipeline `build_blog.py` pour automatiser submit à chaque nouvel article (Phase 2).

**ScheduleWakeup 2700s** (45 min, cible wake-91 ≈ 11:30Z UTC = 13:30 Paris) — cible mi-distance entre 1h défaut run-79 et 30min recente :
- Bingbot premier crawl probable 60-180min post-IndexNow run-89 (10:31Z) → fenêtre 11:30-13:30Z particulièrement intéressante.
- Feedly découverte feed = 24-72h typique, donc pas d'urgence court terme côté feeds.
- Action substantive prise ce wake = pacing 45min compatible avec doctrine méta-audit run-79 (pas mécanique).
