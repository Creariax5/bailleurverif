# Run-194 — 2026-05-17T12:14Z — EXPORT CSV PUBLIC OBSERVATOIRE + IndexNow R-61

**Wake** : 70ᵉ DIRECTIVE 7 ZERO-POSE. ~14 min après run-193 (12:00Z attendu + drift).
**Mode** : 1 wake moat (quota alternance ouvert post 4/4 hors-moat record-11) — option (iii) state.md run-193 PLAN-NEXT.
**Input Florian** : 0 nouveau msg depuis 08:05Z mission moat. Pas de clé API data.gouv.fr → TODO-24 reste latent.

## Décision & justification

Choisie option (iii) : export CSV public observatoire. Rationale :
- Asset distribution citable indépendant du site (peut survivre cache / archive / partage email).
- Donne au reuse data.gouv.fr (TODO-24 latent) une distribution `DataDownload` structurée — condition prérequise pour qualité éditoriale data.gouv.
- Friction extérieure ÷21 (de "21 min clone+run" à "1 clic download").
- Moat compatible : CSV est snapshot statique CC-BY ; le pipeline crawler+dedupe+scoring auto-update (cron 03:00 UTC) reste non-copiable (la copie d'un snapshot CSV ne procure aucun compounding au copieur).

Options écartées : (i) PAP sitemap probe = source dead-end probable d'après run-189 (Next.js shell / 404) ; (ii) cron LIMIT 30→50 = bénéfice indirect uniquement (N+200 lendemain seulement, vs CSV immédiat) ; (iv) wedge LMNP = copyability ~80% low ROI run-193.

## Actions

### 1. Script export CSV
- `/tmp/export_observatoire_csv.py` 86 LOC Python stdlib (csv, json, hashlib, pathlib).
- Input : `wedge-tool/data/listings/all-cities-2026-05-17.dedup.scored.jsonl` (160 rows).
- Output : `wedge-tool/static/data/observatoire-annonces-loyer-2026-05-17.csv`.
- 23 colonnes : ts_score, source, url_hash, accommodation_id, ville_label, code_postal, code_dept, in_scope_encadrement, surface_m2, loyer_eur_total, eur_per_m2, dpe_letter, ges_letter, meuble, commune_slug, plafond_applied_eur_m2, encadrement_violation, encadrement_excess_eur_m2, encadrement_excess_pct, dpe_violation, violation_type, violation_score, score_version.
- Dérivations vs JSONL brut :
  - `code_dept` = 2 premiers digits `code_postal` (cas Corse 20 → "2A_or_2B" placeholder)
  - `in_scope_encadrement` = "true" si `plafond_applied_eur_m2 is not None` sinon "false" (1 booléen explicite vs check indirect)
- Anti-PII : URL raw exclue, `url_hash` exposé seul (déjà sha1[:16] côté crawler).
- Dedup safety : set `seen_hash` (idempotent même si JSONL contient doublons).
- Exit OK : `rows_in=160 rows_out=160 bytes=24996`.

### 2. Publication prod
- Fichier déposé `wedge-tool/static/data/observatoire-annonces-loyer-2026-05-17.csv` (24 996 bytes).
- Server.py static handler ligne 279 mappe `.csv` → `text/csv; charset=utf-8`. Routing existant — **0 server restart nécessaire**.
- Validation live : `curl -sS https://bailleurverif.fr/data/observatoire-annonces-loyer-2026-05-17.csv` = HTTP 200 text/csv 24 996 bytes, header + 160 rows servi.

### 3. Patch observatoire-annonces-loyer.html
- Méthodologie : insertion `<li>` #7 après ligne reproductibilité GitHub :
  ```html
  <li><strong>Téléchargement direct</strong> : <a href="/data/observatoire-annonces-loyer-2026-05-17.csv" rel="noopener" class="underline" style="color:var(--accent)" download><code>observatoire-annonces-loyer-2026-05-17.csv</code></a> (160 lignes × 23 colonnes, 25 KB, licence ouverte Etalab 2.0, prêt Excel/R/pandas). 0 PII vendeur (URL hashée).</li>
  ```
- JSON-LD `Dataset` (ligne 460) : insertion `distribution: [{...DataDownload}]` avant `license` :
  ```json
  "distribution": [{
    "@type": "DataDownload",
    "encodingFormat": "text/csv",
    "contentUrl": "https://bailleurverif.fr/data/observatoire-annonces-loyer-2026-05-17.csv",
    "contentSize": "24996",
    "name": "Observatoire BailleurVérif — annonces locservice.fr scorées — 2026-05-17 (CSV)",
    "description": "160 lignes × 23 colonnes. URL hashée (0 PII vendeur). Colonnes: ..."
  }]
  ```
- Validation prod : `curl https://bailleurverif.fr/observatoire-annonces-loyer.html | grep -c observatoire-annonces-loyer-2026-05-17.csv` = 2 (lien méthodologie + JSON-LD `contentUrl`).

### 4. IndexNow round-61
- Payload : 2 URLs (CSV + observatoire HTML refresh JSON-LD).
- Endpoints : api.indexnow.org=200, bing.com/indexnow=200, yandex.com/indexnow=202 (queued).
- Round 60→61 lifetime.

### 5. Enrichissement data-gouv-fr-reuse-payload.json
- Ajout section "Téléchargement direct des données scorées" :
  ```
  ## Téléchargement direct des données scorées
  - CSV brut scoré : https://bailleurverif.fr/data/observatoire-annonces-loyer-2026-05-17.csv
    (160 lignes × 23 colonnes, 25 KB, licence ouverte Etalab 2.0). Importable directement
    Excel / pandas / R sans dépendance. URL hashée (0 PII vendeur). Colonnes incluent
    eur_per_m2, plafond_applied_eur_m2, encadrement_violation, encadrement_excess_pct,
    dpe_letter, violation_type.
  ```
- Description 3674 → 4104 chars (+430). JSON valide (`python3 -c "json.load(...)"` OK).

## Validation artefacts

```
$ python3 /tmp/export_observatoire_csv.py
OK rows_in=160 rows_out=160 bytes=24996 dst=.../observatoire-annonces-loyer-2026-05-17.csv

$ curl -sSo /dev/null -w "%{http_code} %{content_type} %{size_download}" \
    https://bailleurverif.fr/data/observatoire-annonces-loyer-2026-05-17.csv
200 text/csv; charset=utf-8 24996

$ curl -sS https://bailleurverif.fr/observatoire-annonces-loyer.html \
    | grep -c "observatoire-annonces-loyer-2026-05-17.csv"
2

$ python3 -c "import json; d=json.load(open('data-gouv-fr-reuse-payload.json')); \
    print(len(d['description']))"
4104

IndexNow R-61:
  api.indexnow.org   HTTP 200
  www.bing.com       HTTP 200
  yandex.com         HTTP 202
```

## KPIs

| Métrique | Avant | Après |
|---|---|---|
| csv_export_public_rows | 0 | 160 ★ |
| csv_export_public_bytes | 0 | 24 996 ★ |
| dataset_distributions_lifetime | 0 | 1 ★ |
| indexnow_rounds_total_lifetime | 60 | 61 |
| reuse_payload_chars | 3 674 | 4 104 |
| wakes_construction_consecutifs_moat | 0 | 1 |
| visits_total | 164 | 164 |
| pages_total_live | 170 | 170 |
| humans_engaged_lifetime | 2 | 2 |
| wakes_total_lifetime | 193 | 194 |

## Discipline

- DIRECTIVE 5 ZERO-POSE : ScheduleWakeup 270s (cycle cache warm).
- DIRECTIVE 9 copyability check : ✅ CSV statique CC-BY, mais pipeline crawler+dedupe+scoring+cron quotidien = compounding non-copiable.
- DIRECTIVE 9 alternance moat ≤1/3 : ✅ 1 wake moat / 5 wakes récents = 20 % (run-190/191/192/193 hors-moat + run-194 moat).
- DIRECTIVE 7 (zéro-pose) : ✅ wake productif (1 cardinale + 1 IndexNow), pas un STOP.
- 0 dépense / 0 régression / 0 PII vendeur / 0 nouvelle page HTML / 0 server restart / 0 BG process / 0 crawl ce wake.

## Plan run-195

- (A) Inbox 70ᵉ STOP minimal
- (B) Si silence : pivoter hors moat — options (i) PAP sitemap-only V1 probe ; (ii) audit `/api/step` jsonl (probable 0 sessions, attendre J+1+ Googlebot post pages pre-fill) ; (iii) wedge LMNP régime fiscal ; (iv) draft thread X/Bluesky preuve-CSV
- (C) PAS moat (1/1 consommé)
- (D) PAS new HTML (31ᵉ wake)
- (E) PAS IndexNow (no nouvelle URL)
- (F) Si Florian a collé `TODO-24 api-key:` inbox → priorité MAX submit + archive + révocation
- (G) Si Florian "stop" inbox → arrêt
