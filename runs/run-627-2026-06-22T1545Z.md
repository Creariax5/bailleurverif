# run-627 — 2026-06-22T15:45Z

## WHY_THIS_NOT_THAT (DIRECTIVE 10)

**Contexte** : run-626 (T-2h) a shippé le friction-fix P1 #1 (chips ville cliquables Step 1) sur le goulot home→q1 = 7,6 % (Strategic audit-67 §4, nommé 3×). Smoke run-626 = curl + lecture code uniquement. Pas de nouveau critic/Florian depuis critic-91 (06-20T07:01Z, déjà honoré run-625). PAT toujours invalide (test push HEAD → `Invalid username or token` confirmé empirique, token `.env` mort) ⇒ SB-5 N/A reflag, PAS de re-flag Florian (feedback [[gh-pat-conserve]] + anti-récursion).

**THIS — Validation user-réelle du fix P1 + P3 MESURE (PAS un nouveau ship redondant)** :
Le fix run-626 cible directement le levier North-Star (home→q1). Le valider via curl/lecture ≠ prouver qu'il marche pour le user mobile cible. J'ai écrit `agent-browser/smoke_city_chips_run627.py` (Playwright headless, viewport iPhone 390×844, `is_mobile`, UA self-tag `BailleurVerifSmoke` → exclu metric humans). Résultat **PASS** : tap chip "Paris" → `#q-ville='Paris'` + `Question 2 / 5` atteinte + touch-target = **44px** (mobile-first conforme). Le fix fonctionne réellement bout-en-bout en prod. Harness réutilisable versionné.

**NOT THAT** :
- **PAS un 2ᵉ ship produit le même jour** : run-626 a déjà livré le fix P1 il y a 2h ; re-shipper une feature serait du volume, pas de la valeur (critic-91 STOP over-ship). Valider ce qui vient d'être livré = boucle qualité honnête.
- **PAS de méta-théâtre "wake X/N acquis"** (critic-91 STOP #1 STRICT preserved).
- **PAS de robots.txt llms.txt "fix"** : vérifié, robots.txt RÉFÉRENCE déjà llms.txt + llms-full.txt (commentaires convention llmstxt.org) + `Sitemap:`. Strategic flag (i) audit-67 §1 = déjà adressé. Touch = théâtre, abandonné.
- **PAS FAQPage cycle 6 Plaine Commune** (critic-90 STOP #1 STRICT).
- **PAS de push GitHub** : token `.env` invalide (auth fail empirique), re-flag = anti-récursion SB-5.

## SHIP / SMOKE

`agent-browser/smoke_city_chips_run627.py` (NEW, 68L test harness — pas user-facing, hors pages_total). Run via `venv-browser/bin/python` :
```
NOTES: chip height=44px | counter='Question 2 / 5' q-ville='Paris'
PASS: tap chip Paris -> Question 2/5 + q-ville=Paris (fix P1 VALIDÉ user-réel) — EXIT 0
```

## État

- visits 507 / humans 9-13 raw UNCHANGED gap ~146h depuis #14 Paris (06-16T10:10Z) — bruit Poisson dominant (test Mann-Whitney run-601)
- pages_total=233 UNCHANGED (harness smoke ≠ page user-facing)
- PAT 67 commits ahead, push auth fail empirique (token mort) — SB-5 N/A, ledger-only, 0 Florian re-flag
- PAS ScheduleWakeup (DIRECTIVE 7). NEXT : cron. Observation funnel home→q1 ratio post-fix run-626 (baseline 7,6 %) verdict T+7j signal lent.
