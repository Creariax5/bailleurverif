# Run-93 — 2026-05-15T11:18Z

**Type** : Wake déclenché par signal externe (TODO-16 levé apparemment, MDP Mastodon collé .env 11:08:54Z par Florian). Découverte incident ★★★ + livraison cardinal compensateur.

---

## CONTEXT

- Wake +18min après run-92 (DIRECTIVE 5 conforme pacing actif).
- `wake_healthcheck.sh` (run-88) signale : `TODO-16 MDP Mastodon : PRESENT` → unblock POST-002 attendu.
- visits.jsonl 36→38 (+2 en 18min, crawlers/passifs typiques, 0 humain engagé).
- 10 TODO Florian OPEN (TODO-16 ★★★, TODO-17 ★★, 8 autres).
- HUMAN_DIRECTIVE.md inchangée depuis run-91 (DIRECTIVE 5 active).

## ASSESS

**Signal externe Florian** : OUI, `MASTODON_PASSWORD=` collé .env à 11:08:54Z. C'est le déblocage que j'attendais depuis run-71 (5h53min). Action attendue = exécuter POST-002 via `mastodon_post_local.py`.

**Tâches mûres** : POST-002 cardinal + cycle Patch JSON-LD #3/#4/#5 (Phase 2 GEO/SEO).

**Anti-pattern à éviter** : si POST-002 réussit, NE PAS enchaîner immédiatement POST-003→006 (engagement run-55 + cadence anti-shadowban DIRECTIVE 3 : max 5 posts/jour, ratio 80% utile / 20% promo).

## PLAN

1. POST-002 via mastodon_post_local.py + audit qualité immédiat (API publique).
2. Si OK → marquer TODO-16 DONE + state.md + ledger.md.
3. Patch JSON-LD #3 FAQPage en parallèle (cardinal indépendant Mastodon).
4. ScheduleWakeup court (180s, DIRECTIVE 5).

## ACT

### Step 1 — POST-002 tentative

```bash
source venv-browser/bin/activate
python3 agent-browser/mastodon_post_local.py agent-browser/drafts/POST-002.txt
```

Résultat : **exit 1 — not_logged_in**. Login flow exécuté (`login_performed=true`), MAIS verify URL = `https://piaille.fr/auth/edit` (au lieu de `/home`). Log JSON : `agent-browser/logs/mastodon-post-local-POST-002-20260515T111707Z.json`.

Précision : un log antérieur existe à `mastodon-post-local-POST-002-20260515T110858Z.json` (~4s après collage MDP par Florian) → Florian probablement testé lui-même avant de me signaler. Même symptôme `/auth/edit`. Aucun ledger entry → résultat non documenté côté Florian.

### Step 2 — Diagnostic /auth/edit

Script ad-hoc créé : `agent-browser/mastodon_diagnose_authedit.py` (~70 lignes Playwright headless, read-only, dump URL + title + body excerpt + form fields).

Résultat :

| Champ | Valeur |
|---|---|
| `url_after_submit` | `https://piaille.fr/auth/edit` |
| `title` | "Paramètres du compte - Piaille" |
| `body_excerpt` | **"Vous ne pouvez plus utiliser votre compte, votre profil et vos autres données ne sont plus accessibles. […] Suspension de compte du 15 mai 2026 — 15 mai 2026 à 10:32."** |
| `after_home_attempt_url` | `/auth/edit` (redirect persistant) |

→ **Compte `@bailleurverif@piaille.fr` SUSPENDU par admin piaille.fr 2026-05-15T10:32Z**. ~2.5h avant que Florian colle le MDP. Login fonctionne (cred OK) mais compte gelé.

### Step 3 — Incident logged + Florian notifié

- `incidents.md` § 2026-05-15T11:17Z : 70 lignes (symptôme + 3 causes hyp + mitigation + 5 findings méta).
- `florian-todos.md` TODO-16 **RE-PURPOSE** : 3 options (A migration mastodon.social reco / B appel modération / C abandon Mastodon).
- `inbox.md` : notif explicite Florian + reco A + question discrète sur lien-in-bio post-migration.
- `tasks.md` : POST-002 → BLOCKED suspension.

### Step 4 — Patch JSON-LD #3 FAQPage (cardinal compensateur)

DIRECTIVE 5 : pacing actif continu. Pas de dormance après blocage. Bascule sur cardinal indépendant Mastodon.

**Implémentation** :
- 2 fonctions pures `extract_faq()` + `faq_node()` dans `dashboard/build_blog.py` (~40 lignes nettes).
- Pattern parser : `^**N. Question ?**\nRéponse` regex multiline. Garde : section bornée par heading `## FAQ` (sinon faux-positif sur arbre décisionnel DPE-F où le format syntaxique est identique).
- Extension `article_jsonld(..., faq_qa=None)` : si non-vide, ajoute FAQPage au @graph.

**Build vérification** :

| Article | Graph types | FAQ count |
|---|---|---|
| dispositif-jeanbrun-2026 | Article + Organization + WebSite + **FAQPage** | **10 Q/A** |
| dpe-f-location-2026 | Article + Organization + WebSite | 0 ✅ (garde efficace) |
| encadrement-loyer-... | idem | 0 |
| verifier-dossier-... | idem | 0 |
| obligations-bailleur-... | idem | 0 |

**HTTPS public** : `https://bailleurverif.fr/blog/dispositif-jeanbrun-2026.html` HTTP 200 (67ms). Parser JSON-LD interne confirme 4 nodes avec FAQPage 10 mainEntity Question/Answer.

**IndexNow round-4** : URL Jeanbrun soumise aux 3 endpoints (api.indexnow.org HTTP 200, bing.com HTTP 200, yandex.com HTTP 202). URLs lifetime soumises 12 → 13.

## FINDINGS

1. **Premier canal autonome opérationnel = mort en 17h** (signup 2026-05-14T17:50Z, suspension 2026-05-15T10:32Z). Hypothèse de travail : instances Mastodon associatives FR (piaille.fr, mamot.fr) sont hostiles aux comptes promo, même factuels. Migration mastodon.social = candidate principale (instance officielle ~1M users, modération plus permissive).

2. **Engagement run-55 vindiqué empiriquement** : doctrine "0 stock post tant que 0 canal débloqué" a limité la perte à **1 post** (POST-001). Si on avait posté POST-002→006 immédiatement, 6 posts à jeter au lieu de 1. Doctrine durable.

3. **Diversification obligatoire** : ne plus dépendre d'un seul compte / une seule instance. Multi-instances Mastodon + Bluesky (TODO-14) + LinkedIn (TODO-3) doivent être parallèles, pas séquentiels.

4. **Causalité incident** : indices contre "spam policy" stricte (POST-001 avait 0 engagement externe = pas signalé via fav négatif). Pencher (a) pattern Browserbase US-West + Playwright local (changement d'IPs inhabituelle, red-flag bot) ou (b) modération préventive de signaux faibles (compte neuf + URL promo + UA scripté).

5. **Garde `FAQ heading` nécessaire** dans parser : sans elle, arbre décisionnel DPE-F est faussement déclaré FAQPage. Découverte au 1er run, fix au 2e build. Pattern de prudence applicable à futurs parsers GEO (Dataset, etc.).

## VERIFY

| Item | Avant | Après |
|---|---|---|
| jsonld_nodes_per_page_max | 3 (run-92) | **4** (Jeanbrun FAQPage) |
| jsonld_faqpage_articles | 0 | **1** (Jeanbrun) |
| faq_qa_pairs_lifetime | 0 | **10** |
| urls_soumises_indexnow_lifetime | 12 | **13** |
| mastodon_account_status | active | **SUSPENDED 10:32Z** ★★★ |
| premier_canal_autonome_lifespan_h | open | **CLOS ~17h** |
| wakes_sans_humain_engaged | 50 (run-87 max) | **52** (record) |
| Stock produit utilisateur | 0 | 0 ✅ (engagement run-55) |
| Budget BB | 0 | 0 (48e wake record) |
| Dépense € | 0 | 0 |

## METRIC

- `directive4_angle1_cycles_cardinaux_consecutifs` : 4 → maintenu (Patch JSON-LD #3 = continuation pas nouveau cycle angle 1)
- `patches_jsonld_completed_phase2` : 1/5 → **2/5** (#1 Org+WebSite run-92, #3 FAQPage run-93)
- `florian_todos_open_count` : 10 → **10** (TODO-16 re-purposé, pas fermé)
- `incidents_logged_lifetime` : 1 (smoke test BB run-29) → **2** (suspension Mastodon)

## NEXT (run-94 candidats)

1. **Wake court 180s** (DIRECTIVE 5 conforme).
2. Si Florian répond TODO-16 dans inbox.md → exécution autonome :
   - (A) migration mastodon.social → adapt `mastodon_post_local.py` INSTANCE + `mastodon_profile.py` + republier POST-001 sans URL
   - (B) appel modération → préparer texte d'appel respectueux + colle dans inbox.md pour Florian-action
   - (C) abandon → archive `agent-browser/drafts/POST-*.txt` dans subdir `archived/`, pivot 100% Bluesky/Twitter
3. Sinon, **Patch JSON-LD #4 Dataset encadrement loyer** (★★★, ~60-80 lignes Python, données déjà côté `app.js` 31 communes × T1-T4 × meublé/nu, niveau publication scientifique GEO).
4. OU **Patch JSON-LD #5 SoftwareApplication global** (★★★, ~25-30 lignes Python, identité produit sur toutes pages, différentiateur Qlower/Hestia).
5. Visits delta surveillance (Bingbot crawl attendu post-IndexNow round-1 il y a ~47min).

**ScheduleWakeup 180s** justifié DIRECTIVE 5 + backlog Patch JSON-LD restant cardinal.
