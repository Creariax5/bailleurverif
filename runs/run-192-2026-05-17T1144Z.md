# Run-192 — 2026-05-17T11:44Z — ★★★ /api/step instrumentation funnel quiz (levier e conversion 3ᵉ wake consécutif)

**Wake 68ᵉ DIRECTIVE 7 ZERO-POSE. 3ᵉ wake LEVIER (e) CONVERSION consécutif** (run-190 contraste CTA + run-191 pre-fill `?q=Ville` + run-192 télémétrie step). Pas moat (engagement alternance ≤1/3 honorée 3/3 hors-moat). Aucun nouveau msg Florian depuis 08:05Z mission moat. Plan (B) state.md run-191 exécuté.

## Action substantive (1 cardinale + 0 polish)

### ★★★ /api/step endpoint + client step instrumentation

**Diagnostic** : avant ce wake, la mesure du funnel quiz se limite à `visits → results` (1,2 % baseline post-run-190). Drop entre Q1 et Q5 = boîte noire — impossible de savoir où le drop majeur a lieu. Question opérationnelle : faut-il optimiser Q1 (ville), Q2 (nu/meublé), Q3 (surface), Q4 (loyer), Q5 (DPE), ou autre chose entièrement ? Sans télémétrie par step, chaque optim conversion = pari.

**Implémentation** :

1. **server.py** : `STEPS_FILE = data/steps.jsonl` + handler POST `/api/step` (lignes ~30 + ~957-983). Validation stricte `from_step ∈ {1..5}`, `to_step ∈ {1..5, "result"}`, `ms_on_step` clamp [0, 3 600 000] (1h max). Champ `ip_hash` réutilisé (pseudonymisé). 27 LOC ajoutées.

2. **app.js** : `state.stepStartTime` ajouté init + reset dans `restart()`. Dans `next(fromStep)`, après validation et avant transition DOM, POST `/api/step` fire-and-forget avec `from_step`, `to_step` (= `fromStep+1` ou `"result"`), `ms_on_step = Date.now() - state.stepStartTime`, `path = location.pathname`. Reset `stepStartTime` après envoi. 13 LOC ajoutées.

**Pourquoi cardinal** : sans cette télémétrie, **chaque optim conversion run-190+ est aveugle**. Après 1-2 jours de trafic SEO live + crawl Google des 81 pages pre-fill, on saura :
- Q1 → Q2 drop-rate (pré-fill efficace ? les visiteurs SEO trouvent-ils la ville pre-remplie acceptable ?)
- Q2 → Q3 drop-rate (binary choice nu/meublé = friction minimale attendue)
- Q3 → Q4 drop-rate (surface m² = first numeric input)
- Q4 → Q5 drop-rate (loyer = second numeric, sensitive)
- Q5 → result drop-rate (DPE = last + optional via "continuer sans DPE")
- Distribution `ms_on_step` par step (révèle où le visiteur hésite/lit/réfléchit)

C'est la 1ʳᵉ télémétrie funnel granulaire du projet en 192 wakes. Couplée avec instrumentation `path` (run-190), on pourra croiser "quelle page d'entrée donne quel pattern de drop ?" — par exemple si les visiteurs SEO Lille drop massivement Q3 (surface) parce qu'ils sont locataires curieux et pas bailleurs (vrai use-case).

## Validation live (artefacts)

```
$ python3 -c "import ast; ast.parse(open('server.py').read()); print('syntax OK')"
syntax OK

$ kill 1245689 ; nohup python3 server.py > server.log 2>&1 &
$ ss -ltnp | grep 8102
LISTEN ... pid=1254741

# Smoke 3 happy paths
$ curl -sS -X POST http://localhost:8102/api/step -d '{"sessionId":"smoke-run192","from_step":1,"to_step":2,"ms_on_step":3200,"path":"/lille-dpe-f-g-interdit-location.html"}' -H 'Content-Type: application/json'
{"ok": true}
$ curl -sS -X POST http://localhost:8102/api/step -d '{"sessionId":"smoke-run192","from_step":5,"to_step":"result","ms_on_step":1500,"path":"/lille-dpe-f-g-interdit-location.html"}' -H 'Content-Type: application/json'
{"ok": true}

# Smoke 2 validation rejects
$ curl -sS -X POST http://localhost:8102/api/step -d '{"sessionId":"smoke-bad","from_step":9,"to_step":10}' -H 'Content-Type: application/json'
{"ok": false, "error": "invalid from_step"}
$ curl -sS -X POST http://localhost:8102/api/step -d '{"sessionId":"smoke-bad","from_step":"abc"}' -H 'Content-Type: application/json'
{"ok": false, "error": "bad step"}

# JSONL persisted with full fields
$ tail -3 data/steps.jsonl
{"ts":"2026-05-17T11:44:25+00:00","sessionId":"smoke-run192","from_step":1,"to_step":2,"ms_on_step":3200,"path":"/lille-dpe-f-g-interdit-location.html","ip_hash":"2811160087"}
...

# Prod HTTPS endpoint live
$ curl -sS -X POST https://bailleurverif.fr/api/step -d '{"sessionId":"smoke-https-run192","from_step":1,"to_step":2,"ms_on_step":2000,"path":"/"}' -H 'Content-Type: application/json'
{"ok": true}

# Prod app.js shipping new code
$ curl -sS https://bailleurverif.fr/static/app.js | grep -c "/api/step"
1
```

## KPIs run-192

- **bug_latent_fixé_lifetime=14 maintenu** (instrumentation = ajout télémétrie, pas un bug)
- **funnel_telemetry_per_step=live** ★ NEW
- **conversion_levier_e_consecutifs=2→3** ★ NEW (run-190 + run-191 + run-192)
- **conversion_levier_e_stale_wakes=0 maintenu** (touché ce wake)
- **wakes_construction_consecutifs_moat=0 maintenu** (3/3 wakes hors-moat post-record-11, alternance honorée DIRECTIVE 9)
- **visits_total=164 maintenu** (smoke HTTPS = ip_hash mien)
- **pages_total_live=170 maintenu** (28ᵉ wake discipline empilement)
- **wakes_total_lifetime=191→192**
- **wakes_executifs_nouvelle_mission=92→93**
- **humans_engaged_lifetime=2 maintenu**
- **schedulewakeup_default=270s** (cycle cache warm)
- **0 dépense, 0 régression, 0 PII, 0 nouvelle page HTML, 0 IndexNow round-61, 0 crawl, 0 BG process**
- **server PID 1245689 → 1254741** (restart pour charger nouveau endpoint)

## Discipline DIRECTIVE 9 copyability check

`/api/step` = instrumentation interne, pas asset publié pour compétiteur. Aucun moat compromis. La data steps.jsonl agrégée pourrait éventuellement devenir un signal "quel step casse le plus" public, mais pour l'instant = télémétrie privée → décisions optim. ROI direct mission 5000 users via réduction friction itérative basée data.

## Honnêteté distribution

- Headline observatoire 36/61=59,0 % CI ±12 pts inchangé (pas de crawl)
- **0 nouveau signup, 2 humains lifetime maintenu** — l'instrumentation est utile **dès qu'il y a du trafic**. Aujourd'hui 0 humain visit → records vides à J+0 ; J+1+ post Googlebot crawl pages pre-fill = mesure réelle possible
- Run-192 = 3ᵉ wake levier (e) en 82 wakes (run-110 = précédent levier e pré-pivot). Chaîne conversion progressivement musclée

## Next run-193 (~270s, ~11:48Z)

- **(A)** Inbox 68ᵉ STOP minimal
- **(B)** Si silence : pivoter levier — **3 wakes conversion consécutifs = suffisant**. Options run-193 :
  - (i) **Audit data.gouv.fr API** : possible POST `/api/1/reuses/` avec un user-agent légitime + champs minimal pour soumission TODO-24 ? (vérifier si scope nécessite token vs anonyme)
  - (ii) **Wedge tool simulateur LMNP régime fiscal** : levier (c) multi-wedge, nouveau wedge gratuit angle bailleur — angle pas couvert par pages existantes
  - (iii) **Reddit r/Immobilier/r/vosfinances** : levier (b) distribution social, browser-bridge probe Florian session live
- **(C)** PAS moat (alternance ≤1/3 wakes prochains 9 ; ce wake fait 3/3 hors-moat acquis → 1 moat ok run-194+ si pertinent)
- **(D)** PAS IndexNow (no nouvelle URL)
- **(E)** PAS new HTML (28ᵉ wake)
- **(F)** PAS draft Show HN (anti-spam Florian, attendre traction)
