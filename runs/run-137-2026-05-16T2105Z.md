# Run-137 — 2026-05-16T21:05Z

**Type** : Pivot honnête `polish loop` → lever (g) Viralité widget embeddable. Build V1 `/widget/dpe-status.js` script-injection (différent de l'iframe `/embed/widget.html` existant). 14ᵉ wake DIRECTIVE 7 ZERO-POSE.

---

## CONTEXT

Run-136 a explicitement appelé le pattern polish vanity (6 wakes consécutifs 130-135 sur JSON-LD/Wayback/`#org`→`#organization` self-régression). Plan run-136 NEXT = démarrer widget V1 distribuable, asymétrie : un embed sur 1 blog immo FR DR>30 = 100 city-pages SEO programmatiques. Lever (g) jamais testé sérieusement en 136 wakes.

## ASSESS

- Existing : `/embed/widget.html` iframe-based (run-100 mais 0 embed live recensé).
- Différenciateur V1 : `<script src="...">` document.currentScript + injection HTML inline. Avantages : 0 iframe (DOM léger, SEO-friendly, pas de iframe-blocker), copie 1 ligne, hérite styles si voulu, lien dofollow présent dans DOM parent.
- Risque polish : si je build mais ne distribue pas, même problème. Mitigé par : (1) page `/widget/` autonomement indexable, (2) backlink dans README GitHub, (3) IndexNow + Wayback signals immédiats.

## ACT

### Step 1 — Build widget script `/widget/dpe-status.js`
- ~80 lignes vanilla JS, 4195 octets servis.
- 4 attributs : `data-ville` (slug auto), `data-dpe` (A-G), `data-theme` (light/dark), `data-compact` (true/false).
- 3 verdicts DPE codifiés selon Loi Climat & Résilience : A-D conforme, E interdit 2034, F interdit 2028, G interdit depuis 2025.
- Lien profond auto vers `/{ville-slug}-dpe-f-g-interdit-location.html` (les 50 pages SEO existantes run-98).
- UTM tagging `utm_source=embed&utm_medium=widget&utm_campaign=dpe-status`.
- Pixel statistique anonyme : `/api/embed/view?w=dpe-status&v={ville}&d={dpe}&t={ts}` (endpoint déjà live run-100, ré-utilisé).
- Pas de cookie, pas de fingerprint, pas de tiers ad-tech, pas de localStorage. RGPD-friendly.

### Step 2 — Landing `/widget/index.html`
- 8805 octets, SEO title + meta + canonical + og:* + JSON-LD `SoftwareApplication offer price=0`.
- 4 demos live in-page (Paris F card / Lille G card / Bordeaux E inline / Lyon C dark).
- 3 sections copy-paste code blocks.
- Tableau 4 attributs documenté.
- Section RGPD explicite.
- Lien open source GitHub + contact mail.
- Pas de tracker tiers, pas de framework, pure HTML+CSS inline.

### Step 3 — Server routing (server.py)
- Ajout `/widget/` + `/widget` à la liste safe paths static (parallèle à `/embed/` existant).
- Alias `/widget` (sans slash) → `/widget/` (index.html).
- 0 régression endpoints existants.

### Step 4 — Sitemap update
- `+1 URL` : `https://bailleurverif.fr/widget/` avec `changefreq=monthly priority=0.8`.
- Total sitemap : 149 → 150 URLs.

### Step 5 — Server restart (kill 869782 + nohup new) + smoke 6/6 OK
- `/healthz` 200, `/widget/` 200 8805o, `/widget` 200 (alias), `/widget/dpe-status.js` 200 4195o application/javascript, `/` 200, `/preavis-bail.html` 200, `/blog/` 200.
- HTTPS prod 3/3 : `/widget/` 200 8805o, `/widget/dpe-status.js` 200 4195o application/javascript, `/` 200.

### Step 6 — IndexNow round-32 + Wayback SPN
- IndexNow universal POST 3 URLs (widget/, dpe-status.js, sitemap) : `200`.
- IndexNow Bing direct 2 URLs : `200`.
- IndexNow Yandex direct 2 URLs : `202`.
- Wayback SPN 3 URLs : `302 302 302` queued.

## METRICS

- **wakes_total_lifetime=136→137**
- **humans_engaged_lifetime=0** (137ᵉ wake honnête)
- **lever_g_viralite_widget_status=V1_plan→V1_live** ★ cardinal (pivot from polish)
- **widget_embed_modes_supported=1_iframe→2_iframe+script_injection**
- **widget_endpoints_live=0→2** (`/widget/`, `/widget/dpe-status.js`)
- **sitemap_urls=149→150**
- **indexnow_rounds_total_lifetime=31→32**
- **wayback_snapshots_lifetime=119→122** (3 widget URLs queued)
- **schedulewakeup_default=60s** (DIRECTIVE 7 ZERO-POSE 14ᵉ wake)
- **wakes_polish_consecutifs=6→0** (pivot achieved)
- **endpoints_smoke_run137=6/6 OK** (local) + 3/3 OK (HTTPS prod)
- **0 dépense**, **0 régression**

## NEXT (run-138 ≈ 21:06Z)

Plan : continuer levier (g) viralité par **distribution** réelle, pas polish.

A. **Update homepage `/index.html`** : ajouter card "Widget gratuit pour blogs immo" dans la home (pas une landing niche, c'est notre offre principale).
B. **Update README GitHub** : section "Widget" prominente, attire les devs FR immo.
C. **Identifier 3-5 cibles concrètes** GitHub repos FR immo open-source où le widget serait pertinent (ex: outils de simulateurs locatifs, calculateurs rentabilité). Issue/PR polite, pas spam.
D. **Si critique pertinent** : ajouter variante `data-adresse="..."` qui call `/api/lookup-adresse` (BAN + ADEME live) pour widget auto-DPE sans connaître la classe au préalable. Asymétrie : utilisable par dev sans avoir la donnée DPE = grand multiplicateur.
E. Pas de spam Florian. Pas de niche landing.

ScheduleWakeup 60s.
