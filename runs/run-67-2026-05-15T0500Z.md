# Run-67 — 2026-05-15T05:00Z (07:00 Paris)

## Contexte

Wake +15min après run-66 (26e reload runbook initial Florian consécutif). 07:00 Paris = **T-60min avant fenêtre POST-002** (cible ≥06:00Z UTC = 08h Paris). Healthcheck OK :

- Wedge 8102 stats : 11/4/1/0/0/0/0 (34e wake inchangé)
- Mastodon `@bailleurverif` API publique : 0 followers / 0 following / 1 statuses (POST-001 seul)
- POST-001 T+~549min = 0/0/0 (33e mesure stable)
- 10 TODO Florian OPEN inchangés
- Inbox silencieuse depuis 02:05Z (~175min)

**Encore trop tôt pour POST-002.** Plutôt qu'un 3e wake consécutif de maintenance pure (run-65 compaction ledger / run-66 dédup state), action concrète **utile et réutilisable** : créer l'orchestrateur Browserbase manquant.

## Décision

**Créer `agent-browser/post_via_bb.sh`** — wrapper qui encapsule le pattern à 3 étapes utilisé manuellement à chaque wake critique BB (run-29/31/32/40) :
1. `POST /v1/sessions` (avec contextId.persist=true pour préserver cookies Mastodon)
2. lancement `mastodon_post.py CONNECT_URL SESSION_ID draft_path`
3. `POST /v1/sessions/{id}` `status=REQUEST_RELEASE`

**Justifications** :
- Florian DIRECTIVE 4 §4 (automatisation de soi-même) explicite : "Y a-t-il des tâches que tu refais à chaque wake et qui pourraient devenir un script ?"
- 4 occurrences de ce pattern dans le ledger (run-29 smoke, run-31 healthcheck, run-32 POST-001, run-40 profil), 100% manuel curl + parse + python à chaque fois
- Sous pression POST-002 (fenêtre étroite), 1 commande vs 13 lignes manuelles = réduit chance d'erreur
- Coût : ~10min écriture + syntax check, 0 € (pas de session BB consommée pour le test)
- Réversible (script unique, suppressible, indépendant des scripts Python existants)

## Actions

### 1. Vérification pré-vol POST-002

- `wc -c drafts/POST-002.txt` : **471 chars** (sous limite 500 Mastodon) ✅
- `cat drafts/POST-002.txt` : 4 hashtags `#immobilier #bailleur #logement #Paris` (patchés run-65 sur la base de l'audit J-1 des 10 hashtags candidats) ✅
- `head mastodon_post.py` : signature `argv[1]=CONNECT_URL argv[2]=SESSION_ID argv[3]=draft_path` confirmée (paramétrable run-34) ✅
- `.env` : `BROWSERBASE_API_KEY`, `BROWSERBASE_PROJECT_ID`, `BROWSERBASE_CONTEXT_ID` tous présents ✅
- `which jq python3 curl` : tous installés (jq-1.7) ✅

### 2. Création `agent-browser/post_via_bb.sh` (~75 lignes)

Spec :
- argv[1] = chemin draft (obligatoire, vérifie existence)
- Source `.env` strict (`: "${VAR:?...}"`)
- POST API Browserbase avec `jq -nc` pour générer JSON proprement (anti injection)
- Capture SESSION_ID + CONNECT_URL depuis réponse JSON
- `set +e` autour de mastodon_post.py pour ne pas perdre la release session si le post échoue
- Release session **toujours** (même sur échec post — release est cheap, oublier = facture continue côté BB)
- Log JSON final dans `logs/bb-orchestration-{ts}.json`
- `set -euo pipefail` + `--max-time` sur curl

### 3. Validation syntax-only

```
bash -n agent-browser/post_via_bb.sh && echo SYNTAX_OK
```

→ `SYNTAX_OK` ✅

**Pas de dry-run réel** (consommerait une session BB ~1-2 min budget). Le script reposera sur des API et un script déjà éprouvés au run-40. Si problème au wake suivant : fallback = exécuter les 3 étapes manuellement comme avant.

## Verify

- 1 nouveau fichier : `agent-browser/post_via_bb.sh` (executable, syntax OK)
- 0 fichier draft créé, 0 article ajouté, 0 module Python nouveau
- 0 TODO Florian créé
- 1 fichier `runs/run-67-2026-05-15T0500Z.md` (rituel)
- 3 fichiers d'état édités (ledger.md append + state.md header + tasks.md)
- Conforme engagement run-55 ✅ (automatisation infra ≠ stock content)

## Métriques

- `bb_orchestration_script_present` : false → **true** (NEW)
- `mastodon_post_pre_flight_done` : false → true (NEW pour POST-002)
- `wakes_consecutifs_research_active` : 24 → 24 (rompu run-65, maintenu)
- `wakes_sans_budget_bb_consomme_consecutifs` : 26 → 27 (record)
- `mastodon_post001_engagement_T549min` : 0/0/0 (33e mesure stable)
- `wedge_stats` : 11/4/1/0/0/0/0 inchangé 34e wake
- `florian_todos_open` : 10 inchangé
- `runbook_initial_reloads_consecutifs` : 25 → 26
- `inbox_silence_duration_min` : 160 → 175
- `bb_session_create_pattern_manual_repetitions` : 4 (run-29/31/32/40) → désormais encapsulé

## Next

- **ScheduleWakeup 3600s** → wake ~06:00Z UTC = **pile la fenêtre POST-002** (cible ≥06:00Z UTC = 08h Paris).
- **Action run-68 attendue** : EXÉCUTER POST-002 via 1 commande :
  ```
  cd /home/deploy/saas-florian && agent-browser/post_via_bb.sh agent-browser/drafts/POST-002.txt
  ```
  Estimation ~2-3 min wall (vs ~5 min manuel run-32). Budget BB consommé ~14min cumulés sur 3000.
- **Hypothèse à mesurer** post-publish : ratio fav/vues vs POST-001 (cible x3-x10 grâce profil corrigé run-40 + hashtags actifs run-65).
- **Fallback** si le script échoue : 3 étapes manuelles (curl create / python post / curl release) comme au run-40.
- Si wake run-68 est trop tôt (avant 06:00Z UTC) → attendre 1 wake supplémentaire, pas de POST.
