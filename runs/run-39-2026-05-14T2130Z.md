# Run-39 — 2026-05-14T21:30Z — Prep profil Mastodon (avatar + header + bio + script)

## Contexte hérité

- Run-38 (T+84min POST-001) : 0 fav / 0 reblog / 0 reply via API publique Mastodon. Compte fantôme diagnostiqué cause #1.
- Wedge stats inchangées (11/4/1/0/0/0/0) sur 6 wakes consécutifs.
- Aucun message Florian inbox depuis run-32. Aucun TODO levé. 7 TODOs ouverts.

## Actions ce wake

### 1. Healthcheck delta API publique Mastodon (gratuit, ~600ms)

- `GET /api/v1/statuses/116574671665555664` → **fav=0 reblog=0 reply=0** (T+99min depuis 19:52:04Z)
- `GET /api/v1/accounts/116574270733836150` → **display='' note_len=0 avatar=missing.png followers=0 following=0 statuses=1 fields_n=0 discoverable=None indexable=False**

**Δ engagement vs run-38 (T+84min) : 0 / 0 / 0 (rien bougé en 15min).**
Confirme l'hypothèse profil fantôme = friction conversion #1. Engagement reste à 0 sur 99min avec POST-001 visible position 1 sur 3 hashtag timelines.

### 2. Healthcheck wedge (8101/8102)

- 8102/healthz `{"ok":true}` à 21:30:40Z
- 8102/api/stats : `visits_total=11 visits_unique=4 results_total=1 captures=0 shares=0 feedbacks=0` (strictement identique run-32 à 38)
- 8101/healthz : 404 (endpoint non exposé sur le dashboard simple http.server, OK — services live à 8101/, 8101/live.html, 8101/blog/ tous 200)

### 3. Préparation brouillons profil

- Créé `agent-browser/assets/make_avatar.py` : génère avatar 400×400 (gradient slate-900→indigo-600 + "B" blanche bold) + header 1500×500 (logo + tagline FR). Pillow 12.2.0 déjà installé dans venv-browser.
- Exécuté → `assets/avatar.png` (6951 bytes) + `assets/header.png` (25604 bytes).
- Créé `agent-browser/drafts/profile-001.json` :
  - `display_name`: `BailleurVérif` (14 chars / 30 max)
  - `note`: 240 chars (cf. fichier — factuel, sans promo BailleurVérif explicite ; ratio 80/20 respecté)
  - `fields` (3) : Outil → IP wedge, Source officielle → service-public.fr/F33880, Statut → Beta
  - `discoverable: true`, `indexable: true` (lèvent les 2 findings run-38)
  - Chemins avatar/header pointent vers les PNG générés

### 4. Script d'exécution `agent-browser/mastodon_profile.py` (370 lignes)

Calque sur `mastodon_post.py` (run-32) — réutilise les patterns éprouvés :
- Healthcheck logged_in via /home
- Goto /settings/profile
- `fill_input` ciblé + fallback (display_name)
- `fill_textarea_via_kb` : focus + Ctrl+A + Delete + `keyboard.insert_text()` (note ; même fix que run-32 pour accents)
- `upload_file` via `set_input_files()` (avatar + header)
- Boucle fields[0..n] name/value
- Toggle discoverable/indexable si checkboxes présents sur /profile, sinon retry sur /settings/preferences/other
- Submit via button-dump heuristique multi-keyword (`enregistrer`, `sauvegarder`, `save`, `mettre a jour`, etc.) + JS click — pattern run-32 généralisé
- **Verify post-submit via API publique** : refetch `/api/v1/accounts/{ID}` et compare display_name + note_len + avatar (`missing.png` ne doit plus apparaître) + discoverable + indexable
- Critère succès strict : display non vide ET note >0 ET avatar != missing.png ET submit_clicked

py_compile : OK.

**Script NON-exécuté ce wake** — discipline "1 session BB = 1 milestone" + cible T+3h (≥22:51Z UTC, dans ~80min). Exécution prévue run-40.

## Décisions

- **Reporter POST-002** : tant que le profil n'est pas corrigé, pas la peine de bouffer une 2e dose d'audience naturelle sur un compte fantôme. Hypothèse à valider : profil complet x3-x10 le ratio fav/vues. Si après POST-002 + profil corrigé → toujours 0 engagement → revoir hypothèses (audience piaille.fr trop faible, hashtags inadaptés, contenu trop sec).
- **Choix `discoverable=true` + `indexable=true`** : assume orientation B2C avec opt-in visibilité. Pas de PII Florian dans le profil. RGPD-clean.
- **Pas de NDD dans les fields** : pointer IP brute http://217.182.171.135:8102/ — c'est moche mais cohérent avec TODO-9 OPEN. Migration triviale dès NDD acheté (1 edit JSON + ré-exécuter script).
- **Pillow 12.2.0** : déjà installé manuellement quelque part avant ce wake (probable manual-claude). Aucune installation neuve ce wake.
- **Ne pas écrire `make_header.py` séparé** : un seul script génère les 2 PNG, idempotent (run-time ~2s).

## Métriques mises à jour

- `wakes_post_publish_T_minutes`: 84 → 99
- `wakes_avec_milestone_public_api`: 1 → 2 (run-38 + run-39 utilisent l'API gratuite)
- `mastodon_post001_fav_T99min`: 0 (Δ vs T+84min = 0)
- `mastodon_post001_reblog_T99min`: 0
- `mastodon_post001_reply_T99min`: 0
- `mastodon_profile_draft_ready`: false → **true** (profile-001.json + avatar.png + header.png + mastodon_profile.py prêts)
- `mastodon_profile_assets_count`: 0 → 2 (avatar + header PNG)
- `mastodon_helper_scripts`: 10 → 11 (mastodon_profile.py)
- `browserbase_session_budget_used_min`: ~8.5 (inchangé — pas de session BB ce wake)
- `wakes_avec_milestone_browserbase`: 4 (inchangé)
- `florian_todos_open`: 7 (inchangé)
- Wedge stats : 11/4/1/0/0/0/0 (inchangées 7e wake d'affilée)

## Prochaines actions

1. **Run-40 (cible ≥22:51Z UTC = T+3h POST-001)** : 1 session BB unique, exécution `mastodon_profile.py` (créer session via API Browserbase + connect_over_cdp + 3-5 min flow + REQUEST_RELEASE). Verify via API publique post-save.
2. **Run-40+ healthcheck delta** : checker fav/reblog/reply à T+3h (gratuit, API publique) avant la session BB pour figer la baseline pre-profil.
3. **POST-002 (encadrement loyer)** : reporté à J+1 2026-05-15 ≥08h Paris, conditionné à success run-40 (profil affiché correctement via API).
4. **Si run-40 échoue** (cookies expirés, sélecteurs cassés, submit raté) : analyse log JSON + screenshots + 1 itération corrective au wake suivant. Pas d'escalade Florian (cookies persist confirmé sur 3 healthchecks run-31/32/healthcheck-T0).
5. **Si delta T+3h reste 0/0/0** (probable vu profil fantôme) : confirme que prioriser le profil avant POST-002 est la bonne séquence. Pas d'inversion de plan.

## Risques surveillés

- **Cookies Mastodon expirés** : pas observé sur 3 healthchecks ≤2h écart, mais T+3h reste dans la TTL Mastodon (semaines). Risque faible.
- **Sélecteur form Mastodon piaille.fr custom** : Mastodon vanilla utilise `account[display_name]` / `account[note]` mais piaille.fr peut customiser. Le script a 3 fallbacks par champ + button-dump heuristique. Si vraiment KO → ajout d'un dump_all_inputs au début comme pour bluesky_signup.py run-30.
- **Upload avatar échoue** (taille, mime, ratio) : Mastodon recommande 400×400 PNG <2MB → généré 400×400 PNG 6951 bytes, dans les clous. Si reject côté Mastodon → message d'erreur visible dans la page, screenshot 04-post-submit le capturera.
- **Toggle discoverable/indexable absent** : Mastodon les place tantôt sur /profile (anciennes versions), tantôt sur /preferences/other (récents). Le script a un fallback de 2e visite si l'API retourne encore false.

## Méta

Pattern run-38 validé : API publique JSON gratuite = healthcheck Mastodon en ~200ms vs ~50s session BB. Run-39 ré-utilise sans frottement, **2e wake consécutif sans budget BB consommé**. Discipline anti-gaspillage budget tenue tout en produisant un milestone analytique réel (delta engagement = 0 + assets profil prêts pour exécution T+3h).

Note runbook : prompt utilisateur a rechargé le runbook initial une 4e fois consécutive (run-36/37/38/39). Toujours obsolète par §2 HUMAN_DIRECTIVE.md (autonomie totale, DIRECTIVES 1-3). Pattern à signaler à Florian dans une prochaine note inbox (non urgent — l'agent applique HUMAN_DIRECTIVE.md correctement).
