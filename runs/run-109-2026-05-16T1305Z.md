# Run-109 — 2026-05-16T13:05Z — Form signup étendu aux 2 wedges (homepage + preavis)

## CONTEXT
14e wake exécutif mission B2C 5000 users / 90j. Wake +15 min après run-108 (DIRECTIVE 5 pacing respecté, ~13:05Z vs 12:50Z). Florian silence depuis 09:40Z. Plan run-108 NEXT branche (A) : maximiser surface signup en répliquant la mécanique sur les 2 autres pages produits (homepage wedge + preavis-bail.html). Tokens, KPI, et endpoint déjà LIVE depuis run-108 — il ne manquait que les points d'entrée.

## ASSESS
- 1 page sur 90 portait le form (`/locataire-loyer-legal.html`, topic `veille-reglementaire`).
- Pages les plus visitées potentiellement = `/` (quiz wedge bailleur) et `/preavis-bail.html` (tool #2).
- Endpoint `/api/subscribe` accepte 4 topics : `loyer-legal, dpe-bailleur, preavis, veille-reglementaire`. Audience match :
  - `/` (BailleurVérif quiz encadrement) → topic `loyer-legal` (bailleurs vérifiant leur loyer).
  - `/preavis-bail.html` (calculateur préavis) → topic `preavis`.
  - (note : plan run-108 mentionnait `dpe-bailleur` pour homepage mais homepage = wedge encadrement loyer, pas DPE — j'ai retenu `loyer-legal` qui colle au contenu. Topic `dpe-bailleur` sera activé quand on étendra aux 50 pages DPE F/G.)
- Risque collision IDs : 3 pages exposent maintenant `#bv-subscribe-form` — pas un problème, chaque page chargée isolément, scripts scopés via IIFE.

## PLAN
1. Patch `static/preavis-bail.html` : insérer aside `#alerte-maj` + form + script entre la liste « Outils complémentaires » et le footer. Style Tailwind utility (cohérent avec locataire-loyer-legal.html).
2. Patch `static/index.html` : insérer aside `#alerte-maj` + form + script entre `#guides` et `#about`. Style `glass` + CSS vars (cohérent palette light service-public.fr posée run-101).
3. Vérif HTTP 200 + grep `alerte-maj`.
4. Smoke E2E 2 topics nouveaux (loyer-legal depuis `/`, preavis depuis `/preavis-bail.html`) + confirm round-trip + negative paths (consent manquant, bad topic, bad email).
5. Purge entrées smoke → baseline 0/0/0/0.
6. IndexNow round-14 (2 URLs : `/` et `/preavis-bail.html`).
7. Update state.md + ledger.md + run report + inbox.md + tasks.md.

## ACT

### 1. Patch `preavis-bail.html` (+~88 lignes)
- Aside `#alerte-maj` (mêmes styles que locataire) entre `</ul>` Outils complémentaires et `<footer>`.
- Copy adapté : « Être prévenu si les règles de préavis évoluent » + mention Loi Climat / motifs réduits / jurisprudence cassation.
- Form : `<input type="hidden" name="topic" value="preavis">` + source `/preavis-bail.html`.
- Script IIFE : POST `/api/subscribe`, gère 200/already/429/400/network, affiche lien confirm inline (workaround SMTP TODO-20).
- Consent checkbox obligatoire + lien `/politique-confidentialite.html`.

### 2. Patch `index.html` (+~85 lignes)
- Aside `#alerte-maj` `class="glass rounded-2xl"` (cohérent style homepage) entre `#guides` et `#about`.
- Copy adapté : « Bailleur ? Soyez prévenu si l'encadrement change » + mention décret / prolongation / cassation / arrêtés préfectoraux.
- Form : `<input type="hidden" name="topic" value="loyer-legal">` + source `/`.
- Style boutons / inputs : reprend les CSS vars `var(--accent)`, `var(--bg)`, `var(--text-secondary)` pour cohérence avec `#email-gate` et `#watch-gate` existants.
- Script IIFE indépendant — pas d'interférence avec `captureEmail()` existant qui hit `/api/capture` (mécanisme parallèle, audience différente : `#email-gate` post-quiz, `#bv-subscribe-form` toujours visible).

### 3. Vérif live
- `curl /` 200 / 27 457 bytes (vs ~25 kB avant, +~2.4 kB).
- `curl /preavis-bail.html` 200 / 36 093 bytes (vs ~32 kB avant, +~4 kB).
- `grep alerte-maj /` = 1 (présent), `grep alerte-maj /preavis-bail.html` = 1 (présent). Pages static lues à chaud par le serveur Python à chaque requête (pas de cache build), aucun restart nécessaire.

### 4. Smoke E2E HTTPS — 7/7 OK
| # | Cas | Résultat |
|---|---|---|
| 1 | POST `/api/subscribe` topic=loyer-legal source=/ consent=true | 200 `{ok, confirm_url, unsubscribe_url}` ✅ |
| 2 | POST `/api/subscribe` topic=preavis source=/preavis-bail.html consent=true | 200 `{ok, confirm_url, unsubscribe_url}` ✅ |
| 3 | `/api/stats` → subscribers_pending=2 | ✅ |
| 4 | GET `/api/confirm?token=A` (loyer-legal) | 200 HTML 1647 bytes ✅ |
| 5 | GET `/api/confirm?token=B` (preavis) | 200 HTML 1643 bytes ✅ |
| 6 | `/api/stats` → confirmed=2, signups_24h=2 | ✅ KPI live |
| 7 | Negative : consent missing 400, bad topic `{error:"invalid topic"}`, bad email `{error:"invalid email"}` | ✅ |

### 5. Purge smoke
3 entrées (1 subscribe loyer-legal + 1 subscribe preavis + 2 confirm tokens correspondants + 1 entrée smoke-final résiduelle) → `data/subscribers.jsonl` vidé à 0 lignes. `/api/stats` → 0/0/0/0. Baseline clean.

### 6. IndexNow round-14
2 URLs (les pages modifiées) :
- api.indexnow.org : HTTP 200 ✅
- www.bing.com : HTTP 200 ✅
- yandex.com : HTTP 202 ✅

## VERIFY
- `grep id="alerte-maj"` sur les 3 pages tenant (locataire, preavis, index) = 1 chacune.
- `grep bv-subscribe-form` = 6 occurrences total (2 par page : form + script reference).
- `grep value="loyer-legal"` sur `/` = 1 (topic homepage).
- `grep value="preavis"` sur `/preavis-bail.html` = 1 (topic preavis).
- `grep value="veille-reglementaire"` sur `/locataire-loyer-legal.html` = 1 (topic préservé run-108).
- `/api/stats` baseline 0/0/0/0 post-purge.
- Healthz 200.
- 0 régression : autres endpoints `/api/visit`, `/api/capture`, `/api/result`, `/api/share`, `/api/feedback`, `/api/embed/*` non touchés.
- 0 dépendance externe ajoutée (vanilla JS, pas de CDN), 0 tracker, 0 cookie tiers.
- DIRECTIVE 6 light theme : preavis-bail.html déjà light (run-101), index.html déjà light (run-101), aside ajoutées avec palette cohérente.

## METRIC
| Métrique | Avant | Après | Note |
|---|---|---|---|
| `pages_with_signup_form_live` | 1 | **3** | `/locataire-loyer-legal.html` + `/` + `/preavis-bail.html` |
| `signup_form_topics_live` | 1 | **3** | veille-reglementaire + loyer-legal + preavis (dpe-bailleur reste latent pour DPE pages) |
| `endpoints_api_count` | 12 | **12** | inchangé (réutilisation des routes run-108) |
| `signup_mechanism_live` | true | **true** | inchangé (déjà déclenché run-108) |
| `indexnow_rounds_lifetime` | 13 | **14** | +1 |
| `urls_soumises_indexnow_lifetime` | ~120 | **~122** | +2 URLs |
| `leviers_cyclés_nouvelle_mission` | a,c,d,e,f,g,h+méta-D6 | **idem (+e re-cyclé 2e activation)** | levier e étendu à 2 nouvelles surfaces |
| `wakes_executifs_nouvelle_mission` | 13 | **14** | |
| `humans_engaged_lifetime` | 0 | **0** | 109e wake honnête, mais surface signup multipliée par 3 |
| `subscribers_baseline_post_purge` | 0/0/0/0 | **0/0/0/0** | clean |
| Fichiers modifiés | n/a | 2 | `static/index.html` + `static/preavis-bail.html` |
| Fichiers créés | n/a | 1 | run report |
| Dépenses | 0 | **0** | |
| Régressions | 0 | **0** | |
| Nouvelles pages | 0 | **0** | gel run-103 respecté (extension form sur existantes ≠ nouvelle page) |

## NEXT
Run-110 candidats par priorité :
- **(A) levier (a) SEO programmatique + (e) optim conversion** : étendre form aux 50 pages DPE F/G via patch `build_dpe_pages.py` (topic `dpe-bailleur`), re-build idempotent, sitemap update. 50 surfaces signup d'un coup, c'est le ROI structurel max. **Probable run-110.**
- **(B) levier (d) outreach warm** : si Florian ré-active toujours pas Bluesky/GSC, tester accès Reddit via browser-bridge → 1 commentaire utile sur r/ImmobilierFrance / r/vosfinances avec lien wedge (1 vrai humain = preuve concept, débloque stagnation 109-wakes).
- **(C) levier (h) content authority + (a)** : ajouter form aux 6 articles blog (topic `veille-reglementaire`), volume + audience guides = lecteurs déjà engagés.
- **(D) refresh sitemap.xml** : ajouter `lastmod` aux 2 pages modifiées, soumettre via IndexNow + GSC quand débloqué.

Branche (A) probable car ROI massif (50 surfaces vs 6) et auto-portable via le builder existant.

ScheduleWakeup 180s DIRECTIVE 5.
