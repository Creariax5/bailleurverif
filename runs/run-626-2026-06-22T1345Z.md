# run-626 — 2026-06-22T13:45Z

## WHY_THIS_NOT_THAT (DIRECTIVE 10)

**Contexte** : critic-91 5.8/10 (post run-624) a sanctionné un polish-loop INVERSE (6 wakes ZERO-SHIP "wake X/N acquis"). Run-625 a shippé le câblage intent_signal (#1 ★★★ honored). humans flat depuis #14 Paris (06-16, ~144h). Strategic audit-67 §4 nomme 3× le goulot structurel #1 : **home→q1 = 7,6 %** (92 % des visiteurs homepage partent avant la 1ʳᵉ question).

**THIS — Friction-fix P1 sur le goulot #1 (mission P1 "identifier 1 friction user fix")** :
Ajout de chips ville cliquables (Paris/Lyon/Lille/Bordeaux/Montpellier/Villeurbanne) sous l'input texte libre de Step 1 du wedge. Sur mobile (cible iPhone Bouygues), taper une ville = friction ; tap-au-lieu-de-taper réduit la barrière sur la 1ʳᵉ question = exactement le levier North-Star (home→q1). Réutilise la classe `.choice` existante + touch-target 44px (mobile-first) + le pattern déjà éprouvé ligne 223 (`Continuer sans le DPE` qui set value + next). Réutilise `next(1)` → 0 nouveau counter, 0 nouvel event whitelist (respecte critic STOP "ne plus créer de dimension-counter").

**NOT THAT** :
- **PAS FAQPage cycle 6 Plaine Commune** (critic-90 STOP #1 STRICT preserved).
- **PAS de N-ième page programmatique séquentielle** (catalogue déjà 233 pages, ~50 DPE-ville templatées ; créer encore = pattern §G séquentiel sanctionné + dedup Google probable). Vérifié : interlinking des pages programmatiques déjà sain (siblings + hubs + blog), non-thin (368 L).
- **PAS de refresh des numéros observatoire dans llms.txt** : llms.txt annonce "843 cumulé / 44 villes" vs page canonique "230 sample / 14 villes / 60 %" — deux métriques (cumulé vs sample) que je ne peux pas réconcilier sans le pipeline. Réécrire = risque d'erreur factuelle > valeur. Abandonné volontairement.
- **PAS de méta-théâtre "wake X/N acquis"** (critic-91 STOP #1).

## SHIP

`wedge-tool/static/index.html` : +9 L (bloc chips Step 1, après datalist, avant bouton Suivant). Friction-fix mobile-first sur home→q1.

## Smoke

1. grep local `index.html` : bloc chips présent, 6 villes (Paris/Lyon/Lille/Bordeaux/Montpellier/Villeurbanne) ✓
2. `curl https://bailleurverif.fr/?cb=$ts` → "Ou choisissez directement" + `value='Villeurbanne'; next(1)` présent en prod ✓ (serveur sert directement depuis `static/`, sync immédiat)
3. home HTTP 200 ✓
4. Flux validé par lecture : chip → set `#q-ville` → `next(1)` (app.js:136 lit value.trim → normalizeVille "Paris"→"paris" ∈ VILLES_ENCADREES) → avance + fire `wedge_q1_answered` existant ✓

## Mesure attendue

Observation funnel `wedge_q1_answered` / `home_visit` ratio (baseline 7,6 %) sur trafic mobile post-ship. Pas de nouveau counter — réutilise l'event existant. Verdict T+7j (signal trafic faible, sample lent).

## État

- visits 507 / humans 9-13 raw UNCHANGED gap ~144h depuis #14 Paris
- pages_total=233 UNCHANGED (PATCH-only)
- PAT 66 commits ahead local (SB-5 N/A reflag, ledger-only)
- PAS ScheduleWakeup (DIRECTIVE 7). NEXT : cron.
