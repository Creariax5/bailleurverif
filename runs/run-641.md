# run-641 — 2026-06-23T22:00Z — Default (a) EXÉCUTÉ : instrument test acquisition mesurable (STOP refresh-boucle)

## WHY_THIS_NOT_THAT

**Contexte** : Florian n'a PAS répondu au gate a/b/c (FYI 20:00Z HEAD inbox). NEXT run-640 littéral = « STOP refresh-boucle ; si Florian gate → exécuter ; sinon défaut (a) snapshot GSC 1 page + deadline mesure 30j ». Critic-94 #3 ★ = « si la thèse est data-unique→sort-du-dedup gsc~8, instrumente le test (gsc avant/après, deadline). Sinon arrête de cadrer l'intégrité comme acquisition ». Les 2 convergent → **défaut (a)**.

**Action choisie** : transformer la thèse mission P2 (non-prouvée, 5 wakes supply-side 636-640 sans feedback) en **test FALSIFIABLE**, au lieu d'un 6ᵉ enrichissement aveugle.

**Découverte clé** : expérience naturelle déjà live, 0 nouvelle page requise —
- **ENRICHED N=13** city-pages avec bloc observatoire data UNIQUE (aubervilliers, bordeaux, echirolles, ile-saint-denis, lille, lyon, marseille, montpellier, montreuil, paris, pierrefitte-sur-seine, saint-denis, villeurbanne).
- **THIN N=20** quasi-dupliquées template, 0 data unique (bagnolet, bobigny, bondy, epinay, eybens, fontaine, grenoble, hellemmes, la-courneuve, le-pre-saint-gervais, les-lilas, lomme, noisy-le-sec, pantin, romainville, saint-martin-d-heres, saint-ouen, stains, villetaneuse).
C'est l'A/B exact de « data-unique sort du dedup » : si ENRICHED s'indexe ≥+25pt vs THIN à T+30j (2026-07-23) → thèse VALIDÉE ; si delta <+10pt → FALSIFIÉE → pivot canal distribution (option b).

**Signal mesuré** : `urlInspection.index.inspect` → `coverageState` (source canonique Google, pas proxy `site:` scrapé).

**Livré** :
1. `agent-browser/gsc_inspect.py` (114L) — batch 2 cohortes, append `gsc-dedup-test.jsonl`, aggrégat indexed_rate par cohorte + delta. Syntaxe OK, atteint l'API (403-disabled gracieux per-URL en batch).
2. `memory-agent/kpis/gsc-dedup-test.md` — design + critère décision + blocker.
3. `florian-todos.md` TODO-39 ★★ — activer Search Console API projet GCP 897971836052 (1 clic).

**Blocker unique** : Search Console API désactivée sur projet 897971836052. SA `bailleurverif-indexing` déjà owner GSC (siteverification+indexing OK) mais API searchconsole off. **Tentative auto-activation via SA = 403** (`serviceusage.services.enable` permission denied, attendu pour SA indexing-narrow). ⇒ 1-clic Florian (TODO-39), puis script 100 % autonome (baseline + T+30j sans humain).

**NOT** : 6ᵉ enrichissement supply-side aveugle (critic STOP#3, défaut (a) explicite) / NOT nouvelle page (expérience naturelle suffit) / NOT proxy `site:` scrapé (non-fiable, Cloudflare/captcha) / NOT cadrer intégrité=acquisition (critic STOP#1) / NOT 4ᵉ fix conversion (critic-92/94 STOP, 0 funnel) / NOT escalade récurrente PAT (SB-5) / NOT ScheduleWakeup.

## Vérif
- `ast.parse` OK / run single-url atteint API (403 disabled = preuve auth+endpoint corrects, blocker = activation pas code).
- Auto-activation SA = 403 documenté (pas de faux espoir autonome).
- Cohortes ENRICHED/THIN dérivées du live (`grep observatoire + N=annonces` sur `encadrement-loyer-*-2026.html`).

## KPIs
visits ~548 / humans 9-13 raw (7-9 conf-adj) gap ~7j Poisson UNCHANGED / pages 233 UNCHANGED (instrument≠page) / subscribers 1 pending / moat cat-1 UNCHANGED / tactical_cumul=112 (critic-94 #3 ★ honored J+0, #1+#2 déjà run-640) / strategic_cumul=65/65 UNCHANGED / PAT mort SB-5 ledger-only / PAS ScheduleWakeup.

## NEXT cron 2026-06-24T00:00Z
Si TODO-39 activé par Florian → run `gsc_inspect.py` baseline + log + lire delta initial. Sinon : Villeurbanne N=21→38 stale (2ᵉ rang intégrité, recalc CSV avant) OU mesure `recourse_viewed` T+7j. NE PAS empiler supply-side aveugle tant que test non-instrumenté en prod (gate critic STOP#3).
