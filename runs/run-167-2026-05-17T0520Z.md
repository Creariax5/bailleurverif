# run-167 — 2026-05-17T05:20Z — Traceability ship : UA logging server.py (pivot honnête depuis scrape Leboncoin bloqué)

43ᵉ wake DIRECTIVE 7 ZERO-POSE. **8ᵉ wake critic-compliance consécutif** (0 nouvelle page HTML). Florian silent 2h06 post-escalade A/B run-160 → défaut (A) STOP construction + distribution autonome tient.

## Plan initial → pivot

Plan annoncé run-166 NEXT incluait : audit scanner V1.2 sur 10 annonces **réelles** Leboncoin (alimentation default A : "audit scanner 10 annonces Leboncoin réelles").

**Réalité empirique 3 tentatives WebFetch** :
- `leboncoin.fr/recherche?category=10&locations=Marseille` → **HTTP 403** (Datadome anti-bot + robots.txt /recherche Disallow)
- `pap.fr/annonces/locations-paris-g439` → **HTTP 403** (Cloudflare)
- `cybermalveillance.gouv.fr/...arnaques-location...` → **HTTP 404** sur 3 chemins candidats (page d'index générale OK, mais pas de section "arnaques location" canonique)

**Conclusion** : scraping anti-bot rentals FR impraticable depuis ce runtime sans Browserbase. Décision : pivot vers une action infra à **vraie valeur autonome**.

## Action substantive shipping : log_message X-Forwarded-For + UA

### Diagnostic
Inspection `wedge-tool/server.log` : tous les hits récents loggent IP `172.22.0.3` (= docker-proxy interne) et **0 User-Agent**. Impossible d'identifier bots vs humains côté serveur. La fonction `_client_ip()` existe (ligne 432) mais n'est utilisée que pour les API endpoints (/api/subscribe, /api/scan-annonce, etc.), pas pour le log d'accès général.

### Patch (server.py:403-411)
```python
def log_message(self, fmt, *args):
    try:
        xff = self.headers.get("X-Forwarded-For", "") if getattr(self, "headers", None) else ""
        client_ip = xff.split(",")[0].strip() if xff else self.client_address[0]
        ua = (self.headers.get("User-Agent", "") if getattr(self, "headers", None) else "")[:120]
    except Exception:
        client_ip = self.client_address[0]
        ua = ""
    sys.stdout.write("[%s] %s - %s ua=%r\n" % (now_iso(), client_ip, fmt % args, ua))
    sys.stdout.flush()
```

9 lignes nettes. Defensive vs malformed requests (try/except + getattr). UA tronquée 120 chars (vs 200 pour /api/embed/view) pour log léger.

### Validation
- `python3 -c "import ast; ast.parse(open('server.py').read())"` → syntax OK
- `kill 1082755` (ancien PID) → `nohup python3 server.py &` → PID 1099792 05:19:54Z cwd=wedge-tool
- `curl -A "Mozilla/5.0 (compatible; SmokeTestBot/1.0)" https://bailleurverif.fr/robots.txt` → log line :
  ```
  [2026-05-17T05:20:27+00:00] 217.182.171.135 - "GET /robots.txt HTTP/1.1" 200 - ua='Mozilla/5.0 (compatible; SmokeTestBot/1.0)'
  ```
- `curl http://127.0.0.1:8102/healthz` → log line :
  ```
  [2026-05-17T05:20:27+00:00] 127.0.0.1 - "GET /healthz HTTP/1.1" 200 - ua='curl/8.5.0'
  ```

Real client IP correctement résolu via X-Forwarded-For (217.182.171.135 = ma propre IP outbound) ; UA correctement loggé.

## KPIs delta

- `log_message_patch=shipped` ★
- `log_format_v2=ip_xff+ua120` ★
- `server_pid=1082755→1099792` (restart clean)
- `real_listing_scrape_attempts=3` (leboncoin/pap/cybermalveillance) — **0 succès** (Cloudflare/anti-bot bloque sans browserbase)
- `pages_total_live=115 maintenu` ★ 8ᵉ wake 0 construction
- `wakes_construction_consecutifs=0 maintenu`
- `wakes_total_lifetime=166→167`
- `wakes_executifs_nouvelle_mission=67→68`
- `humans_engaged_lifetime=2 maintenu`
- `schedulewakeup_default=60s` (43ᵉ wake compliance)
- 0 dépense, 0 régression, 0 PII, 0 IndexNow stérile, 0 nouvelle page HTML

## Différentiateur

Avant : si Googlebot crawle 100 pages dans la nuit, 100 lignes dans server.log avec IP=172.22.0.3 UA=∅ → impossible distinguer du trafic interne ou outils smoke. **Aucune mesure crawl empirique possible sans GSC.**

Après : `grep -iE "googlebot|bingbot|yandex|claudebot|gptbot|petalbot|applebot" server.log | wc -l` donne le crawl count empirique réel. **Indépendance GSC partielle débloquée** (GSC reste cardinal pour rendering JS + query reports mais le crawl headcount devient mesurable côté serveur).

## Honnêteté

- 0 humain nouveau ce wake. Le patch ne crée pas d'utilisateurs, il améliore la **mesure** d'acquisition. Mais sans mesure, pas de pilotage.
- Plan initial "audit scanner sur 10 annonces réelles" reporté faute d'accès. Reste 2 voies pour le faire : (i) Florian provisionne Browserbase ou (ii) je teste sur annonces archivées Wayback (latence + qualité dégradée).
- Server restart = 8 secondes downtime (kill → 2s sleep → start). Tolérable (0 user en attente). 0 régression smoke.
- Le patch logue **toutes** les requêtes y compris bots inutiles → server.log va croître plus vite (~+30% rough). Prévoir rotation log logrotate dans 1-2 jours (TODO interne, pas Florian).
- Si traefik ne propage pas X-Forwarded-For correctement pour tous les paths, le fallback `client_address[0]` reste juste = pas de régression silencieuse, juste log dégradé sur ces requêtes.

## Couverture leviers (cycle continu)

- (e) optim conversion → instrumentation côté serveur (mesurabilité = prérequis pour A/B)
- (f) veille → sera utilisé pour mesurer crawl rate des AI bots (ClaudeBot / GPTBot / PerplexityBot) — proxy GEO ranking

## Next run-168 (60s)

(A) inbox check Florian (43ᵉ wake STOP-list, proba +30% matin FR ~07Z dans ~1h45)
(B) Si silence :
  (i) **1ʳᵉ mesure empirique bot crawl 24h** via `grep -iE "(google|bing|yandex|claude|gpt|petal|apple|perplexity|cc|chatgpt)bot|crawler" server.log` (cumul depuis restart 05:19Z = baseline T+0, mesure à T+1h, T+24h pour rate trend)
  (ii) **Mini-audit scanner V1.2 sur annonces archivées Wayback** : alternative au scrape live bloqué. Récupérer 3-5 archive web.archive.org de listings PAP/Leboncoin (mois antérieurs) pour stress test ground-truth dégradé. Ou écrire dans `florian-todos.md` TODO-25 demande explicite Browserbase scrape via lui.
  (iii) **Discovery test SearchBots additionnels** : tester si Yep.com et Brave Search ont submission form web sans login.
(C) Discipline 0 nouvelle page HTML 9ᵉ wake maintenue.
