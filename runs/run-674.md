# run-674 — 2026-06-26T~21:00Z — Réconciliation sitemap↔prune (tactical critic-100 #1)

## WHY_THIS_NOT_THAT (DIRECTIVE 10)

**Choisi** : retirer les 44 URLs 301-redirigées de `sitemap-dpe.xml` (+ `sitemap-all.xml` orphelin).

**Pourquoi ça et pas autre chose** :
- Tactical critic audit-100 (8.5/10, post run-673) a posé **#1 priorité explicite** : `sitemap-dpe.xml` liste encore les 44 URLs metz/caen/… passées en 301 par run-672, lastmod stale 05-16 = « demi-mesure qui rate les 2 bénéfices » (réallocation crawl-budget + signal de-thin). Honorer un flag critique #1 J+0 = ROI le plus haut du wake + maintient streak honored.
- **PAS** P1/P2 inventés : critic #3 + bans brief Florian disent STOP burst au-delà des 5 P0 (« retour gated, pas d'invention »). Burst P0 fini run-673.
- **PAS** recourse gate maintenant : gate-temps = 06-30 (critic #3), pas ce wake.
- **PAS** toucher home/scan-url/share-card (ban brief, 0 signal nouveau).
- **PAS** bumper lastmod→06-26 (l'autre option critic) : aurait exigé un trigger-retrait à gérer (dette) + les pages DPE-ville keep n'ont pas changé matériellement (les REFRESH 06-23/24 portaient sur `encadrement-loyer-*`, pas `*-dpe-f-g-interdit`) ⇒ bump = lastmod malhonnête. **Retirer** = option propre sans dette.

## Action

Script déterministe : retire toute ligne `<loc>` dont le path ∈ clés `redirects-301.json` (44 keys).
- `static/sitemap-dpe.xml` : 55→11 loc (Paris/Lyon/Toulouse/Nantes/Bordeaux/Lille DPE keep + blog dpe-f-location + dpe-fiabilite + calendrier hub + 2 `/api/recourse/dpe-invalide`).
- `static/sitemap-all.xml` (orphelin, hors sitemapindex, legacy) : 195→151 loc.
- Sitemaps = URLs canoniques 200 uniquement (guideline Google : ne pas lister de 301).

## Smoke 6/6 (prod live)

1. `sitemap.xml` index → 200
2. `sitemap-dpe.xml` redirected-listed → **0** (était 44)
3. `metz-dpe-f-g-interdit-location.html` → 301 → calendrier hub
4. `paris-dpe-f-g-interdit-location.html` (keep) → 200
5. `calendrier-interdiction-dpe-2025-2028-2034.html` hub → 200
6. `sitemap-all.xml` redirected-listed → **0**

static-à-chaud (server.py pid 122363 sert le disque ; prod affichait déjà 11 loc avant push). Commit `0150741` pushé origin/main.

## Mesure / expectations

- **T+14j 07-10 (critic #2)** : cette réconcil. + P0-1/2/3/5 n'adressent que causes #3/#4/#5 du diagnostic GSC (thin / internal-linking / engagement). Attendre mouvement **MODESTE** `detected_not_indexed` 177→~150. NE PAS sur-attribuer : levier dominant = **externe** (trust domaine sandbox <120j + backlinks sous-pages ≈0), mécanisme Florian/awesome-PRs (TODO-39/40), pas Builder-interne.
- recourse_letter_copied gate = **06-30** (critic #3) : attester viewed 2 / copied 0 ce wake-là, puis stop.

## KPIs

humans 9-13 (7-9 conf-adj) UNCHANGED ~255h depuis Paris #14 06-16 / visits ~564 / pages 233 / subscribers 1 / moat 8 / `tactical_critic_recommendations_honored_cumul` 116→117 / `strategic` 66/66.

## NEXT

Gate recourse 06-30 (attester viewed/copied) ; T+72h sweep brief check 06-29 avant toute escalade ; TODO-39 (GSC API/resubmit sub-sitemaps) + TODO-40 (relances awesome PRs) = leviers Florian ; SB-6 verify-and-stop si rien neuf.
