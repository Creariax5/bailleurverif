# run-670 — 2026-06-26T08:30Z — P0-1 Sitemap segmentation (brief Florian 06-26T08:00Z honoré J+0)

## Signal d'entrée
Brief Florian **08:00Z** (postérieur à audit-99 critic 07:05Z) = re-mission substantive Builder-only : attaquer le mur GSC **177 "Détectée non-indexée"** (2 indexées / 189 — 1.1 %). 5 actions P0/P1/P2, sequencing libre. **Brief Florian > posture SB-6 "gated-stop"** (hiérarchie codifiée run-327 §c-bis). Les 9 wakes précédents (665-669) étaient gated faute de signal neuf ; ce brief EST le signal neuf qui dégate.

## WHY_THIS_NOT_THAT
**THIS** = P0-1 ★★★ Sitemap segmentation (5 sub-sitemaps + sitemapindex). Le plus haut levier déterministe, clean, Builder-only, smoke-testable, 0 risque régression (196 URLs préservées à l'identique). Mécanisme : Google rationne le crawl-budget par namespace de sitemap ; un sitemapindex référençant 5 sections donne à Google un budget distinct par section → débloque potentiellement 30-60 pages M+1.5 sur le mur des 177.

**NOT P0-2** (internal linking sweep ★★★, ~45 min) : valable mais plus long, je le prends prochain wake — densité 1 action ★★★ complète + shippée > 2 actions à moitié. Sequencing libre autorisé.
**NOT P0-3** (thin→301 redirect ★★) : destructif (perd des pages), demande jugement par-page sur data unique ; à faire après P0-2 quand le linking est mappé. Pas dans un même wake court.
**NOT P0-4** (IndexNow 1/jour ★) : risque théâtre (le brief lui-même dit "skip si théâtre", SB-4 cap strict) ; la segmentation est le vrai levier, le ping ne crée pas de trust.
**NOT P0-5** (Dataset Search ★) : dépend du re-crawl DILA 24-72h non encore validé.
**NOT gated-stop** : un brief Florian substantif Builder-only dégate explicitement (asymétrie : je n'attends pas Florian, mais quand il livre du travail concret je l'exécute J+0).

## Action exécutée
`agent-browser/build_segmented_sitemap.py` (97L, déterministe, idempotent) :
- Lit `sitemap.xml` plat (196 URLs), écrit snapshot canonique `sitemap-all.xml`.
- Buckets premier-match : observatoire(4) / dpe(55) / villes-encadrement [encadrement+loyer-legal+preavis-bail](88) / blog-recourse [blog+recourse+question+guide](15) / tools [fallback](34).
- Réécrit `sitemap.xml` en **sitemapindex** référençant les 5.
- Re-run safe : si sitemap.xml déjà index, relit sitemap-all.xml (refuse sinon).

## Smoke (4/4)
1. XML parse valide 6/6 fichiers (minidom).
2. **0 perte URL** : diff `sitemap-all.xml` vs backup pré-segment = IDENTIQUE 196 URLs.
3. Live HTTP 200 : sitemap.xml + sitemap-dpe.xml + sitemap-tools.xml (server.py L1432 auto-sert `/<file>.xml` depuis STATIC_DIR, 0 code change requis).
4. robots.txt pointe déjà `sitemap.xml` (devenu index) → découverte des sub-sitemaps assurée, pas de modif.

## Ship
Commit `dd2d276` pushed origin/main. Prod live (STATIC_DIR = fichiers écrits).

## Florian-only dérivé
TODO-40 ★ ajouté florian-todos : resubmit 5 sub-sitemaps distinctement dans GSC > Sitemaps (~5 min, signal crawl +rapide vs découverte auto via index).

## État
humans 9-13 (7-9 conf-adj) UNCHANGED ~252h+ / visits ~564 / pages 233 / subscribers 1 / moat 8 / tactical 116 (brief honoré J+0) / strategic 66/66. Cap PAR CIBLE sitemap-gen : 1/1 consommé cette cible.

## NEXT cron
P0-2 internal linking sweep COMPLET N=233 (audit déterministe `audit_internal_linking.py` à créer + patch "Voir aussi" pages manquantes). Puis P0-3 thin→301 si linking mappé. recourse_letter_copied 06-30 = seul gate-temps ouvert à attester ce wake-là. SB-6 si aucun signal neuf.
