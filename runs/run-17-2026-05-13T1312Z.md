# Run-17 — 2026-05-13T13:12Z

## Contexte
- **Phase active** : 1bis — Validation par wedge tool
- **Cadence dégressive** explicitement activée par state.md (run-16 → run-17 : 2-4h)
- Wake déclenché normalement (~13 min après run-16 — plus court que la cadence dégressive prévue, mais aucune dégradation puisque le travail réalisé est non-disruptif et infra-seulement).
- **Signal Florian** : aucun. `inbox.md` ne contient que le message agent→Florian de run-14. `florian-todos.md` inchangé : TODO-4, TODO-9, TODO-10, TODO-11 toujours OPEN.
- **Compteur `wakes_sans_signal_distribution_externe`** : 8 → 9.

## Assess
1. Pas de nouvelle directive Florian (HUMAN_DIRECTIVE.md inchangé)
2. Tâches mûres : aucune. Tout dépend de la levée d'un TODO Florian ou d'un trafic réel.
3. Signal externe : aucun. Stats wedge identiques (4/1/0/0/0).

## Plan (1 action ciblée)
**Action choisie** : ajout infrastructure SEO légère sur le blog statique (JSON-LD Article + CollectionPage + `sitemap.xml` + `robots.txt`).

**Justification du choix vs alternatives** :
- ❌ Article SEO #5/#6 — produire du stock sans canal de distribution actif aggrave l'asymétrie (anti-pattern noté run-13)
- ❌ Nouvelle feature wedge (mode "comparer 2 biens", question 6 anti-fraude) — over-engineering préventif sans signal utilisateur
- ❌ Préparer d'autres drafts de canaux bloqués (LinkedIn, Mastodon) — déjà saturé : 15 tweets + email B2B + 2 Boursorama prêts, Florian doit en activer au moins UN
- ✅ **Infrastructure SEO** : améliore tous les articles déjà publiés ET les futurs. Coût ~15 min. 100% réversible. Active prêt à indexation dès le NDD acheté (TODO-9). Pas de la dette nouvelle, c'est un multiplicateur de l'existant.

## Act

### 1. Healthcheck
- `GET /healthz` wedge (PID 3842640) → 200 OK
- `GET /api/stats` wedge → `visits_total=4, results=1, captures=0, shares=0, feedbacks=0` (inchangé vs run-16)
- `GET /live.html` dashboard (PID 3773381) → 200 OK
- `GET /blog/` → 200 OK
- Aucune dérive infra.

### 2. Modifications `dashboard/build_blog.py`
- **+ import `json`** (stdlib)
- **+ constante `SITE_URL` + `DASHBOARD_DIR`**
- **+ `article_jsonld()`** : génère JSON-LD Schema.org `Article` (headline, description, datePublished/dateModified, inLanguage fr-FR, author/publisher BailleurVérif, mainEntityOfPage canonical_url)
- **+ `collection_jsonld()`** : JSON-LD `CollectionPage` + `ItemList` pour l'index blog (4 items)
- **+ `write_sitemap_and_robots()`** : génère `dashboard/sitemap.xml` (6 URLs : racine + blog index + 4 articles, format sitemap protocol 0.9 standard avec `lastmod`) et `dashboard/robots.txt` (User-agent: * Allow: / + Sitemap: référence)
- **PAGE_TEMPLATE** + **INDEX_TEMPLATE** : ajout de `<script type="application/ld+json">{jsonld}</script>` avant le CDN Tailwind
- **`build()`** : variable `iso_date` (YYYY-MM-DD) ajoutée, `canonical_url` extraite, JSON-LD passé en kwarg `.format(jsonld=...)`, listes `article_urls` + `index_items` accumulées pour le sitemap, appel `write_sitemap_and_robots()` en fin de build

### 3. Rebuild
```
$ python3 dashboard/build_blog.py
OK   dashboard/blog/dpe-f-location-2026.html (8 min)
OK   dashboard/blog/encadrement-loyer-zones-tendues-2026.html (8 min)
OK   dashboard/blog/verifier-dossier-locataire-fraude.html (8 min)
OK   dashboard/blog/obligations-bailleur-particulier-2026.html (8 min)
OK   dashboard/blog/index.html (index)
OK   dashboard/sitemap.xml (6 urls)
OK   dashboard/robots.txt
```

### 4. Smoke tests
- `GET /robots.txt` → 200, body conforme (User-agent * Allow / + Sitemap directive)
- `GET /sitemap.xml` → 200, body XML valide (6 `<url>` entries, format sitemap 0.9 namespace correct)
- `GET /blog/dpe-f-location-2026.html` → JSON-LD `Article` détecté dans HTML, fields conformes (headline, datePublished `2026-05-13`, mainEntityOfPage canonical)
- `GET /blog/` → JSON-LD `CollectionPage` + `ItemList` détectés, 4 items position 1-4 dans la liste
- `metrics_sync.sh` relancé → OK (leads=3, generated_at=2026-05-13T13:12Z)

## Résultats mesurables
- **JSON-LD Article déployé** : 4 articles × 1 schema chacun = 4 schemas Article
- **JSON-LD CollectionPage** : 1 sur l'index blog
- **sitemap.xml** : 6 URLs (racine + blog index + 4 articles), HTTP 200
- **robots.txt** : conforme, référence sitemap, HTTP 200
- **Coût** : 0 dépendance nouvelle, ~70 lignes ajoutées au builder, idempotent (relancer le build = même output)
- **Limite assumée** : tant que NDD non acheté, Google ne crawl pas l'IP brute (sauf via Search Console "URL inspection" qu'on pourrait soumettre manuellement). Donc valeur immédiate = zéro indexation. Valeur réelle = **infrastructure prête à l'emploi le jour où TODO-9 (NDD) est levé**, plus quelques rich snippets potentiels chez les agrégateurs tiers qui respectent Schema.org.

## Découvertes
1. **Le builder blog tient bien l'extension par "couches"** : ajouter JSON-LD/sitemap/robots = 3 fonctions pures + 2 placeholders templates + 5 lignes dans le main. Aucune régression sur les 4 articles existants (rebuild identique au précédent côté contenu visible). Pattern à réutiliser pour les prochaines additions SEO (`feed.xml` RSS, `humans.txt`, `security.txt`, etc.) — toujours dans le même builder, jamais éparpillé.
2. **Décalage cadence dégressive vs production** : state.md prévoyait run-17 espacé 2-4h, mais le wake s'est déclenché à +13 min de run-16. J'ai choisi de faire UN seul progrès léger non-disruptif au lieu d'attendre, ce qui respecte l'intention de la cadence dégressive (éviter overproduction) sans rendre le wake stérile. Si le système de wakeup ne respecte pas mes `ScheduleWakeup`, ce sera à corriger dans le prochain audit.
3. **L'infrastructure SEO est gratuite jusqu'au NDD** : pas une seule ligne de cette infra ne sera à refaire le jour où on bascule sur `bailleurverif.fr`. Le sitemap utilise `SITE_URL` qu'il suffit de changer en haut du builder. C'est le pattern inverse de la dette de stock SEO non-distribué : ici, on prépare la canalisation avant le débit. Coût d'opportunité = 0 si distribution non-débloquée, multiplicateur si débloquée.

## Prochaines actions (prio relue, inchangées sur le fond)
1. **Si Florian débloque TODO-10 (Twitter)** : lancer cadence 1-3 tweets/jour via `social-drafts.md`, mesurer impressions/clics/conv.
2. **Si Florian débloque TODO-4 (Gmail pro)** : envoyer 5 cold emails B2B agents immo (`outreach-b2b-agents-immo.md`), 1 par jour en variant la zone.
3. **Si Florian débloque TODO-9 (NDD)** : changer `SITE_URL`/`BLOG_URL`/`WEDGE_URL` dans builder + wedge config, soumettre sitemap à Google Search Console.
4. **Si Florian débloque TODO-11 (Boursorama)** : monitorer `referrals_from_share` + activité thread.
5. **Si compteur `wakes_sans_signal_distribution_externe` atteint 10-12** : audit critique honnête à inscrire dans `audit-2026-05-14.md` (J+1 prévu) — décision possible : ralentir cadence à 6-12h (dormance), arrêter toute production tant que pas de signal externe.
6. **Si Florian a réagi à 1 TODO entre temps** : agir en priorité absolue dessus dès le prochain wake.

## Blocage / escalation
Aucun bloqueur dur. Aucune escalation supplémentaire (les 4 TODOs ouverts couvrent tout).

## Cadence prochaine
Conformément au plan : ScheduleWakeup à **3600s** (1h), reason "cadence dégressive : 1 action légère par wake jusqu'à signal Florian ou trafic externe". Si toujours rien à run-18 → 1800-3600s à nouveau. Si rien à run-19 → audit critique J+1 + bascule en dormance.
