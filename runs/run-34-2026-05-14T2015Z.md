# Run-34 — 2026-05-14T20:15Z — REFACTOR `mastodon_post.py` PARAMÉTRABLE

## TL;DR

T+24 min après POST-001 (publié 19:51Z). Trop tôt pour healthcheck Mastodon (cible T+3h = 22:51Z). Wedge stats inchangées (11/4/1/0/0/0/0). Aucun message Florian, aucun TODO levé. Au lieu de dormance minimaliste (run-33 pattern), je refactor `mastodon_post.py` pour accepter le draft en argv[3] — NEXT déclaré run-32, ligne 66 tasks.md. Prep concrète pour POST-002 J+1 (encadrement loyer, 460 chars). Pas de session Browserbase consommée.

## Contexte chargé

- `HUMAN_DIRECTIVE.md` : DIRECTIVE 3 (browser automation, focus distribution, 5 posts/jour max Mastodon). Toujours valide.
- `state.md` : run-33 archive intacte. POST-001 = signal courant. Cookies BB Context Mastodon persistants confirmés.
- `tasks.md` : ligne 66 explicitement "Réutiliser `mastodon_post.py` (refactor mineur : paramètre POST_CONTENT en arg)". Run-32 NEXT idem.
- `ledger.md` (50 derniers) : run-32 = POST-001 publié. Run-33 = dormance min (1 entrée 20:01Z).
- `florian-todos.md` : 7 OPEN (TODO-3, 4, 5, 9, 10, 11, 13, 14, 3-bis). Aucun mouvement.
- `inbox.md` : dernier message agent→Florian = 19:51Z (annonce POST-001). Rien de Florian.
- `incidents.md` : 3 entrées (Reddit IP, Bluesky hCaptcha, Mastodon LIVE). Pas de nouveau.

## Pourquoi ce refactor maintenant (pas plus tard)

1. **Coût marginal nul** : édit local, pas de session Browserbase, pas de risque réseau, pas d'engagement public.
2. **Friction levée pour POST-002 demain** : sans refactor, je devrais éditer `mastodon_post.py` à chaud demain en plein workflow Browserbase. Avec, c'est juste un argv différent.
3. **Pattern réutilisable** : `bluesky_post.py` (futur, déblocage TODO-14) calquera la même interface. Et tout `mastodon_reply.py` éventuel.
4. **Pas du stock spéculatif** : c'est de la préparation d'une action *déjà planifiée pour T+12-24h*, pas une feature inventée pour le futur.
5. **DIRECTIVE 3 dit "focus distribution"** : ce refactor sert directement la cadence Mastodon (5/jour max, ratio 80/20). Pas du SEO stock.

## Action réalisée

### 1. `mastodon_post.py` paramétrable

**Avant** : `POST_CONTENT` hardcodé (POST-001). Pour POST-002 il fallait éditer le fichier en place.

**Après** :
- `sys.argv[3]` = chemin du draft (optionnel, fallback `drafts/POST-001.txt`)
- `POST_CONTENT = DRAFT_PATH.read_text("utf-8").rstrip("\n")`
- `DRAFT_ID = DRAFT_PATH.stem` (e.g. `POST-002`)
- `VERIFY_NEEDLE = 1ère ligne non-vide tronquée 60 chars` (dérivé)
- Guard `POST_LEN > 500` (limite Mastodon piaille.fr)
- Guard `not DRAFT_PATH.exists()` (exit 2 avec JSON error)
- Log + screenshots préfixés par `DRAFT_ID` (`mastodon-post-POST-002-20260515TXXXXXXZ.json` etc.) → traçabilité multi-posts

### 2. Drafts exécutables

- `agent-browser/drafts/POST-001.txt` (430 chars) — copié depuis `mastodon-drafts.md` pour rétrocompatibilité fallback.
- `agent-browser/drafts/POST-002.txt` (460 chars) — encadrement loyer 31 communes, validation len + preview OK.

### 3. Validation

- `python3 -m py_compile agent-browser/mastodon_post.py` → OK
- Preview content POST-002 : len 460 chars (sous 500), needle = "Encadrement des loyers en 2026 : 31 communes appliquent déso"

## Ce qui n'a PAS été fait (volontairement)

- ❌ Pas de session Browserbase. T+24min trop tôt pour healthcheck POST-001 (impressions = ~0 garanties, bruit + budget). Cible T+3h+ = 22:51Z.
- ❌ Pas de helper `fill_form.py` (formaliser le pattern button-dump+JS click). Discipline run-32 : attendre 3+ plateformes (= déblocage TODO-14 Bluesky). Aujourd'hui 1 seule plateforme.
- ❌ Pas de production SEO (article #6). DIRECTIVE 3 + anti-stock confirmé.
- ❌ Pas de touch `bluesky_signup.py` (TODO-14 Florian, déprio).
- ❌ Pas de message Florian inbox (rien d'urgent à dire — refactor de plomberie, pas un incident).

## Découvertes / signaux

- **Pattern "argv[3] = chemin fichier draft"** : interface minimale, lisible, déboguable (cat le fichier = lire le post). Préférable à un système de templating + variables d'env qui obscurcirait le contenu réel posté.
- **Le `mastodon-drafts.md` reste source de vérité humaine** (table de tracking + commentaires), les `drafts/*.txt` sont l'export machine-exécutable. Pas de duplication structurelle : `drafts/*.txt` n'inclut que les posts effectivement prêts à exécuter.
- **Compteur `wakes_sans_signal_distribution_externe` reste à 0** (POST-001 = signal courant, pas régressé).
- **Compteur `wakes_avec_milestone_browserbase` inchangé à 4** : refactor offline ne consomme pas la quota BB (50h/mois). Justifie cette discipline.

## Métriques mises à jour

| Metric | Avant | Après | Δ |
|---|---|---|---|
| mastodon_drafts_executable | — | 2 | nouveau (POST-001, POST-002) |
| mastodon_helper_scripts | 10 | 10 | inchangé (refactor in-place) |
| production_nouvelle_run | (cumul) | +1 | refactor + 2 drafts files |
| browserbase_session_budget_used_min | 8.5 | 8.5 | inchangé (pas de session) |
| wakes_avec_milestone_browserbase | 4 | 4 | inchangé |
| wakes_sans_signal_distribution_externe | 0 | 0 | inchangé (POST-001 reste signal) |

## Prochaines actions (priorité)

1. **Run-35 (cible ≥22:51Z UTC, T+3h POST-001)** : healthcheck rapide. Soit étendre `mastodon_healthcheck.py` avec un mode "fetch post metrics" (URL `/116574671665555664` + scrape boosts/favourites/replies via DOM/`og:` meta), soit ajouter `mastodon_metrics.py` séparé. Estimé ~45s session BB. Si réactions = 0 mais pas de shadowban suspect (post existe encore, profile compose OK) → GO POST-002. Si profile fermé / post 404 → incident.
2. **Run-36 (J+1 2026-05-15, ≥08h Paris)** : poster POST-002 via `python3 agent-browser/mastodon_post.py <connect> <session> agent-browser/drafts/POST-002.txt`. Espacement minimum 12-18h depuis POST-001 (anti-bot profile neuf).
3. **Run-36+ si Florian active TODO-14** : créer `bluesky_post.py` calqué sur `mastodon_post.py` (même interface argv[3] = path + drafts/*.txt) → 2e canal autonome.
4. **Audit J+1 wedge** : prévu 2026-05-15 (intégrer 1er post Mastodon + metrics impressions si dispo).
5. **Article SEO #6 DPE 2026 facteur 1.9** : NE PAS produire avant signal réaction Mastodon (anti-stock confirmé).

## Blocages

Aucun nouveau. TODO-13/14/3-bis/9 restent côté Florian (déprio depuis Mastodon live).

## Cadence prochaine

`ScheduleWakeup(3600)` par convention (probablement clamp ou ignoré). Fenêtre cible run-35 = 22:51Z+ UTC. Si Florian relance avant : healthcheck léger + dormance min jusqu'à T+3h.
