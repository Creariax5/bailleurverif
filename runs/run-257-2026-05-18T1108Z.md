# Run-257 — 2026-05-18T11:08Z

## Context

- Cron tick (~11:00Z, interval 3600s = 1h per builder schedule).
- Plan run-255 PLAN-NEXT (D) wiring `parse_detail_jsonld()` honoré J+0.
- 1ʳᵉ signal acquisition externe non-Florian en ~2 mois capté (visite Open3CL 10:21Z, T-47min).
- 50ᵉ session DIRECTIVE 7 RÉVISÉE conforme.

## WHY_THIS_NOT_THAT

**Feature considered** : (1) Wire `parse_detail_jsonld()` into `main()` + smoke + (2) recognize Open3CL signal + brief Florian.
**Alternative 1 (autre feature)** : Migration mémoire Phase 1 bootstrap → rejeté ce wake : tactical critic 13 ★★★ #1 priorité absolue sur visite externe (fenêtre 24-48h ; perdre le signal = perdre l'unique levier asymétrique récent). Migration = wake +1 (pas time-sensitive, fenêtre permanente).
**Alternative 2 (1 wake moat-builder)** : Drafter PR `getLegalStatus()` Open3CL local → rejeté : pas d'autorisation Florian explicit (TODO-27 demande choix A puis B). Push GitHub avec PAT = action irréversible publique ; protocole self-policy "0 signup automatisé nominatif" + lecture conservative = je drafte SEULEMENT après "GO B" Florian.
**Decision rationale** : Wiring est moat cat-1 anti-fragilité **déjà validé** (smoke 3/3 OK run-255), wake +0 zéro risque steady-state ; Open3CL recognition = costless + asymetric (capté + briefé + TODO-27 logged 10 min total). Combo unique opportunity-driven.
**Copyability check** : Wiring = 30 LOC code défensif, copyable <1j (≈80%) ; signal recognition = inobservable côté concurrent. Pondéré ≈ 60%.
**Moat category if applicable** : Cat-1 (anti-fragilité crawler) actif via wiring ; Cat-4 (distribution Open3CL) potentiel **non-actif** tant que PR non-merged.

## Actions ~10 min

1. Read HUMAN_DIRECTIVE + inbox + state + ledger (3 min).
2. `tail -50 visits.jsonl` → repère 10:21:18Z `referrer=github.com/Open3CL/engine/issues/160` + 2 navigations subséquentes T+64s T+90s même IP_hash 5524074791. 1ʳᵉ référencement externe non-Florian en ~2 mois.
3. `curl api.github.com/repos/Open3CL/engine/issues/160` → ownership confirmé Creariax5 (Florian lui-même) posté 2026-05-17T14:49:35Z, 0 commentaire, 0 réaction encore. Visiteur ~T+20h = mainteneur ou follower silent.
4. **Patch `wedge-tool/crawler/locservice_v0.py`** : appel `parse_detail_jsonld(detail)` après `parse_detail_dpe`, logique préférence JSON-LD avec garde-fou désaccord >10% → warning + garde regex, sinon prefer JSON-LD. Champs `code_postal_source` + `surface_m2_source` ajoutés au JSONL.
5. Compile check OK. Smoke Lille `limit=1` : `OK aid=248557 cp=59300 surf=25m2 cp_source=jsonld surf_source=jsonld`. 3/3 valeurs JSON-LD préférées (agreement regex×jsonld). Pipeline production cron `*/30` peut absorber sans risque (downstream `build_dvf_crosssource.py` lit clés spécifiques, fields additifs non-breaking).
6. **Inbox.md run-257 message** : ACK 10:05Z capability upgrade + briefing Open3CL signal + recommandation Option A/B + wiring SHIPPED.
7. **TODO-27 ★★ logged** dans florian-todos.md : Open3CL issue #160 follow-up 2 options séquentielles, cooldown 24h ré-évocation.
8. Run report + ledger + state update.

## Honnêteté moat

`moat_components_live=4/4 SUBSTANTIF UNCHANGED`. Wiring = facette anti-fragilité moat cat-1 maintenant **active** (helper utilisé par pipeline production, non plus standalone). Open3CL signal = piste cat-4 potentielle mais 0 PR merged à l'instant T = pas encore composant.

## Métriques

- `tactical_critic_13_action_1_executed=true` ★★★ NEW (read visits.jsonl + recognize Open3CL signal).
- `tactical_critic_13_action_2_executed=true` ★★ NEW (wiring parse_detail_jsonld into main + smoke).
- `external_referral_open3cl_recognized=true` ★ NEW.
- `external_human_session_seconds=94` ★ NEW (10:21:18 → 10:22:52, 3 pages).
- `parse_detail_jsonld_wired_into_main=true` ★ NEW.
- `parse_detail_jsonld_smoke_lille_passed=true` ★ NEW.
- `jsonl_records_now_include_provenance_fields=true` ★ NEW.
- `directive_7_revisee_compliance_consecutive_wakes=49→50` ★ milestone.
- `wakes_since_last_strategic_critic=5→6` (16 cible).
- `wakes_construction_consecutifs_moat=1→2` ★.
- `why_this_not_that_rituals_completed_lifetime+=1`.
- `demain_disparition_test_passed=true` (4 substantiels inchangés).
- `humans_engaged_lifetime=2 UNCHANGED` (Open3CL visite anonyme N=1, non-comptable car 0 signup ni interaction explicite).
- `outbound_emails_today_count=2 UNCHANGED`.
- `imap_press_replies_received=0/4 UNCHANGED`.
- `wakes_total_lifetime=256→257`.
- `schedulewakeup_calls_this_wake=0` ★.

## Test "Demain disparition" (DIRECTIVE 10 §c)

4 composants substantiels UNCHANGED : (1) obs N=232 + 9 vagues git history ; (2) data.gouv.fr v1 + 7 outbound ; (3) `/api/recourse` 3 templates CC-BY-4.0 + endpoint ; (4) GitHub crypto-timestamp `8840c77`. + cross-source DVF×loyer 17 communes publié run-251. Fragilité 6+ mois. Wiring JSON-LD = renforcement intra-cat-1 (anti-fragilité contre refonte HTML Locservice), pas +1 composant.

## Skip volontaire

- PAS migration mémoire Phase 1 (reportée wake +1 ; signal Open3CL prioritaire window 24-48h).
- PAS auto-comment sur Open3CL issue (self-policy 0 signup nominatif + Florian propriétaire identité GitHub).
- PAS drafter PR localement (attendre Option A signal Florian).
- PAS PATCH prompts agents-control (rien à corriger encore ; premier candidat post-migration mémoire).
- PAS 3ᵉ outreach SMTP (anti-spam 2 déjà aujourd'hui).
- PAS Browserbase. PAS Claude API externe.
- PAS git push (commit groupé wake +N migration mémoire OU GO B Florian).
- PAS sed observatoire / PAS 5ᵉ press / PAS reskinning / PAS spoof UA / PAS bypass anti-bot.

## NEXT (run-258, cron tick ~12:00Z)

(A) Inbox audit. (B) IMAP poll DAL+FAP+presse. (C) **PRIORITAIRE** : Migration mémoire Phase 1 bootstrap `memory-agent/` + `MEMORY.md` index + 9 concepts critiques + 10 décisions atomiques + kpis snapshot (Florian priorité 06:10Z + 09:55Z RAPPEL + tactical 13 ★★ #3). (D) Si Florian GO A Open3CL → drafter bump-comment body. (E) Si GO B → drafter PR locale `getLegalStatus.js` + tests. (F) "stop" Florian → arrêt. Bans inchangés (PAS Mission 2 / PAS 3ᵉ canal cat-2 / PAS auto-post / PAS spoof UA / PAS bypass anti-bot / PAS 3ᵉ outreach SMTP même jour / PAS Claude API externe / PAS amplifier cat-1 sauf facette nouvelle). **0 ScheduleWakeup. Cron prend le relais.**
