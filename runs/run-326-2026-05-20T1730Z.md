# run-326 — 2026-05-20T17:30Z — TELEGRAM BOT DAEMON SHIPPED (brief 13:45Z 4/4 closed)

## WHY_THIS_NOT_THAT (DIRECTIVE 10)

**Pourquoi Telegram daemon CE wake, pas autre chose ?**

1ʳᵉ wake nouvelle cadence 3h (run-325 14:32Z → 17:30Z = T+2h58min écart attendu = compliance baseline `0 */3 * * *`). Densité minimum 6/8 substantifs/jour codifiée run-325 = OBLIGATION substantive. Hiérarchie ce wake :

- **1A) Brief 13:45Z step 4/4 défer run-326** : explicit `Time-box ship 1-2 wakes Builder Opus (script + service + test)`. run-325 a fait 3/4 J+0 + déféré step 4 ; ne pas closer ce wake = drift babysitting + brief incomplet visible (75% completion). **CHOISI.**
- 1B) Cycle 2 sub-linkedin-drafter check (cible 16:45Z → T+45min passé) : différé run suivant, cycle Sonnet 24h pas urgent.
- 1C) Paris page bot crawls spot-check T+20h post-ship strategic-9 : fenêtre mesure 7j ouverte, anti-touch baseline.
- 1D) Audit-27 tactical critic cible ~run-330 marge ~4 : pas encore.
- 1E) TODO-34 ★★ Florian silent 15ᵉ wake : anti-noise, pas vol décision.

**Pourquoi `urllib.request` stdlib NOT `python-telegram-bot` lib (suggéré brief) ?**
1. Zéro nouvelle dépendance pip = installation déterministe (pip install peut fail réseau, lock chain `python-telegram-bot` ≥ 20 sous-deps).
2. Telegram Bot API HTTP est trivial (sendMessage + getUpdates POST/GET) = pas besoin abstractions async.
3. 218 lignes lisibles single-file = debug + maintenance Sonnet/Haiku futures aisée.
4. Lifecycle systemd simple, pas async lib gymnastics.

**Pourquoi systemd service NOT cron NOT screen/tmux ?**
- Long-running long-polling (timeout 30s blocking) = pas cron compatible.
- systemd `Restart=on-failure` + `RestartSec=30` = robustesse au reboot VPS.
- `EnvironmentFile=.env` direct = pas de leak en `ps`.

**Pourquoi anti-PII hash chat_id ?**
Self-policy DIRECTIVE PII clair impossible. SHA256(`bv-{chat_id}`)[:16] = anonymisation suffisante audit trail sans révéler identité Telegram users.

**Pourquoi footer bailleurverif.fr 100% réponses NOT seulement sur certaines ?**
Funnel Telegram → site mesurable via `referer` server.log. Driver trafic systématique = compounding Pilier 5 (LinkedIn + autres canaux distribution). Mission revenu passif = besoin trafic site (signups + affiliés).

## Actions exécutées (3 substantives + 4 housekeeping)

1. **SHIP `agent-browser/telegram_bot.py`** — 218 lignes Python stdlib. Handlers /start /help /check /observatoire. Long-polling getUpdates timeout 30s, backoff exponential 1→60s erreur réseau.
2. **SHIP `/etc/systemd/system/bailleurverif-telegram-bot.service`** — installé via `sudo install -m 644`, daemon-reload, enable --now. Active PID 2750444 RSS 11.0M stable.
3. **TEST smoke** — `getMe` API ✅ bot identifié `@BailleurVerifBot` id 8744168793. Daemon 10s timeout pré-systemd ✅ no crash.
4. Housekeeping : decision file créé + MEMORY.md index L+1 + ledger.md SHIP+DECISION+METRIC+PLAN-NEXT + inbox.md HEAD ajouté + ce file.

## Métriques delta

- `wakes_total_lifetime` : 325 → **326**
- `directive_7_revisee_compliance_consecutive_wakes` : 105 → **106**
- `brief_13_45z_completion_pct` : 75% → **100% ★**
- `distribution_channels_active` : 4 → **5 ★** (Telegram NEW)
- `long_running_services_count` : 1 → **2** (public.service + telegram-bot.service)
- `memory_agent_decisions_count` : 29 → **30**
- `m0_consecutive_count` : 0 UNCHANGED (substantive)

## BANS respectés

- 0 ScheduleWakeup (106ᵉ session DIRECTIVE 7 conforme)
- 0 dépense
- 0 PII clair (hash chat_id SHA256)
- 0 server restart (wedge-tool inchangé)
- 0 nouvelle page HTML
- 0 IndexNow round
- 0 outbound SMTP (ANIL + Que Choisir cooldowns actifs)
- 0 spawn sub-agent (Telegram = long-running pas sub-agent)
- 0 touch Paris page + homepage (fenêtres mesure 7j ouvertes)

## PLAN-NEXT run-327 (~20:30Z cron 3h)

Hiérarchie :
1. **Florian DM bot rapport** (si entrée inbox.md HEAD) → log success + update concept distribution.
2. **Cycle 2 sub-linkedin-drafter check** (T+4h post-cible 16:45Z) → social-drafts.md HEAD nouveau draft attendu.
3. **Paris page bot crawls spot-check** (T+23h post-ship) → `grep loyer-legal-paris /var/log/wedge-tool/server.log | tail -20`.
4. **Audit-27 tactical critic** (~run-330 marge ~4) → préparer mental.
5. **Fallback M0** : nouvelle cadence 3h rend M0 plus rare, mais si triggers tous négatifs → hygiène minimale.
