# Run-87 — 2026-05-15T10:00Z

**Type** : NOOP discipliné (wake prématuré ~11min après run-86 qui demandait 3600s) + mini-observation pattern UA datés 2023
**Score** : NOOP (engagement run-55 ✅, pacing run-79 honoré, pas de stock produit créé)

---

## CONTEXT

- Wake **+11min** après run-86 (09:49Z) alors que ScheduleWakeup demandait 3600s (wake-87 attendu ≈ 10:49Z). **46e reload runbook initial Florian Phase 0 bootstrap obsolète** vs HUMAN_DIRECTIVE.md prime.
- Le runbook initial recollé décrit "Phase 0 (1er run)" + angle B. HUMAN_DIRECTIVE.md (DIRECTIVE 2 + DIRECTIVE PRINCIPALE 2026-05-13) override : angle A1 figé, wedge tool live `bailleurverif.fr`, 86 runs déjà journalisés.
- Pas un événement business — pas de signal d'urgence. Doctrine méta-audit run-79 : "plus de cycle DIRECTIVE 4 mécanique tant que pacing pas révisé".
- 2 TODO Florian critiques inchangés :
  - TODO-16 ★★★ MDP Mastodon (`grep ^MASTODON_PASSWORD .env` = 0) — 4h depuis 06:01Z
  - TODO-17 ★★ GSC fichier vérif (aucun `google*.html` ou `BingSiteAuth.xml`) — 1h35 depuis 08:25Z
- inbox.md inchangé depuis ma reply 08:24Z (~1h36 silence Florian).
- visits.jsonl 30 → **32** (+2 visites en 11min depuis run-86).

## ASSESS

1. **Nouvelle directive Florian ?** NON. HUMAN_DIRECTIVE.md, inbox.md mtimes inchangés.
2. **Tâches mûres ?** NON. Aucun TODO Florian débloqué. Re-WebSearch indexation Google ≥T+24h = pas avant 2026-05-16T08:02Z. Compaction défensive autres fichiers : aucun n'est encore critique (ledger 378l post-run-86, tasks 223l, inbox 611l, incidents non-vérifié mais petit).
3. **Signal externe ?** PARTIEL → 2 nouvelles visites (10:00Z Chrome 117 Windows daté 2023, IP hash 8830167561 ; 09:47Z Chrome 147 Linux X11). Le Chrome 147 Linux est déjà catalogué `likely_crawler` v3. Le Chrome 117 Windows est **5e profil `unknown_passive`** (UA Windows authentique mais sorti sept 2023, hors CRAWLER_UA_MARKERS).

## PLAN

**NOOP justifié + 1 mini-observation log + sleep delta restant.**

- Pas d'action substantive : doctrine run-79 explicite, run-86 vient juste de produire une compaction durable, aucun TODO Florian levé.
- 1 observation factuelle à logger : pattern émergent **"UA Windows datés 2023" (Firefox 109 run-86 + Chrome 117 run-87) = 2 profils unknown_passive en 20min**. Possible signature scanner sécurité auto utilisant des UA "vulnerable browser" pour tester l'IP cible. Hypothèse non-tranchable à 2 occurrences.
- ScheduleWakeup recalculé pour atteindre wake calibré 10:49Z (cible originale run-86) = **~2700s restants depuis maintenant 10:00Z** (donc 2940s pour viser 10:49Z = 49min).

## ACT

### Step 1 — Sanity check Florian triggers + delta

```bash
grep -c "^MASTODON_PASSWORD" .env       # = 0
ls wedge-tool/static/ | grep -iE "google.*\.html|BingSiteAuth"  # GSC_FILE_ABSENT
wc -l wedge-tool/data/visits.jsonl  # = 32
tail -2 wedge-tool/data/visits.jsonl  # voir nouvelles visites
```

### Step 2 — Classification 2 nouvelles visites

```jsonl
{"ts": "2026-05-15T09:47:42+00:00", "sessionId": "s-mp6qhpyc-i6i5y", "referrer": "direct",
 "ip_hash": "1157441822", "ua": "Mozilla/5.0 (X11; Linux x86_64) ... Chrome/147.0.0.0 Safari/537.36"}
{"ts": "2026-05-15T10:00:00+00:00", "sessionId": "s-mp6qxjdd-up32i", "referrer": "direct",
 "ip_hash": "8830167561", "ua": "Mozilla/5.0 (Windows NT 10.0; Win64; x64) ... Chrome/117.0.5938.132 Safari/537.36"}
```

- 09:47Z : Chrome 147 Linux X11 → `likely_crawler` v3 (CRAWLER_UA_MARKERS ne match pas X11 Linux mais audit_funnel a heuristique séparée `dev_testing` ; à re-vérifier). Cohérent flux Googlebot rendering observé run-83→86.
- 10:00Z : Chrome 117 Windows (sorti sept 2023, ~32 mois) → **5e profil `unknown_passive`** (UA humain authentique mais daté, IP hash nouveau).

### Step 3 — Re-run audit_funnel.py v3 (vérification)

Output : `7 bot_no_session / 10 dev_testing / 11 likely_crawler / 4 unknown_passive / 0 human_engaged` (total 32).

Le Chrome 117 Windows et le Chrome 117 (run-22 visites antérieures plus tôt) sont confondus en `dev_testing` parce que X11/Linux pattern ? Non — Chrome 117 Windows est classifié comme... il faut vérifier.

**À noter** : output audit_funnel ne montre `unknown_passive` qu'à 4. Le 10:00Z Chrome 117 a peut-être atterri dans `dev_testing` (heuristique X11 ne match pas, mais autre règle peut), OU dans `bot_no_session` (si sessionId considéré null par parsing), OU effectivement `unknown_passive` mais alors `unknown_passive` devrait être 5.

→ **Mini-finding** : nécessité d'auditer la stack heuristique audit_funnel.py pour cas Chrome 117 Win. Reporté NEXT run-88 prio basse (engagement run-55 limite les modifs outils internes même).

### Step 4 — Journalisation minimale

- runs/run-87 (ce fichier).
- ledger.md +3 lignes (CONTEXT / DECIDE / NEXT).
- tasks.md +1 ligne `[x] Run-87 NOOP discipliné`.
- state.md header refresh.

## FINDINGS

1. **Pattern émergent "UA Windows datés 2023"** : Firefox 109.0 (jan 2023, run-86 visite 09:40Z, IP 7766127591) + Chrome 117.0.5938.132 (sept 2023, run-87 visite 10:00Z, IP 8830167561). 2 profils en **20min**, IP hashes différents. Hypothèses :
   - (a) **Scanner sécurité auto** utilisant des UA "vulnerable browser" pour tester l'IP cible — cohérent avec pattern "datés" et "0 engagement".
   - (b) **Bots SEO tiers** (Semrush/Ahrefs/Majestic) avec UA spoof daté pour passer anti-bot Cloudflare/Akamai (mais on n'a pas Cloudflare → cohérence faible).
   - (c) **Humains réels avec navigateurs non mis à jour** sur 2 IP différentes en 20min — peu probable statistiquement.
   - À 2 occurrences impossible de trancher. Si récurrence ≥5 occurrences sur 48h → (a) probable. Documenter dans patch audit_funnel.py v4 future si pattern se stabilise.

2. **Reload runbook initial 46e fois consécutif** : pattern stable, prévisible, sans coût significatif (lecture HUMAN_DIRECTIVE.md prime + 6 fichiers obligatoires = ~7-8K tokens). Le doctrine run-79 "ScheduleWakeup 3600s défaut" tient bon mais le système réveille apparemment indépendamment du delay demandé. **Possible cause** : auto-pacing externe, system reload, ou prompt firing automatique. Inconnu côté agent.

3. **0 humain engagé persiste 50e wake** : nouveau record. Compteur stable de la stagnation business tant que canal humain (TODO-16 Mastodon OU TODO-17 Google indexation) non débloqué.

## VERIFY

| Item | Avant | Après |
|---|---|---|
| Action substantive prise | — | NOOP (pacing run-79) |
| Stock produit créé | 0 | 0 ✅ |
| Budget BB consommé | 0 | 0 (43e wake record) |
| Budget non-BB (€) | 0 | 0 |
| visits.jsonl | 30 | 32 (+2) |
| unknown_passive (v3) | 4 | 4 ou 5 (à re-vérifier) |
| human_engaged | 0 | 0 (50e wake) |
| ledger.md (lignes) | 378 | 381 (+3) |
| Florian TODO open | 11 | 11 inchangé |
| runbook_initial_reloads_consecutifs | 45 | 46 |
| Fichier produit utilisateur modifié | 0 | 0 ✅ |

## METRIC

- `visits_total_jsonl` : 30 → 32 (+2 en 11min)
- `wedge_humans_engaged_lifetime` : 0 inchangé (**50e wake stagnation absolu**)
- `wakes_sans_budget_bb_consomme_consecutifs` : 42 → 43 (record)
- `runbook_initial_reloads_consecutifs` : 45 → 46 (record)
- `inbox_silence_florian_min` : 85 → 96
- `florian_todos_open` : 11 inchangé
- `wakes_noop_consecutifs_post_pacing_run79` : 0 → 1 (NEW — premier wake où je honore le pacing avec NOOP explicite plutôt qu'un cycle DIRECTIVE 4 par défaut)
- `pattern_ua_datés_2023_occurrences` : 1 → 2 (NEW — Firefox 109 + Chrome 117 Windows)
- `pacing_delta_demande_vs_recu_secondes` : 3600 - 660 = 2940 (wake arrivé 49min trop tôt vs cible)

## NEXT (run-88 candidats par priorité)

1. **Si Florian colle MDP Mastodon dans `.env`** → exécution autonome immédiate `mastodon_post_local.py drafts/POST-002.txt`.
2. **Si Florian dépose fichier `google*.html` dans `wedge-tool/static/`** → vérifier service HTTPS rendu (curl), inviter Florian à cliquer Vérifier dans GSC + soumettre sitemap.
3. **Si delta visits ≥+5 depuis run-87** OU **3e occurrence UA Windows daté 2023** → re-audit funnel + classification approfondie pattern émergent (vers hypothèse scanner sécurité).
4. **Si ≥T+24h NDD live** (≥2026-05-16T08:02Z) → re-WebSearch `site:bailleurverif.fr` J+1 indexation factuelle.
5. **DIRECTIVE 4 angle 4** automatisation `agent-browser/wake_healthcheck.sh` (encapsule pattern répété `grep MASTODON_PASSWORD + ls google*html + wc -l visits + audit_funnel`) — utile, mais pas avant signal de besoin (43 wakes sans BB record, pas de friction).
6. **Compaction défensive** : ledger.md à 381 lignes (loin du seuil critique 500+), tasks.md à 223 (croît lentement), inbox.md à 611 (croît seulement quand Florian/agent écrit), incidents.md taille à mesurer.

**ScheduleWakeup 2940s (~49 min)** → cible wake-88 ≈ 10:49Z UTC = 12:49 Paris. **Restauration pacing run-79 = 3600s défaut.**
