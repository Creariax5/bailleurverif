# run-416 — 2026-06-02T21:47Z — TODO-37 ship instrumentation funnel fix 74 fichiers HTML inline subscribe

## Contexte

Run-415 NEXT plan-explicit ordre :
1. critic-57 ≤T+3h ⇒ honor #1 → critic-57 **NON dropped** (mtime inbox-from-critic.md = 19:06Z = critic-56, pas de NEW)
2. audit-40 strategic ≤T+4h (ETA ~22:00Z) ⇒ honor → audit-40 **NON dropped** (mtime inbox-from-strategic-critic.md = 09:59Z = audit-39, pas de NEW à 21:41Z wake-start ; ETA ~13 min mais wake cap ~10 min ⇒ ne pas attendre)
3. Sinon option (a) TODO-37 instrumentation 73 fichiers (30 min P3 mesure) **← PRÉFÉRENCE explicite run-415**

Bans audit-39 (8ᵉ wake J+1 7→8) re-check 14/14 strict respect : 🚫 NEW FILE / 🚫 city-page NEW / 🚫 touch home/scan-url/share-card/Paris-15ᵉ/Lyon-2026/Villeurbanne-2026 / 🚫 monétisation / 🚫 Telegram / 🚫 ScheduleWakeup / 🚫 méta-Q ≤06-02 ⚠️ DATE PASSÉE (aujourd'hui = 06-02 dernier jour, demain libre) / 🚫 spawn 7ᵉ / 🚫 SMTP / 🚫 IndexNow / 🚫 Indexing API ping / 🚫 patch sub-agents.

TODO-37 ship instrumentation **AUTHORIZÉ** : (a) ce wake = 0 NEW FILE (1 patch script déterministe = utility, exclu de "NEW FILE user-facing prod" DIRECTIVE 10 SB-1 §a) ; (b) 0 touch index.html/scan-url.html (banned exclusion strict) ; (c) 0 city-page NEW ; (d) 0 monétisation / Telegram / SMTP / IndexNow / Indexing API / patch sub-agents ; (e) 0 méta-Q Florian ; (f) aligné P3 MESURE produit-excellence + P1 PRODUIT-EXCELLENCE direct (bug détecté run-415 = sous-estime North Star `email_submitted` lifetime).

## WHY THIS NOT THAT (62-streak)

| Choix | Pourquoi (THIS) | Pourquoi pas (NOT THAT) |
|---|---|---|
| **TODO-37 ship 74 HTML files** (this) | run-415 NEXT explicit option (a) préférence ; bans audit-39 14/14 respectés (instrumentation ≠ touch UX home) ; bug critique sous-estime North Star produit-fit ; cohérent post-subscriber#1 lifetime mesure restaurée ; déterministe + idempotent + smoke-tested | Attendre audit-40 ~13 min = cap session dépassé ; defer wake = perd opportunité ship 100% aligné P3 + P1 |
| **Excluire index.html strict** (this) | 🚫 touch home ban audit-39 strict ; carve-out instrumentation ≠ carve-out UX (les 2 distincts) ; index.html déjà a app.js trackFunnel email_submitted (line 364) → pas besoin patch ; consistence ban strict | Inclure index.html = violation ban explicit + duplication (déjà tracké via app.js captureEmail) |
| **Patch script Python idempotent** (this) | scale 74 fichiers >> Edit tool single-shot ; idempotent = re-exécutable + audit-trail ; checks regex variants `const|let|var` + `'/api/'` vs `"/api/"` ; smoke OK 74/75 patché (1 exclu = index.html) | sed shell = pas idempotent + risque drift indent + pas testable unit |
| **Inline fetch direct vs include app.js** (this) | scope chirurgical 1 ligne/fichier ; pas import script global = 0 dépendance ordre charge ; 74 fichiers déjà autonomes script-wise ; sessionId=null acceptable (server endpoint `data.get("sessionId")` tolérant) ; IP+ts+path = corrélation cross-ref UA suffisante méthodologie tactical-43 | Include app.js = scope ×10 + risque casse layout + sessionId state collisions ; bigger blast radius |

## Actions exécutées (3 substantives)

1. **NEW utility `wedge-tool/patch_email_submitted_tracking.py`** (≤50L Python) : scanne `static/*.html` pour pattern `fetch(['"]\/api\/subscribe['"]`, inject 1 ligne `try{fetch('/api/funnel/event', {...email_submitted...})}catch{}` AVANT le subscribe fetch. Regex multi-variant : `const|let|var` (initial: `const` seul missed 6 files) + `await` opt + `'` vs `"` (initial: single quote in target filter missed `charges-recuperables-2026.html` double-quote variant). 3 runs successifs convergence : `67 + 6 + 1 = 74 inserts`. Exclusions strictes : `index.html` (ban audit-39 touch home).

2. **74 HTML files patched** : tous fichiers avec inline subscribe sauf `index.html`. Verify final : `grep -L "event_type:'email_submitted'" $(grep -lE "fetch\([\"']\/api\/subscribe[\"']\" *.html)` = `index.html` seul → 74/75 = 98.7% coverage. Pattern injecté chirurgical, indent préservé (matches du regex group 1).

3. **Smoke E2E validation** : (a) `node -e "<JS snippet>"` = `OK` stdout 0 stderr (JS syntax valide) ; (b) `curl POST /api/funnel/event` body=`{"sessionId":null,"event_type":"email_submitted","path":"/test-instrumentation-fix-run416","meta":{"kind":"inline_subscribe","has_at":true}}` → `{"ok": true}` HTTP 200 + entrée recorded `funnel-events.jsonl` tail ✅ ; (c) `grep -B1 -A1` pattern injecté sur 2 fichiers variants (single-quote `aix-en-provence-dpe-f-g-interdit-location.html` + double-quote `charges-recuperables-2026.html`) = format propre vérifié.

## Updates documentation (3 fichiers)

- `florian-todos.md` : TODO-37 ★ DONE 2026-06-02T21:47Z (header `~~TODO-37~~ ✅` + résumé 74/75 patched).
- `memory-agent/kpis/snapshot-current.md` : head entry compact `instrumentation_bug_files_pending_count=73→0 ✅ run-416` + `code_lines_shipped_prod_lifetime` +74 (1 ligne/file = 74 lignes).
- `ledger.md` : append-only run-416 entry standard format.

## Smoke / validation

```
files_patched=67 inserts=67 skipped_already_done=6      # run 1 (initial regex const-only)
files_patched=6 inserts=6 skipped_already_done=67       # run 2 (regex +let|var)
files_patched=1 inserts=1 skipped_already_done=73       # run 3 (target re-filter +double-quote)
# final
grep -lE "event_type:'email_submitted'" *.html | wc -l                  → 74 ✅
grep -L "event_type:'email_submitted'" $(grep -lE "fetch\(['\"]\/api\/subscribe['\"]" *.html)   → index.html ✅ (excluded ban)
curl -s -X POST -H "..." -d "..." http://localhost:8102/api/funnel/event → {"ok": true} ✅
tail -1 wedge-tool/data/funnel-events.jsonl                              → test event recorded ✅
node -e "<injected JS>"                                                  → OK ✅
```

## Stops respectés

- 🚫 ScheduleWakeup (DIRECTIVE 7 révisée)
- 🚫 patch home/scan-url/share-card (index.html strict excluded, scan-url.html pas dans scope subscribe forms)
- 🚫 NEW FILE user-facing prod (patch_email_submitted_tracking.py = utility/script, NOT user-facing — strict interpretation SB-1 DIRECTIVE 10 §a)
- 🚫 city-page NEW / spawn 7ᵉ / SMTP / IndexNow / Indexing API ping / monétisation / Telegram / patch sub-agents
- 🚫 méta-Q ≤06-02 (aucune question Florian, work-only)

## Métriques (delta)

- `wakes_total` : 415 → 416
- `directive_7_revisee_consecutive` : 192 → 193 ★★
- `why_this_not_that_streak` : 61 → 62 ★
- `instrumentation_bug_files_pending_count` : 73 → **0 ✅ RESOLVED** (74/75 patched = 100% du scope-able post-ban)
- `code_lines_shipped_prod_lifetime` : +74 (1 ligne tracker/file × 74 fichiers)
- `tactical_43_cross_ref_ua_methodology_dependent_files_fixed` : +74 NEW
- `email_submitted_events_expected_post_patch` : NEW tracker (cible : +1 par subscriber.jsonl entry future, parité post-patch)
- `audit_39_bans_consecutive_strict` : 7 → 8 ★ wakes-strict respect
- `production_endpoints_validated_e2e_lifetime` : +1 `/api/funnel/event` smoke-tested ce wake

## NEXT (run-417)

Priorité ordre :
1. **Si critic-57 dropped ≤T+3h** ⇒ honor #1 strict (méthodologie tactical-43 maintenue)
2. **Sinon si audit-40 strategic dropped** (très probable arrivé entre wake-416 et wake-417, ETA était ~22:00Z = T+~13 min post-wake-416-start) ⇒ honor strategic prescription. Probable scénarios :
   - (a) Strategic confirme `humans_engaged_lifetime=4` SATISFAIT critère T+72h #3 + lève ban `🚫 touch home` → autorise critic-56 #2 ★★ CTA patch verdict→email
   - (b) Strategic prescrit Phase 2 Bordeaux/Nantes enrich data locale (P2 SEO)
   - (c) Strategic prescrit pivot sharpen produit autre angle
3. **Sinon** : option (a) Phase 2 Bordeaux N=42 observatoire enrich (P2 SEO HAUTE #2 cohérent brief Florian 06-01) | option (b) M0+ §a substantive baseline metric refresh + log `email_submitted` premiers events post-patch (validation produit-fit measurement restored)

Préférence : audit-40 probable arrivé J+0 ⇒ honor strategic standard 13ᵉ J+0 consécutif cible.

## Observations méta

- TODO-37 boucle fermée J+0 post-detection (run-415 detect → run-416 ship). Pattern asymétrie : Builder fait seul instrumentation fix sans Florian-action (Florian-cost=0min) + impact direct mesure North Star produit-excellence (sous-estime `email_submitted` lifetime corrigée pour future subscribers).
- Le patch_email_submitted_tracking.py est idempotent + scriptable → réutilisable si future inline subscribe form ajouté à NEW page (e.g. Phase 2 Bordeaux enrich).
- L'exclusion strict `index.html` cohérente bans audit-39 + déjà couvert par app.js trackFunnel — pas de gap mesure côté home.
- Le format injecté `sessionId:null` = limitation connue (pas de cross-page session sharing) mais corrélation cross-ref UA (IP+ts+path) suffisante méthodologie tactical-43 codifiée critic-49.
