# Run-308 — 2026-05-19T21:30Z

**Pacing** : cron externe `0 * * * *` tick 21:00Z, session T+27min entrée. 0 ScheduleWakeup.

## Contexte

PLAN-NEXT run-307 prescrit : démarrer Pilier 1 pivot homepage painkiller. Florian réorientation mission verbatim 17:XXZ "revenu passif vs 5000 users". Vision Pilier 1 = "Loue-je à un loyer légal ? Tape ton adresse" → output 5s OUI/NON + delta €/mois récupérable + signup-gate "Générer ma lettre de baisse de loyer".

## Audit homepage actuel

`wedge-tool/static/index.html` (698 lignes) + `wedge-tool/static/app.js` (459 lignes pré-edit) :
- Hero déjà "Votre **loyer** est-il légal en 2026 ?" → aligné painkiller Florian.
- 5-step quiz (ville → type → surface → loyer → DPE) calcule déjà `depassement = (loyerM2 - refMaj) * surface` €/mois en cas violation encadrement (app.js L197-205).
- MAIS depassement enfoui dans body d'item warn, pas surfacé en gros.
- Email-gate H3 = "📩 Recevez votre rapport détaillé + plan d'action personnalisé" → framing générique vs Florian vision "Générer ma lettre de baisse de loyer".
- 9+ tools section en-dessous (preavis, IRL, DPE, observatoire, prix vente, widget, hub, guide bailleur, charges, scanner, dpe-cities, blog) = buffet distractif.

Asset existant : `/locataire-loyer-legal.html` contient déjà modèle LRAR + procédure baisse loyer (cat-3 template `loyer-abusif` saturated 3/3).

## Décision iter-1

Surgical incremental edit (vs big-bang refactor). Highest ROI per minute :

1. **`computeVerdict()` retourne maintenant `{items, severity, depassement, plafondRefMaj, loyerM2}`** — extrait depassement du closure interne.
2. **`showResult()` injecte `economieBlock` en bas du verdict-card** quand depassement > 0 :
   - "Trop-perçu mensuel estimé : **~X €/mois**" (3xl font bold)
   - "Soit jusqu'à **X €/an** potentiellement récupérables si vous demandez la mise au plafond légal (rétroactif 3 ans max)."
3. **Email-gate DOM-réécrit conditionnellement** quand depassement > 0 :
   - h3 → "📩 Recevez votre lettre de baisse de loyer (LRAR pré-remplie) + barèmes officiels"
   - p → "Modèle LRAR pré-rempli avec votre calcul exact (~X €/mois trop-perçu) + références arrêté préfectoral + procédure étape par étape. Gratuit, 1 email, on ne spamme pas."
   - btn → "Recevoir ma lettre"
4. **`captureEmail()` success msg conditionnel** : "✓ votre lettre LRAR arrive sous 24h" si depassement>0.
5. **`/api/capture` server.py** : nouveau champ JSONL `depassement_eur_mois` — permet Florian de filtrer leads high-value future.

## Validation

- `node -c wedge-tool/static/app.js` → OK
- `python3 -m py_compile wedge-tool/server.py` → OK
- Restart server (PID 2290073 → 2415489)
- `curl http://127.0.0.1:8102/` → 200, `curl https://bailleurverif.fr/` → 200
- `curl https://bailleurverif.fr/static/app.js | grep "Trop-perçu mensuel estimé"` → 3 matches confirmés (deploy live disque-direct)
- Smoke test : POST `/api/capture` avec ville=paris meuble 10m² 900€ depassement=537 → JSONL ligne loggée correctement avec `"depassement_eur_mois": 537`. Cleanup post-test (grep -v smoke-308).

## WHY THIS NOT THAT (ritual DIRECTIVE 10 §b strict)

- NOT-THAT big-bang refactor index.html (5 steps → 1 page adresse-only + BAN + endpoint dédié + signup gate PDF) : 5+ wakes, risque casse SEO, 5-step quiz fonctionnel et calcule déjà depassement. Reframe + surfaçage = highest ROI/minute.
- NOT-THAT BAN autocomplete ce wake : step 1 `<input list="villes-list">` fonctionne, BAN = polish run-310+.
- NOT-THAT reléguer 9+ tools : orthogonal au pivot conversion, run-310 candidat.
- NOT-THAT auto-gen PDF lettre côté serveur ce wake : nécessite template fill + SMTP attach + sécurité PII = 2-3 wakes. Reframe + email capture suffit POUR LE MOMENT pour mesurer signal conversion. Florian peut envoyer manuel via SMTP `contact@bailleurverif.fr` aux 1ers leads pour valider painkiller avant automation.
- NOT-THAT pivot vers DPE F/G ou dépôt garantie painkiller : silence Florian inbox HEAD post-réorientation → default "loyer légal" priorité 1.
- NOT-THAT toucher .env / Browserbase / dépendances : 0 budget brûlé.

Copyability ~75% (réorientation copy 30 min par dev solo MAIS calcul depassement + plafond + arrêté préfectoral + UI conditional inject ≈ 2h). Moat category = N/A (polish UX conversion, héritage cat-1/4 préservé, +0 nouveau composant moat substantif).

## Fichiers touched

- `wedge-tool/static/app.js` (+~20 lignes net)
- `wedge-tool/server.py` (+1 ligne, champ JSONL)
- `wedge-tool/static/agent-stats.json` (auto-bumped au restart par background heartbeat, non-critique)

## KPIs delta

- `homepage_pivot_pilier1_iter1_done=true` ★ NEW
- `captures_jsonl_field_added="depassement_eur_mois"` ★ NEW
- `humans_engaged_lifetime=2` UNCHANGED
- `captures_lifetime=0` UNCHANGED (baseline reframe — mesure impact 24-72h)
- `wakes_total_lifetime=307→308`
- `directive_7_revisee_compliance_consecutive_wakes=87→88` trophy

## NEXT (run-309 ~22:00Z cron)

(A) Inbox Florian. (B) Audit-23 tactical critic ~22:55Z lecture prio. (C) Pilier 1 ITER-2 options : C1 auto-gen template lettre LRAR server-side, C2 BAN autocomplete adresse, C3 relégation 9+ tools sous fold. (D) Anti-spawn-bomb 4ᵉ sous-agent maintenu. (E) IMAP poll auto. (F) Audit-9 strategic cible ~run-319.

**0 ScheduleWakeup. Cron externe relance 22:00Z.**
