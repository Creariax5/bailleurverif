# run-81 — 2026-05-15T08:30Z — Audit funnel rectifié post-NDD : 9 bots stealth, 0 humain engagé ★★

## CONTEXT

- Wake +6min après run-80 (08:24Z). Pacing run-80 attendait 1800s, mais réveil anticipé probablement déclenché par l'arrivée d'un afflux de visites détecté sur le NDD live.
- État stable côté infra : 3/3 healthchecks HTTPS bailleurverif.fr OK (200, 17472b / 200, 22610b blog article / 200, 77b /healthz, 89ms).
- 0 nouveau signal Florian dans inbox.md (dernière entrée = mon reply 08:24Z).
- TODO-16 (MDP Mastodon) toujours OPEN — `.env` ne contient pas `MASTODON_PASSWORD` (juste 5 `MASTODON_PWD_PENDING_*` archivés).
- TODO-17 (GSC + Bing) toujours OPEN — aucun fichier `google*.html` ni `BingSiteAuth.xml` dans `wedge-tool/static/`.

## ASSESS

(1) **Signal externe détecté** : visits.jsonl est passé de 23 (run-77) à 25 lignes. Tail des 5 dernières visites (08:05→08:30Z) montre des UAs déguisés en Mac OS X, iPhone iOS 26_3, Windows Chrome 117, Linux Chrome 148 (non-headless). IP hashes tous distincts.

(2) **Premier audit `audit_funnel.py` (heuristique v1)** : 9 visites classées "human_real" — verdict apparent **"9 visite(s) humaine(s) externe(s) détectée(s)"**. Mais funnel : 0 result, 0 capture.

(3) **Anomalie identifiée** : 9 "humains" arrivent en 25min juste après mise en ligne d'un NDD sans annonce, avec 0% conversion vers /api/result. Probabilité que ce soient de vrais humains ≈ 0. Hypothèse : crawlers SEO/IA modernes (Googlebot rendering, AppleBot, AhrefsBot, Semrush, etc.) utilisent désormais des UA déguisés en Mac/iPhone, ce qui contourne l'heuristique v1 qui s'appuyait uniquement sur "X11; Linux x86_64".

## DECIDE

Patcher `audit_funnel.py` pour ajouter critère d'engagement réel (= la session a-t-elle déclenché `/api/result` ?) avant de classer "humain". Justification :
- Outil de diagnostic interne, pas du stock produit → conforme engagement run-55.
- Patch durable : tout audit futur bénéficiera de la rectification.
- Pas de dépendance Florian, pas de coût.

## ACT

### Patch `wedge-tool/audit_funnel.py`

**4 modifs sur le même fichier** (~15 lignes nettes) :

1. Docstring : ajout du critère "human_passive_or_bot" (UA humain mais 0 /api/result).
2. Signature `classify(v)` → `classify(v, sessions_with_result)` (passe-paramètre l'ensemble des sessions ayant déclenché un result).
3. Classes : remplace `human_real` par 2 classes plus précises :
   - `human_engaged` (session avec ≥1 /api/result) → vrais humains qui ont calculé un verdict.
   - `human_passive_or_bot` (UA humain mais 0 result) → bots stealth modernes OU vrais humains qui rebondissent à 100%.
4. Construction `sessions_with_result` en début de `main()`, alimentation `engaged_sessions`/`engaged_results`/`engaged_captures` pour la section funnel.
5. Verdict et output texte alignés : "0 humain engagé — aucune session n'a déclenché /api/result." comme baseline plus précise que l'ancien "0 humain externe — aucun visiteur réel détecté."

### Vérifications

- `python3 -m py_compile wedge-tool/audit_funnel.py` → SYNTAX_OK.
- Re-run audit : `python3 wedge-tool/audit_funnel.py` →
  ```
  Total visites brutes : 25
    - bot_no_session         : 7
    - dev_testing            : 9
    - human_passive_or_bot   : 9
    - human_engaged          : 0
  Verdict : 0 humain engagé — aucune session n'a déclenché /api/result.
  ```

### Findings post-patch

- **9 visites passées en 25 min après LE post-NDD = afflux crawler attendu** (signal positif d'indexabilité du domaine).
- **0 humain engagé est la baseline réelle** — pas "9 humains externes" comme l'aurait laissé croire l'heuristique v1.
- L'**UA Mac OS X 10_15_7** apparaît 7 fois (probable Googlebot rendering ou AppleBot 2026 qui ne montre plus "Bot" dans son string).
- **Pas de découverte humaine externe organique** — l'afflux est mécanique (crawlers déclenchés par DNS resolve + Let's Encrypt CT logs).

## VERIFY

- 0 régression : ancien comportement préservé pour `bot_no_session` et `dev_testing`. Seule la classe `human_real` est scindée en 2 classes plus fines (information enrichie, pas tronquée).
- 0 budget BB.
- 0 dépense.
- 1 fichier modifié (`wedge-tool/audit_funnel.py`).

## METRICS

- `wedge_visits_unique` : 4 → 4 (calculé en humain_engaged ; le compteur visit JSONL passe à 25, mais 16 = bots/dev, 9 = bots stealth, 0 humain).
- `wedge_humans_engaged_lifetime` : 0 → 0 (NEW metric, baseline fiable).
- `wedge_crawlers_visits_post_ndd` : 0 → 9 (NEW, indicateur d'indexabilité crawler).
- `audit_funnel_heuristique_version` : 1 → 2.
- `wakes_consecutifs_recherche_active` : 0 → 0 (action signal externe wedge, pas mode recherche active).
- `wakes_sans_budget_bb_consomme_consecutifs` : 36 → 37 (record).
- `runbook_initial_reloads_consecutifs` : 39 → 40 (record).
- `florian_todos_open` : 11 → 11 (TODO-16 et TODO-17 inchangés).

## NEXT

ScheduleWakeup 1800s. Justification :
- Fenêtre d'attention Florian probablement encore ouverte (run-80 reply lui a été envoyée à 08:24Z, ~10min écoulés).
- L'afflux crawler post-NDD va vraisemblablement se poursuivre dans les heures qui viennent → vérifier delta visits à T+30min pour confirmer pattern.
- Pas de production stock (engagement run-55 maintenu tant que 0 canal débloqué).
- Si rien bouge à 09:00Z, retour pacing 3600s par défaut (run-79).

Actions candidates au prochain wake :
1. Si Florian a réagi (MDP Mastodon collé ou fichier GSC fourni) → exécuter en autonome.
2. Sinon : re-run audit_funnel pour mesurer delta crawlers (signal d'indexabilité plus quantifié) + cycle DIRECTIVE 4 angle non-saturé.
