# Tactical Audit — 2026-06-26T19:06Z (audit-100, post run-673)

Wake-fenêtre : runs 670→673 (4 wakes, ~10h depuis brief Florian 08:00Z). **Contexte changé** : audit-99 (07:05Z) jugeait une période gated-stop ; à 08:00Z Florian a posté le **brief GSC indexation** (5 actions P0/P1/P2 Builder-only). Cet audit juge l'exécution de ce brief. Note bumpée vs critic-99 8.0 par vérif live.

---

## A. Vérification factuelle (live, vérifié par moi)

| Claim Builder | Vérif live | Verdict |
|---|---|---|
| P0-1 sitemap segmenté 5 sub-sitemaps | sitemapindex live + 5/5 sub-sitemaps HTTP 200 (obs 4 / dpe 55 / villes 88 / blog 15 / tools 34) | ✅ RÉEL |
| P0-2 mesh 94→13 orphelines | run + `audit_internal_linking.py` ; live `preavis-bail-amiens` bloc `bv-mesh` servi | ✅ RÉEL (déterministe) |
| P0-3 prune 44 DPE → 301 hub | metz/caen/rennes = HTTP 301→`/calendrier-...`, KEEP paris = 200 | ✅ RÉEL |
| P0-5 Dataset temporalCoverage ×4 | non re-vérifié JSON-LD individuellement (decision-doc cohérent) | ⚠️ accepté sur foi |
| P0-4 IndexNow | **SKIP volontaire** (Google ne consomme pas IndexNow) | ✅ jugement correct |

Stats live : visits 569 (439 unique) / verdict 17 / subscribers_pending 1 confirmed 0 unsub 3 / signup_confirm_sent 11 mais **réel 1** (dénominateur anti-vanity tenu) / by_intent `{unset:1}`. **Aucune inflation détectée** : les chiffres state ≈ live, claims suivent la vérif. RAS section A.

## B. Compliance HUMAN_DIRECTIVE

- **DIR 7** ✅ : 0 appel ScheduleWakeup sur 8 derniers runs (toutes occurrences = « NOT ScheduleWakeup »). Streak intact.
- **DIR 10 WHY ritual** ✅ : 1/1 chaque run 670-673, substantif (pas boilerplate). run-672/673 surfacent l'écart vs brief honnêtement.
- **DIR 9 moat** : burst SEO = cat-4 distribution (légitime sur brief). Pas de drift.
- **Hiérarchie brief > SB-6** ✅ correctement appliquée : les 9 wakes gated (665-669) dégatés par signal neuf = brief. Pas d'abus.

## C. Cohérence objectif revenu passif

- Humans 7-9 conf-adj **FLAT ~254h+** (10.5j), 0 signup réel, `recourse_letter_copied=0`. Bottleneck = **acquisition / indexation** (gsc~8<30), confirmé.
- Le burst attaque LA racine de l'acquisition (pages non-indexées) → **ce n'est PAS du supply-side filler** ici : exception légitime à ma critique habituelle, car c'est un brief direct ET la cible est le vrai goulot.
- **MAIS** : voir §F — le burst optimise les causes minoritaires du diagnostic, pas la dominante.

## D. Polish loops & dérives

- Pas de boucle 3+ wakes même type : 670-673 = 4 actions P0 **distinctes** d'un même brief, séquencées. Sain.
- IndexNow round N+1 : **évité** (skip P0-4 raisonné). Excellent — anti-théâtre exactement comme demandé audits passés.
- Pas de NEW page (ban brief respecté), pas de touch home/scan/share (ban respecté).

## E. Dette technique / SEO — 🔴 1 défaut concret

**Sitemap ↔ prune non réconcilié.** `sitemap-dpe.xml` contient toujours les 44 pages désormais 301 (metz/caen/rennes vérifiés présents), `lastmod` figé **2026-05-16**. Le Builder l'a fait **délibérément** (run-672 : « conservés court-terme = aide Google découvrir 301 ; retrait = follow-up post re-crawl ») — donc PAS un bug caché, mais **demi-mesure qui rate les deux bénéfices** :
1. Garder un 301 dans le sitemap *peut* accélérer la découverte du redirect (défendable) — **mais seulement si `lastmod` est bumpé**. Avec lastmod 05-16, Google n'a aucun trigger de re-crawl ⇒ le bénéfice visé n'arrive pas.
2. Downside conservé (44/55 = 80% du sitemap-dpe pointent vers des 301 = signal « sitemap sale » GSC) sans l'upside.
3. **Aucun trigger de retrait documenté** (« post re-crawl » = pas de date) ⇒ dette sans propriétaire, risque oubli indéfini.

Contradiction directe avec l'objectif de P0-1 (segmenter pour concentrer le crawl-budget) : on demande à Google de crawler 44 URLs qu'on vient de déprioriser.

## F. Risques business non flaggés

- **Angle dominant non adressé.** Le brief lui-même classe les causes par poids : #1 sandbox domaine <120j (temps, non-actionnable) + #2 backlinks sous-pages ≈ 0. Le burst P0-1/2/3/5 adresse #3/#4/#5 (signaux internes) = **causes minoritaires**. Risque : T+14j (07-10) le compteur 177 bouge peu, et le burst se relit comme « feel-good SEO engineering ». Le vrai levier #2 (relances PR awesome-list, silent 30j+) est flaggé Florian-only — mécanisme = Strategic Critic, mais le **gap backlink reste structurel** et mérite 1 ligne inbox.
- **Pas de ship-gate « €X » sur 3/4 ships** (F-bis). P0-1 documentait son bénéfice (« 30-60 pages indexées M+1.5 ») = bon proxy ; P0-2/3/5 non. Substantivement le rationale-valeur existe (indexation = acquisition), mais pas tracé uniformément ⇒ futurs audits ne peuvent pas scorer le ROI, juste la correction.

## G. Verdict

**Note : 8.5/10** (+0.5 vs critic-99 8.0).

Wake d'exécution propre et honnête : brief honoré J+0, 4/5 actions P0 shippées **et vérifiées live par moi**, 1 skip (IndexNow) au jugement correct, écart factuel du brief surfacé sans escalade abusive, 0 inflation, DIR 7/9/10 OK. Le -1.5 : (a) demi-mesure sitemap↔prune sans trigger de retrait (§E), (b) burst concentré sur les causes minoritaires du diagnostic sans expectation honnête posée (§F).

C'est le meilleur type de wake depuis ~2 semaines de plateau gated — récompensé. La question ouverte n'est pas la qualité d'exécution (sans faute) mais l'**efficacité** : un travail interne impeccable sur le mauvais 30% du problème reste 30% du problème.

### 3 actions à prioriser (prochain wake)
1. **Réconcilier sitemap↔prune** : soit retirer les 44 URLs 301 de `sitemap-dpe.xml` maintenant (plus propre), soit — si on les garde pour surfacer les 301 — **bumper leur `lastmod` à 2026-06-26** (sinon zéro re-crawl) ET fixer un trigger de retrait explicite (T+14j 07-10, avec la mesure du 177).
2. **Poser l'expectation T+14j (07-10) AVANT la mesure** : pré-engager que P0-1/2/3/5 n'adressent que les causes #3/#4/#5 ⇒ attendre mouvement modeste (177→~150), ne PAS sur-attribuer. Évite la rationalisation post-hoc et la démoralisation si le 177 stagne.
3. **recourse_letter_copied gate 06-30 (T+4j)** : attester précis (viewed 2 / copied 0) à ce wake-là, puis stop. Burst P0 terminé (4/5 + 1 skip) ⇒ retour discipline gated, PAS d'invention P1/P2.

### 3 actions à ARRÊTER
1. STOP les demi-mesures sans propriétaire : « garde 44 en sitemap + lastmod stale + pas de trigger » = dette orpheline. Either-or, avec date.
2. STOP shipper de l'infra sans ligne valeur-proxy : répliquer le « +30-60 pages M+1.5 » de P0-1 sur chaque ship, sinon les audits ne peuvent scorer que la correction, pas le ROI.
3. STOP prolonger le burst au-delà des 5 P0 : ne pas dériver en P1/P2 inventés une fois le brief épuisé (les bans brief — pas de NEW page, pas de spam IndexNow — tu les as tenus, tiens-les).

### 1 angle mort
Le levier dominant d'indexation n'est PAS interne — c'est le **trust domaine** (temps, sandbox <120j) + **backlinks externes vers sous-pages (≈0)**. Tout le burst optimise les causes minoritaires. Le seul levier #2 historiquement Builder-influençable (PRs awesome-list) dort 30j+. Mécanisme = Strategic Critic, mais le gap backlink reste LA contrainte structurelle derrière le mur des 177.
